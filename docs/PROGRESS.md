# PROGRESS

> Diperbarui setiap task selesai. Status: ⬜ belum · 🟨 berjalan · ✅ selesai

## Perbaikan cacat spesifikasi: hari_wajib tanpa tanggal mulai (22 Agustus 2026)

**Ditemukan lewat data uji Checkpoint 3 (bukan bug kode — cacat di rumus `03-CALC-SPEC.md` §3 versi awal).** Toyib bekerja 5 hari, selesai 3, tapi `hari_wajib` lama dihitung dari tanggal 1 bulan berjalan → tercatat bolong 16 hari. Kalau dipakai sungguhan, di bulan pertama peluncuran **seluruh 36 karyawan** akan kehilangan bonus Rp500.000 bukan karena lalai, tapi karena sistemnya baru menyala. User memperbaiki spesifikasi, saya menjalankan perbaikannya.

**Rumus baru:** `hari_wajib` dihitung dari **tanggal mulai** = yang paling akhir di antara (1) tanggal 1 bulan berjalan, (2) `policy.pte_mulai_berlaku` — kalau `null`, `hari_wajib = 0` untuk SEMUA orang, (3) `profile.mulai_kerja`.

- `supabase/migrations/0005_pte_mulai_berlaku.sql` — kolom `profile.mulai_kerja` (date, nullable) + seed `policy.pte_mulai_berlaku = null`. Dijalankan, kolom & seed diverifikasi lewat query langsung.
- `supabase/migrations/0006_marketing_view_perbaikan.sql` — **migrasi baru, bukan edit `0004_marketing_view.sql`** yang sudah pernah jalan (riwayat migrasi tidak diubah diam-diam). **Catatan penting**: blok SQL `v_marketing_bulanan` di `03-CALC-SPEC.md` §3 sendiri **belum diperbarui** — cuma prosa/tabelnya yang diedit user, SQL di dokumen masih versi lama. View di migrasi ini ditulis mengikuti ATURAN BARU di prosa, bukan menyalin SQL basi di dokumen — perlu diberitahukan ke user supaya dokumennya ikut diperbaiki.
- **Jebakan teknis ditemukan saat menulis SQL:** fungsi `greatest()` Postgres **mengabaikan NULL** (bukan mem-propagasi NULL seperti kebanyakan fungsi lain) — kalau cuma ditulis `greatest(tgl1, pte_mulai_berlaku, tgl3)` tanpa guard eksplisit, saat `pte_mulai_berlaku` NULL rumusnya akan diam-diam jatuh balik ke `greatest(tgl1, tgl3)` dan tetap menghitung hari_wajib seolah sistem sudah aktif — persis bug yang sedang diperbaiki, muncul lagi lewat jalur berbeda. Ditambahkan `where p.pte_mulai_berlaku is not null` eksplisit di view.
- `lib/api/pte.ts` — `hitungKelayakanBonus()` dan `hitungPotongan()` ditambahkan sebagai **satu-satunya tempat** rumus bonus/potongan boleh dihitung; keduanya mengembalikan `{ berlaku: false }` (tanpa field angka sama sekali) kalau `policy.pte_mulai_berlaku` masih `null` — "belum berlaku" tertanam di *tipe* return-nya, bukan cuma konvensi, supaya UI masa depan (Task 21/22) tidak bisa lupa memeriksanya sebelum membaca `.layak`/`.nominal`/`.potongan`. Belum ada UI yang menampilkan angka ini sama sekali sampai titik ini (bukan retrofit — memang belum dibangun), jadi ini infrastruktur untuk konsumen masa depan.

**Diverifikasi lewat DB sungguhan:**
1. Dengan `pte_mulai_berlaku` masih `null` (keadaan asli sebelum diuji): `hari_wajib = 0` untuk Toyib walau `pte_daily` historisnya masih ada (`hari_lengkap=3`, `hari_bolong=-3` — nilai mentah view, negatif karena `0-3`; tidak dibersihkan jadi 0 karena UI diinstruksikan menyembunyikan angka sepenuhnya saat `berlaku=false`, jadi nilai mentah ini tidak pernah tampil ke pengguna).
2. Dengan `pte_mulai_berlaku` di-set sementara ke `'2026-08-18'` (tanggal mulai data uji Toyib) untuk pengujian: **`hari_wajib=5, hari_bolong=2`** — persis harapan user.
3. `hitungKelayakanBonus`/`hitungPotongan` diuji 6 skenario langsung: `berlaku:false` tanpa angka apa pun saat null; `no_gap` dengan bolong→`layak:false,nominal:0`; `no_gap` tanpa bolong→`layak:true,nominal:500000`; `per_day` proporsional→`nominal:300000` (`round(500000*3/5)`); `potongan:0` saat closing≥target; `potongan:300000` saat closing<target. Semua cocok.
4. **Dibersihkan setelahnya**: `pte_mulai_berlaku` dikembalikan ke `null` (nilai produksi sesungguhnya — CEO belum mengumumkan tanggal aktivasi), data uji 5-hari + 3 closing dihapus, dicek ulang `count(*)=0` untuk keduanya.

**Koreksi lanjutan (23 Agustus 2026):** user memperbaiki blok SQL `v_marketing_bulanan` di `03-CALC-SPEC.md` §3 (yang di poin sebelumnya saya laporkan masih basi). Dibandingkan lewat `diff` — beda nyata, bukan kosmetik: (1) kolom baru **`pte_berlaku`** (boolean) sebagai satu sumber kebenaran, dipakai `hitungKelayakanBonus`/`hitungPotongan` sekarang alih-alih membaca ulang `policy.pte_mulai_berlaku` sendiri-sendiri; (2) `hari_bolong` di-**`greatest(...,0)`** — inilah yang memperbaiki nilai `-3` aneh yang saya laporkan di poin sebelumnya; (3) `nullif` guard tambahan. Ditulis sebagai migrasi baru `0007_marketing_view_koreksi.sql` (bukan edit `0006`), diverifikasi cocok 100% lewat `diff` sebelum dijalankan. **Ditemukan saat menjalankan**: `create or replace view` menolak — Postgres tidak mengizinkan menyisipkan kolom baru di TENGAH urutan kolom lama (`pte_berlaku` sebelum `hari_wajib`), cuma boleh menambah di ujung. Diperbaiki jadi `drop view` lalu `create view`. Diverifikasi ulang: `pte_berlaku=false`, `hari_wajib=0`, `hari_bolong=0` (bukan `-3` lagi) untuk Toyib saat `pte_mulai_berlaku` null; `security_invoker` tetap `on` setelah drop+recreate; 6 skenario `hitungKelayakanBonus`/`hitungPotongan` dites ulang dengan signature baru (`pteBerlaku` sebagai parameter, bukan dibaca dari `policy`) — semua cocok.

**Utang wajib sebelum sistem menghitung gaji sungguhan** (dicatat eksplisit atas instruksi user): **hari cuti/sakit/izin yang disetujui juga tidak boleh dihitung bolong.** Datanya ada di laporan HRD, yang formnya baru dibangun Task 14. Sampai form HRD dan integrasinya ke `hari_wajib`/`hari_bolong` selesai, karyawan yang cuti resmi akan tetap tercatat bolong secara tidak adil — **ini blocker sebelum go-live untuk penggajian sungguhan**, bukan cuma catatan biasa.

---

## Revisi Batch D — satu angka, satu pengisi (D1-D4, 23 Agustus 2026)

Setelah commit awal Task 14/15 (`d729632`), user meninjau `pic_lokasi` vs `pembangunan` vs `security` dan menemukan tumpang tindih: beberapa besaran ditanyakan ke DUA orang berbeda. `BLUEPRINT.md` §3.5b ditambahkan user sendiri untuk membedakan **duplikasi** (dua orang mengetik hal yang SAMA, hapus salah satu) dari **silang-cek** (dua orang mengukur hal yang BERBEDA yang seharusnya mirip, pertahankan keduanya, munculkan selisihnya). Empat keputusan (D1-D4):

- **D1 (silang-cek, bukan duplikasi):** tamu masuk gerbang (Security) vs konsumen dilayani (PIC Lokasi) TETAP dua field terpisah -- tidak ada perubahan kode. Ditambahkan ke daftar silang-cek otomatis `05-RENCANA-FASE-2.md` §F2-3 sebagai item Fase 2, TIDAK dibangun sekarang.
- **D2 (jangan tanya yang sistem sudah tahu):** field `foto_progress_jumlah`, `video_progress_jumlah`, `progress_dikirim_it` DIHAPUS dari `pic_lokasi` (blok 3) dan `pembangunan` (blok kualitas) -- jawabannya sudah ada di tabel `attachment` (siapa yang unggah). Dicatat sebagai syarat wajib untuk form `it` (Task 14 sisa, belum dibangun): bagian "PIC lokasi yang belum kirim foto/video" WAJIB dihitung dari `attachment`, bukan field yang diketik PIC IT -- lihat catatan di baris Task 14.
- **D3 (duplikasi, PIC Lokasi sumbernya):** field material kurang + estimasi biaya di `pembangunan` DIHAPUS, diganti rollup baca-saja dari laporan `pic_lokasi` hari ini (PIC yang lihat langsung tumpukan material). Ditambahkan blok BARU "Material Tingkat Perusahaan" (`material_perusahaan`) di `pembangunan` untuk hal yang cuma Kepala Pembangunan tahu: pembelian borongan, masalah supplier, pasokan DTI -- bukan per lokasi.
- **D4 (bukan duplikasi, tapi field harus dipisah tegas):** `pic_lokasi` blok 4 sekarang CUMA kondisi hari ini (status jalan/listrik/air + drainase/penerangan/gerbang + kebutuhan sebagai observasi) -- field `infrastruktur_estimasi_biaya` (anggaran) DIHAPUS dari situ. `pembangunan` blok "kondisi infrastruktur" (yang tadinya menanyakan hal SAMA dengan PIC) DIHAPUS, diganti rollup baca-saja dari `pic_lokasi`; ditambahkan blok BARU "Rencana & Biaya Infrastruktur" (`infrastruktur_rencana`) untuk hal yang cuma Kepala Pembangunan tahu: pekerjaan dijadwalkan, kontraktor, anggaran, target selesai.

**Implementasi:**
- `supabase/migrations/0008_pembangunan_per_lokasi_view.sql` -- **dijalankan langsung** (`npm run db`, CREATE VIEW bukan DDL destruktif, user sudah pra-setujui tanpa perlu tunggu) dan diverifikasi: `v_pembangunan_per_lokasi` ada, `security_invoker=on` terpasang. View ini untuk agregasi skalar sederhana di dashboard CEO (Task 20) -- **form `pembangunan` sendiri TIDAK memakainya**, karena butuh menampilkan data tabel/status bersarang (material_kurang, status jalan, dst.) yang sulit dimodelkan lewat SQL view datar.
- `lib/api/pembangunan.ts` -- `useRekapPicLokasi()` (baru, satu-satunya hook yang dipakai) membaca `report.data` langsung + join nama lokasi, mem-parse jadi tiga bentuk (`unit`/`material`/`infrastruktur`) untuk tiga blok baca-saja di `pembangunan`. Hook awal (`useRekapPembangunanLokasi`, versi-view, deprecated) DIHAPUS -- tidak ada yang memakainya lagi, kode mati.
- `components/LaporForm.tsx` -- tiga komponen baca-saja baru: `RekapUnitOtomatis` (sudah ada sejak commit awal, sekarang sumbernya `useRekapPicLokasi` bukan view), `RekapMaterialOtomatis`, `RekapInfrastrukturOtomatis`. Ketiganya dirender SEBELUM `<FormRenderer>`, BUKAN sebagai entri di `schema.blocks` (lihat poin bug di bawah).
- `forms/f15-pembangunan.ts` -- blok 1/3/5 (baca-saja) dihapus total dari `blocks` array (dulu sempat ditulis sebagai entri ber-`fields: []`, dokumentasi keliru menganggap itu aman). Blok baru: `catatan_selisih` (2), `material_perusahaan` (4), `infrastruktur_rencana` (6).
- `forms/f13-pic-lokasi.ts` -- blok 3: 3 field D2 dihapus. Blok 4: `infrastruktur_estimasi_biaya` dihapus (D4).
- `components/fields/Tabel.tsx` + `forms/types.ts` -- `kolom[].pilihan?: string[]` baru, kolom tabel bisa jadi dropdown (dipakai `perizinan` blok "Tahapan" -- perbaikan kecil yang menyertai, bukan bagian D1-D4).
- `lib/api/pembangunan.ts` -- perbaikan tipe: embed `report.lokasi_id -> lokasi` di-cast lewat `unknown` dulu, karena tipe bawaan `supabase-js` keliru menduganya array (proyek ini tidak memakai tipe skema hasil generate).

**BUG ditemukan & diperbaiki saat verifikasi ulang (bukan di commit awal):** `FormRenderer` merender SETIAP entri `schema.blocks` apa adanya, TERMASUK yang `fields: []` -- kalau blok baca-saja (1/3/5) tetap ditulis sebagai entri kosong di `blocks` (seperti di draf awal sebelum revisi ini), judul & catatannya akan tampil DUA KALI: sekali dari komponen baca-saja kustom, sekali lagi dari fieldset kosong `FormRenderer`. Diperbaiki dengan menghapus total ketiga entri itu dari `blocks`, persis pola yang sudah dipakai Blok 1/5/8 di `f01-personal-marketing.ts` sejak Task 12 -- seharusnya diikuti sejak awal, bukan ditemukan belakangan.

**🔴 CELAH RLS BELUM DITUTUP -- BLOCKER untuk D3/D4 berfungsi di produksi.** `can_see_report()` (`0002_rls.sql` §3.1) tidak memberi role `kadiv` akses baca ke laporan `pic_lokasi` milik orang lain -- hanya `author=diri sendiri`, `ceo`, `pusat` (`f<>'accounting'`), `kontrol_marketing`+`f='personal_marketing'`, `accounting`+3 form tertentu, `manager_resto`+`f='personal_marketing'`. Kepala Pembangunan SUNGGUHAN (Ronald, `DATA-KARYAWAN.md` baris 30: role `kadiv`+`karyawan`, BUKAN `ceo`/`pusat`) **tidak masuk kategori mana pun** -- berarti ketiga blok rollup baca-saja (1/3/5) akan tampil **KOSONG** baginya walau semua PIC Lokasi sudah kirim laporan, membuat seluruh desain D3/D4 tidak berfungsi untuk pengguna sungguhan.

Dikonfirmasi KONKRET (bukan cuma dibaca dari kode) lewat `scripts/uji-rls-gap-pembangunan.mjs`: profil+role palsu dibuat persis meniru Ronald (`kadiv`+`karyawan`, di dalam transaksi, di-ROLLBACK) → **0 baris** pic_lokasi terlihat, walau Dadang sudah kirim laporan di transaksi yang sama. Sisi sebaliknya (mekanisme BENAR untuk role yang MEMANG diberi akses) diverifikasi lewat `scripts/uji-rollup-pembangunan.mjs`: sebagai Putri (`ceo`), query yang sama persis mengembalikan data yang cocok 100% dengan yang dikirim Dadang, dan parsing `asInt`/`asBool`/`asStr` semuanya benar.

**Ini SENGAJA tidak diperbaiki tanpa izin** -- mengubah `can_see_report()` menyentuh RLS, dan ada beberapa cara mendesainnya dengan implikasi berbeda:
- **(a) Berbasis assignment** (paling presisi): `exists (select 1 from assignment a where a.user_id=auth.uid() and a.form_key='pembangunan') and f='pic_lokasi'` -- cuma orang yang SUNGGUH ditugaskan form `pembangunan` yang dapat akses, cuma ke `pic_lokasi`, tidak lebih.
- **(b) Berbasis role `kadiv`** (lebih sederhana, tapi lebih longgar): `has_role('kadiv') and f='pic_lokasi'` -- otomatis memberi SEMUA kadiv (HRD, CS, Perizinan, IT, DTI juga, lihat `DATA-KARYAWAN.md`) akses ke laporan pic_lokasi, walau cuma Kepala Pembangunan yang butuh.
- **(c) Role baru khusus**: tambah `'pembangunan'` ke enum `role` (butuh migrasi ubah constraint), assign eksplisit ke Ronald, `has_role('pembangunan') and f='pic_lokasi'` -- paling eksplisit/gampang ditelusuri, tapi butuh migrasi tambahan + isi data.

Menunggu keputusan user opsi mana. Sampai itu terjadi, kode yang ada BENAR secara RLS (tidak membocorkan apa pun) tapi fitur rollup D3/D4 belum berfungsi untuk Kepala Pembangunan sungguhan -- dicatat juga di komentar `lib/api/pembangunan.ts` dan `supabase/migrations/0008_pembangunan_per_lokasi_view.sql` supaya tidak lupa.

### ✅ Diselesaikan — opsi (d): view agregat security-definer (23 Agustus 2026)

User menolak ketiga opsi (a/b/c) di atas -- semuanya masih memberi Ronald akses ke BARIS `pic_lokasi` (termasuk keluhan konsumen, nama, permintaan keputusan CEO milik Dadang/Jery), padahal dia cuma butuh angkanya. `04-CATATAN-TEKNIS.md` §3.4b ditambahkan user: pola view agregat `security_invoker = off` dengan penjaga `boleh_lihat_rekap()` di `where`-nya, sama seperti `v_keuangan_rekap` (Ibu Sabrina tidak dapat baris Accounting, cuma 4 angka).

**`supabase/migrations/0009_boleh_lihat_rekap.sql`** (dijalankan langsung, `create or replace function` + `drop view`/`create view` -- bukan DDL yang membutuhkan izin terpisah karena bagian dari implementasi yang eksplisit diminta):
- `boleh_lihat_rekap(untuk_form text)` -- `security definer`, true kalau `ceo`, `pusat`, atau punya baris `assignment` untuk `untuk_form` itu.
- `v_pembangunan_per_lokasi` ditulis ULANG total: `security_invoker = off` (bukan `on` seperti migrasi 0008 sebelumnya -- SENGAJA, penjaganya ada di `where public.boleh_lihat_rekap('pembangunan')`), dan kolomnya diperluas mencakup rekap material + kondisi infrastruktur (bukan cuma unit seperti sebelumnya) -- TAPI **cuma angka/status terstruktur**: `material_kurang` (array nama material + catatan bebas per baris) diringkas jadi `material_kurang_jumlah` (hitungan, bukan isi); `kiriman_kekurangan` dan `infrastruktur_kebutuhan` (teks_panjang bebas) **tidak dimasukkan sama sekali** -- tidak ada padanan angka yang aman untuk keduanya, jadi dibuang, bukan disamarkan.

**`lib/api/pembangunan.ts`** ditulis ulang total: `useRekapPicLokasi()` (query mentah ke `report.data`, TIDAK PERNAH BISA jalan untuk Ronald) dihapus, diganti `useRekapPembangunanPerLokasi()` yang query `v_pembangunan_per_lokasi` langsung -- satu query gabungan utk blok 1/3/5 (dulu 3 hook terpisah). **`components/LaporForm.tsx`**: 3 komponen baca-saja lama disatukan sumbernya jadi satu `data` prop (dari satu query), tampilan tetap 3 seksi terpisah (1/3/5) sesuai penomoran blok skema, tapi `RekapMaterialOtomatis`/`RekapInfrastrukturOtomatis` kehilangan rincian nama material & catatan bebas PIC -- SENGAJA, bukan bug, karena view-nya memang tidak pernah mengirim itu.

**Diverifikasi lewat `scripts/uji-boleh-lihat-rekap.mjs`, tiga uji persis yang diminta, output mentah:**

```
════ UJI 1 — sebagai Ronald, SELECT * FROM v_pembangunan_per_lokasi (RAW OUTPUT) ════
[
  {
    "lokasi": "Tajur", "target": "10", "sedang_dibangun": "4", "finishing": "2",
    "selesai_hari_ini": "1", "belum_mulai": "3", "material_cukup": false,
    "material_kurang_jumlah": "2", "kiriman_precast_jumlah": "12",
    "jalan_status": "Rusak", "listrik_status": "Proses", "air_status": "Sudah",
    "drainase_baik": false, "penerangan_baik": true, "gerbang_baik": true
  }
]
--> 1 baris.
Tidak ada field "RAHASIA" yang bocor lewat view.

════ UJI 2 — sebagai Toyib (karyawan polos, TANPA assignment apa pun) ════
[]
--> 0 baris.

════ UJI 3 — sebagai Ronald, SELECT langsung ke report WHERE form_key='pic_lokasi' ════
[]
--> 0 baris.
```

Data uji Dadang sengaja disisipi field bernama harfiah `"RAHASIA -- ..."` di `kiriman_kekurangan`, `infrastruktur_kebutuhan`, dan `keperluan_konsumen` untuk memastikan tidak ada satu pun bocor lewat view -- dicek otomatis di skrip (`bocor = JSON.stringify(hasil1).includes('RAHASIA')` → `false`). Semua di dalam `BEGIN...ROLLBACK`, dikonfirmasi ulang lewat `count(*)` -- 0 sisa `report`/profil-Ronald-palsu/assignment setelahnya.

`tsc`/`build`/`lint` bersih, `grep` angka bisnis nol hasil di seluruh file yang diubah. `scripts/uji-rls-gap-pembangunan.mjs` dan `scripts/uji-rollup-pembangunan.mjs` (dari opsi yang ditolak) dibiarkan ada sebagai bukti historis kenapa opsi (a/b/c) ditolak -- keduanya menguji jalur row-level yang sekarang sudah tidak dipakai kode aplikasi, tapi tetap valid sebagai dokumentasi teknis alasan penolakan.

#### Koreksi syarat #1: "hanya angka" terlalu ketat (23 Agustus 2026, lanjutan hari yang sama)

User memperbaiki `04-CATATAN-TEKNIS.md` §3.4b syarat #1: patokannya bukan tipe data ("hanya angka"), tapi **apakah isinya bisa ditebak**. Tabel terstruktur yang memang dibutuhkan pekerjaannya -- termasuk `material_kurang` lengkap dengan nama material, kebutuhan, untuk unit, tanggal dibutuhkan -- BOLEH masuk view. Field teks bebas (`infrastruktur_kebutuhan`, `kiriman_kekurangan`) TETAP ditutup, apa pun alasannya. Alasan konkret: "2 material kurang di Tajur" tidak bisa dipakai memesan apa-apa; Ronald perlu tahu material APA yang kurang.

**`supabase/migrations/0010_material_kurang_terstruktur.sql`**: `material_kurang_jumlah` (hitungan) diganti `material_kurang` (jsonb array) -- direkonstruksi lewat `jsonb_build_object` per elemen dengan **whitelist 4 kunci** (`material`, `kebutuhan`, `untuk_unit`, `dibutuhkan_tanggal`), bukan meneruskan array mentah -- kalau form menambah kolom baru di tabel itu suatu saat, view ini TIDAK otomatis ikut membocorkannya.

**BUG ditemukan lewat uji umpan ulang, diperbaiki `0011_perbaikan_fan_out_material_kurang.sql`**: versi pertama (0010) memakai `left join lateral (jsonb_array_elements(...)) mk on true` -- ini MENGGANDAKAN baris `report` sebanyak jumlah elemen `material_kurang` SEBELUM `group by` dijalankan, jadi `sum((r.data->>'target_unit')::int)` ikut terlipat-ganda (lokasi dengan 2 baris material kurang membuat `target_unit=10` terbaca `20` di hasil uji pertama). Classic SQL fan-out trap. Diperbaiki dengan subquery berkorelasi (bukan JOIN) di dalam SELECT list -- tidak pernah menggandakan baris sumber. Postgres tidak punya agregat `max(jsonb)` bawaan (dicek langsung, error "function max(jsonb) does not exist"), jadi dibungkus `max(...::text)::jsonb` -- aman karena `report_uniq` menjamin cuma 1 baris pic_lokasi per lokasi per hari.

**Uji umpan diperkuat** (bukan cuma field teks bebas top-level, sekarang juga kunci liar DI DALAM elemen `material_kurang` itu sendiri): `scripts/uji-boleh-lihat-rekap.mjs` diedit menambah elemen `material_kurang` kedua dengan kunci ekstra `catatan_pic: "RAHASIA -- kunci liar disisipkan..."` yang TIDAK ada di whitelist. Output mentah setelah kedua perbaikan:

```
════ UJI 1 — sebagai Ronald, SELECT * FROM v_pembangunan_per_lokasi (RAW OUTPUT) ════
[
  {
    "lokasi": "Tajur", "target": "10", "sedang_dibangun": "4", "finishing": "2",
    "selesai_hari_ini": "1", "belum_mulai": "3", "material_cukup": false,
    "material_kurang": [
      { "material": "Semen", "kebutuhan": "50 sak", "untuk_unit": "Blok A", "dibutuhkan_tanggal": "besok" },
      { "material": "Pasir", "kebutuhan": "2 truk", "untuk_unit": "Blok B", "dibutuhkan_tanggal": "lusa" }
    ],
    "kiriman_precast_jumlah": "12",
    "jalan_status": "Rusak", "listrik_status": "Proses", "air_status": "Sudah",
    "drainase_baik": false, "penerangan_baik": true, "gerbang_baik": true
  }
]
--> 1 baris.
Tidak ada field "RAHASIA" yang bocor lewat view (termasuk kunci liar di dalam material_kurang).
material_kurang terstruktur (nama "Semen"/"Pasir") IKUT TERBAWA, sesuai perbaikan syarat #1.

════ UJI 2 — sebagai Toyib (karyawan polos, TANPA assignment apa pun) ════
[]
--> 0 baris.

════ UJI 3 — sebagai Ronald, SELECT langsung ke report WHERE form_key='pic_lokasi' ════
[]
--> 0 baris.
```

`target=10` (bukan `20`) mengonfirmasi fan-out sudah beres; `catatan_pic` (kunci liar di dalam elemen kedua) tidak ikut terbawa mengonfirmasi whitelist per-elemen bekerja, bukan cuma meneruskan array. `lib/api/pembangunan.ts` dan `components/LaporForm.tsx` (blok `RekapMaterialOtomatis`) diperbarui menampilkan daftar material lengkap (nama/kebutuhan/untuk unit/tanggal), bukan cuma hitungan. `tsc`/`build`/`lint` bersih, `grep` angka bisnis nol hasil, rollback dikonfirmasi 0 sisa.

**Pelajaran dicatat untuk view lintas-divisi berikutnya**: (1) uji umpan teks mencolok ("RAHASIA") adalah metode WAJIB, bukan opsional -- ini yang menemukan kunci liar di dalam array bersarang, sesuatu yang tidak akan ketahuan cuma dari membaca SQL-nya; (2) `LEFT JOIN LATERAL` ke fungsi set-returning (`jsonb_array_elements`, `unnest`, dst.) SEBELUM `GROUP BY` yang punya kolom `sum()`/`count()` lain HARUS diwaspadai fan-out -- pakai subquery berkorelasi kalau cuma butuh satu nilai per baris sumber.

---

## Analisis 5 format asli (`pusat`, `accounting`, `it`, `manager_resto`, `ita`) — 23 Agustus 2026

Sebelum menulis kode Task 14/16/17, kelima `docs/FORMAT-ASLI-*.md` dibaca penuh dan dilaporkan ke user: (1) angka yang sudah diketik orang lain (kandidat rollup), (2) field yang butuh view lintas-divisi baru + siapa pembacanya, (3) pertentangan dengan dokumen yang ada. User menjawab dengan 1 konfirmasi bug + 11 rollup diterima + 4 keputusan (B/C/D/D-lanjutan/E). Dicatat di sini supaya tidak hilang saat Task 16/17 dilanjutkan (user menjeda setelah Task 14 utk melihat satu form dulu).

**Temuan A (bug `v_keuangan_rekap`) — DIPERBAIKI user langsung di dokumen.** View itu sudah dispek sejak sebelum §3.4b ada dengan `security_invoker=ON`, padahal Sabrina (`pusat`) dikecualikan dari `f='accounting'` di `can_see_report()` -- akan dapat 0 baris selamanya. `04-CATATAN-TEKNIS.md` §3.4 dan `03-CALC-SPEC.md` §4.3 sudah diperbaiki user: `security_invoker=off` + penjaga `has_role('ceo' or 'pusat' or 'accounting')` langsung di `where`. Tetap 4 kolom (`total_masuk`, `total_keluar`, `net`, `warna`) -- **BUKAN** pola `boleh_lihat_rekap()` (itu utk kasus yang PEMBACAnya tidak punya assignment ke form manapun yang relevan; Sabrina/CEO/Accounting sendiri sudah jelas siapa mereka lewat `has_role`).

**Sebelas rollup lain diterima user, dicatat sebagai referensi saat Task 21 (`pusat`) dikerjakan** (bukan sekarang): §2 CS ← `cs`, §3 operasional kantor ← `ga`, §4 security ← `security` (SUM lintas lokasi+shift), §5 HRD ← `hrd`, §6 perizinan ← `perizinan` (hitung dari tabel `perizinan_berjalan`), §7 pembangunan ← `pembangunan`+`v_pembangunan_per_lokasi` (SUM lintas lokasi), §9 STK ← `pic_lokasi` blok 6 (SUM), §10 DTI ← `dti`, §12 kendaraan ← `kendaraan`, §14 IT/medsos ← `it`, §15 status PIC + §17/§18 keputusan ← pola `v_papan_hari_ini` (§4.1) + tabel `decision`. **Semuanya CUKUP view biasa (`security_invoker=on`)** -- `pusat` sudah punya akses baris lewat `can_see_report()`, BUKAN kasus opsi (d)/`boleh_lihat_rekap()`. Instruksi eksplisit: *"Jangan pakai security definer di mana pun yang tidak perlu."*

**Keputusan B — Sabrina §11 (keuangan) ikut format Accounting, bukan format Sabrina.** Dua dokumen klien bertentangan: format Sabrina minta 7 angka (cash masuk, bank masuk, pengeluaran operasional, petty cash, dst.), format Accounting bilang eksplisit Sabrina cuma dapat rekap umum (total masuk/keluar/posisi/status). **Format Accounting menang** -- itu pernyataan eksplisit soal batas kerahasiaan, dan pemisahan cash vs bank + petty cash justru membocorkan struktur keuangan yang seharusnya tertutup. 🔴 **BELUM DIKONFIRMASI ke CEO -- wajib ditanyakan saat Task 21 dikerjakan**, bukan diasumsikan selamanya. `v_keuangan_rekap` (4 kolom) sudah benar untuk skenario ini, jangan ditambah kolom demi mengejar 7 angka format Sabrina.

**Keputusan C — Ita TIDAK punya PTE sendiri, ikut `personal_marketing` seperti 36 karyawan lain.** `ita` §10 (draf awal) berisi field PTE LENGKAP (live+platform+durasi, undangan, kesaksian, google review, VT, video mentahan, closing) -- beda sendiri dari SEMUA form Task 14/15 lain yang cuma menampilkan rollup baca-saja 3 baris. **Alasan bukan cuma konsistensi**: `pte_daily` punya `unique(user_id, tanggal)` -- kalau `ita` form JUGA menulis ke situ lewat jalur terpisah dari `personal_marketing`, dua submission akan **saling menimpa diam-diam** (yang terakhir kirim menang, tanpa error) begitu Ita mengisi kedua form di hari yang sama. Saat Task 16 (`ita`) dikerjakan: blok 10 jadi rollup generik yang sama seperti form lain (`perluRollupMarketing` di `LaporForm.tsx`, sudah ada, tidak perlu kode baru). **Dua field DIPINDAHKAN ke blok Thrifting/F&B** (bukan dibuang) karena itu data usaha, bukan PTE personal: "Google Review Indokopi" (spesifik outlet, beda dari `review_jumlah` generik `personal_marketing`) dan durasi live.

**Keputusan D — rekonsiliasi 3 lapis (Manager→Ita→Accounting) DIPERTAHANKAN, itu fitur bukan bug.** Tiga orang menghitung uang dari tiga SUMBER berbeda (Manager dari catatannya sendiri, Ita dari sistem POS, Accounting dari mutasi bank) -- itu kontrol keuangan dasar (mirip D1, silang-cek), jangan disederhanakan jadi satu sumber. **TAPI**: Accounting §13 ("REKONSILIASI RESTO") tidak boleh mengetik ULANG angka Manager Resto dan Ita -- keduanya harus tampil TERISI OTOMATIS (rollup baca-saja) di sebelah angka bank yang Accounting ketik sendiri, dengan selisih dihitung sistem. Tiga pengukuran independen, satu layar -- bukan tiga form yang saling tidak tahu satu sama lain. Berlaku saat Task 17 (`accounting`) dikerjakan; butuh Task 16 (`manager_resto`/`ita`) selesai dulu supaya ada sumbernya.

**Keputusan D-lanjutan — Ita §9 (RAB) vs Manager Resto §4 (stok habis): itu DUPLIKASI, diperbaiki.** Manager Resto sumbernya (dia yang lihat rak kosong) -- Ita §9 jadi rollup baca-saja dari stok-habis Manager, lalu Ita MENAMBAH yang cuma dia tahu: RAB (nominal) dan status pengajuan ke Pak Eri/Bu Rika. Pola identik D3 (material_kurang pic_lokasi→pembangunan).

**Keputusan E — kontrak kunci `omzet_indosteak`/`omzet_indokopi` di `ita` HARUS persis nama itu.** `v_selisih_resto` (§4.4, sudah dispek) meng-hardcode pola `'omzet_' || lower(o.nama)`. Wajib diikuti persis saat menulis `forms/f16-ita.ts` -- kalau beda satu huruf, `v_selisih_resto` diam-diam tidak pernah match.

**View baru disetujui untuk Task 17**: Accounting §8 ("Kebutuhan Pembangunan") dan §10 ("Kontraktor/Supplier/DTI") butuh angka dari `pembangunan` (`material_borongan`, `infrastruktur_rencana_kerja` -- keduanya sudah punya kolom `estimasi_biaya`/`anggaran`) dan `dti` (`belanja_rab`) -- role `accounting` **tidak** punya `can_see_report()` ke `pembangunan`/`dti` sama sekali (cuma `accounting`/`manager_resto`/`ita`). **Disetujui: buat view security-definer baru, pola §3.4b, penjaga `boleh_lihat_rekap('accounting')`.** Daftar putih per field (bukan seluruh row), uji dengan field umpan "RAHASIA" seperti `v_pembangunan_per_lokasi` -- dikerjakan saat Task 17.

---

## Task 16 (`manager_resto`, `ita`) + Task 17 (`accounting`) -- 23 Agustus 2026

Ketiganya dipratinjau non-teknis dulu (`docs/PRATINJAU-FORM-*.md`), direview & disetujui user dengan 5 keputusan sebelum kode ditulis (lihat riwayat percakapan) -- ringkasan implementasinya:

**Klarifikasi §3.4b sebelum mulai**: user memperjelas "nama orang" yang dilarang di §3.4b syarat #1 khusus **nama konsumen/pihak luar**, BUKAN nama karyawan -- siapa PIC lokasi/outlet mana sudah terbuka lewat `assignment`+`profile` (`0002_rls.sql`: *"semua yang login boleh melihat nama rekan"*) sejak awal proyek, jadi memuatnya di view agregat tidak membocorkan apa pun. Desain `lib/api/it.ts` (Task 14) sudah sesuai tanpa perubahan.

**Dua kemampuan generik baru di `forms/types.ts` (dipakai lintas form, bukan solusi sekali pakai):**
- `Field.wajibJika: { field, nilai }` -- field wajib diisi KALAU field lain (di blok mana pun di form yang sama) bernilai persis `nilai`. Dipakai pola "kalau ada selisih, penyebab wajib" di `manager_resto` dan `ita` (instruksi Task 16 papan tugas). Diimplementasikan di `forms/validasi.ts` `superRefine`.
- `Field.sumberKeputusan: true` (khusus `type:'tabel'`) -- tiap BARIS tabel jadi satu baris `decision` TERPISAH saat kirim (urgensi = urutan baris, maks. 3), bukan satu keputusan per laporan seperti `blokKeputusanCeo`. Dipakai `accounting` §16 "Prioritas Pembayaran" (keputusan 5 user: *"CEO bisa menyetujui prioritas 1, menunda 2, menolak 3 -- satu baris decision tidak bisa menampung tiga hasil"*). `lib/api/decision.ts` `buatKeputusanDariLaporan()` diperluas terima `{nominal, deadline, dampak, urgensi}` opsional, dipanggil dari loop generik di `LaporForm.tsx` (bukan mekanisme baru terpisah, sesuai instruksi *"pakai mekanisme generik yang sudah kamu bangun di Task 13/15"*).
- `Block.hanyaHari: number[]` -- blok cuma tampil & tervalidasi hari tertentu (ISO 1=Senin). Dipakai blok "Stock Opname Mingguan" `ita` (papan tugas Task 16: *"Blok stock opname muncul Senin, hilang di hari lain"*). `blokBerlakuHariIni(schema, tanggal?)` (baru, `forms/validasi.ts`) dipakai KEDUANYA `FormRenderer` (tampilan) dan `buildZodSchema` (validasi) supaya tidak pernah tidak-sinkron.
- **`LaporForm.tsx` digeneralisasi utk scope `'outlet'`** (sebelumnya cuma `'lokasi'`) -- `manager_resto` butuh pemilih outlet kalau seseorang mengurus >1 outlet, pola identik `kombinasiDitugaskan` yang sudah ada, cuma ditambah dimensi `outletId`. `lib/api/outlet.ts` (`useDaftarOutlet`) baru, meniru `lib/api/lokasi.ts`.

**`manager_resto` (`forms/f16-manager-resto.ts`)**, 13 blok:
- Keputusan 1: "Dapur" (dobel di blok 6 & 8 dokumen asli) dipertahankan CUMA di blok 6 (`dapur_bersih_sebelum_buka`), dihapus dari blok 8.
- Keputusan 2: blok 12 "Kebutuhan untuk Besok" TIDAK ADA di `blocks` -- rollup DALAM SATU FORM dari blok 4+5 yang sedang diisi sendiri (§3.5b berlaku juga di dalam satu form), fungsi murni `ringkasanKebutuhanBesok()` (`lib/api/manager-resto.ts`, pola sama `ringkasanPteHariIni`), dirender `KebutuhanBesokRestoOtomatis`.
- Blok 4 "Stok Habis/Kebutuhan Kiriman Pusat" diubah dari teks bebas (dokumen asli) jadi TABEL terstruktur (`stok_habis`, `stok_akan_habis`) -- wajib, karena field ini JUGA sumber rollup Ita (lihat di bawah), dan §3.4b melarang teks bebas lewat view security-definer.
- Blok 9 "Video Kontrol Wajib": 10 field terpisah persis nama dokumen asli, semua `wajib:true` + `buktiWajib:true` (papan tugas Task 16: *"daftar 10 video kontrol wajib"*).
- Blok 3: `ada_selisih_stok` (ya_tidak) + `selisih_stok` (tabel) `wajibJika`.

**`ita` (`forms/f16-ita.ts`)**, 10 blok + 1 blok Senin-saja:
- Keputusan C: blok PTE (§10 dokumen asli) TIDAK ADA di `blocks` -- rollup generik 3-baris yang sama dipakai semua form (`perluRollupMarketing`). Alasan teknis: `pte_daily` punya `unique(user_id, tanggal)`, dua form yang menulis PTE hari sama akan saling menimpa diam-diam. "Google Review Indokopi" pindah ke blok 6, "durasi live" pindah ke blok 1.
- Keputusan D-lanjutan: blok 9 "Kebutuhan Stok/RAB" -- daftar stok habis TIDAK diketik ulang, rollup baca-saja dari `manager_resto` (lewat view baru, lihat di bawah), Ita cuma menambah RAB + status pengajuan.
- Keputusan 3: blok 8 "Kontrol Stok Restoran" DIPERTAHANKAN sebagai isian Ita sendiri (silang-cek independen, bukan rollup) -- angka Manager ditampilkan otomatis di sebelahnya (`StokManagerUntukItaOtomatis`) sebagai pembanding, bukan menggantikan isian Ita.
- Kunci `omzet_indosteak`/`omzet_indokopi` (blok 5/6) persis kontrak `v_selisih_resto` (§4.4).
- `sesuai_*`/`status_stok_thrifting` (ya_tidak) + `selisih_*`/`penyebab_*` `wajibJika` di blok 1, 3, 5, 6.
- Blok "Stock Opname Mingguan" (`hanyaHari: [1]`).

**`accounting` (`forms/f17-accounting.ts`)**, 17 blok, `rahasia: true`:
- Keputusan 4: blok 10 "Kontraktor/Supplier/DTI" -- Precast/DTI, Material, Infrastruktur/jalan OTOMATIS (pengajuan divisi lain, dari view baru); Kontraktor jatuh tempo & Listrik/utilitas proyek DIKETIK (tagihan yang cuma diketahui Accounting). Blok 8 "Kebutuhan Pembangunan" JUGA sepenuhnya otomatis dari view yang sama, TIDAK ADA di `blocks`.
- Keputusan 5: blok 16 "Prioritas Pembayaran" pakai `sumberKeputusan: true` (lihat di atas) -- form ini TIDAK memakai `blokKeputusanCeo()` sama sekali, karena blok 16 sudah menutupi kebutuhan keputusan CEO (status hari ini ada di blok 6, catatan umum di blok 18).
- Blok 13 "Rekonsiliasi Resto" -- omzet versi Manager & versi Ita ditampilkan otomatis (`OmzetRestoOtomatis`) berdampingan dengan angka bank yang diketik Accounting, selisih dihitung sistem ("tiga pengukuran, satu layar"). **TIDAK lewat view security-definer** -- role `accounting` SUDAH punya `can_see_report()` langsung ke `manager_resto`/`ita` (`0002_rls.sql`, sejak awal), jadi `lib/api/accounting.ts` `useOmzetRestoHariIni()` query biasa, sesuai instruksi *"jangan pakai security definer di mana pun yang tidak perlu"*.
- Blok 3 "Detail Penerimaan Konsumen" berisi nama konsumen sungguhan -- TIDAK PERNAH jadi sumber view lintas-divisi mana pun (§3.4b larangan mutlak).

**Dua migrasi view security-definer baru (pola §3.4b, sama-sama diuji field umpan "RAHASIA"):**
- `0013_manager_resto_untuk_ita.sql` -- `v_manager_resto_untuk_ita`, penjaga `boleh_lihat_rekap('ita')`. Kolom `stok_habis`/`stok_akan_habis` direkonstruksi whitelist (pola sama `material_kurang`), `jumlah_item_selisih` dihitung dari `jsonb_array_length`. Dibungkus `max(...::text)::jsonb` (BUKAN `left join lateral`) supaya tidak kena bug fan-out yang sama seperti `v_pembangunan_per_lokasi` versi pertama.
- `0014_kebutuhan_pembangunan_accounting.sql` -- `v_kebutuhan_pembangunan_accounting`, penjaga `boleh_lihat_rekap('accounting')`. Pakai CTE (`WITH ... AS`) + subquery skalar per kolom (bukan `GROUP BY`) karena `pembangunan`/`dti` masing-masing cuma 1 baris/hari (scope `global`) -- tidak butuh agregasi lintas-baris sama sekali, jadi pola `v_pembangunan_per_lokasi` (yang memang butuh `GROUP BY` per lokasi) tidak relevan di sini.

**Diverifikasi:**
- SSR ketiga schema render tanpa crash, semua judul blok ada (kecuali blok `hanyaHari`, diuji terpisah), tidak ada key dobel, `buildZodSchema` tidak error.
- `wajibJika` diuji langsung: `manager_resto` (`ada_selisih_stok='ya'` tanpa isi `selisih_stok` → ditolak; terisi → lolos; `='tidak'` → tidak dituntut) dan `ita` (`sesuai_indosteak='tidak'` tanpa `selisih`+`penyebab` → keduanya ditolak; `='ya'` → tidak dituntut).
- `hanyaHari` diuji dengan tanggal eksplisit (Senin 2026-08-24 vs Selasa 2026-08-25, bukan bergantung tanggal sistem saat uji dijalankan): blok Stock Opname ada di Senin, hilang di Selasa.
- `scripts/uji-batch16-17.mjs` -- DB sungguhan, penyamaran RLS, BEGIN/ROLLBACK, 9 uji:
  1. `manager_resto` scope outlet: 2 laporan (Indosteak+Indokopi) hari sama, tidak bentrok `report_uniq`.
  2. Sebagai profil persis Ita: `v_manager_resto_untuk_ita` kembalikan 2 baris terstruktur (`jumlah_item_selisih=1` utk Indosteak yang punya 1 baris selisih, `stok_habis` ikut terbawa), field umpan `"RAHASIA"` di `dugaan_penyebab`/`masalah_karyawan` TIDAK bocor.
  3. Ita akses LANGSUNG ke `report` (`form_key='manager_resto'`) → 0 baris (lewat view, bukan baris).
  4. Sebagai profil persis Accounting: `v_kebutuhan_pembangunan_accounting` kembalikan rollup benar (`total_material=20000000`, `total_infrastruktur=15000000`, `precast_dti=8000000`), field umpan `"RAHASIA"` di `kontraktor_bermasalah_masalah` TIDAK bocor.
  5. Accounting akses LANGSUNG ke `report` (`form_key='pembangunan'`) → 0 baris.
  6. Accounting akses LANGSUNG ke `report` (`form_key='manager_resto'`) → 2 baris (BENAR bisa, row-level `can_see_report`, bukan lewat view).
  7. Laporan Accounting dengan 4 baris `prioritas_pembayaran` (baris ke-4 sengaja) → cuma 3 baris `decision` terbuat, urgensi persis 1/2/3, semua status `menunggu`, deadline tidak valid (`"bukan-tanggal-valid"`) jadi `null` bukan tanggal salah, nominal cocok.
  8. CEO (Putri) tetap bisa lihat kedua view lewat `has_role`, bukan cuma `assignment`.
  9. Toyib (karyawan polos, tanpa assignment relevan apa pun) → 0 baris dari kedua view.
- Semua di-`ROLLBACK`, dikonfirmasi ulang `count(*)=0` utk `report`(4 form_key)/`decision`/profil sementara.
- `tsc`/`build`/`lint` bersih, `grep` angka bisnis nol hasil di seluruh file baru/ubah.

**Keterbatasan yang dicatat, bukan diselesaikan diam-diam:**
- Tanggal di tabel `sumberKeputusan` (`deadline` kolom bebas teks) CUMA diterima kalau persis format `YYYY-MM-DD` -- format lain (`"besok"`, `"01/09"`) sengaja DIABAIKAN (jadi `null`) daripada ditebak lewat `new Date()`/`toISOString()`, yang bisa meleset zona waktu (CLAUDE.md aturan #2). Accounting perlu diberi tahu mengetik deadline persis format itu kalau mau baris itu masuk antrean CEO dengan tanggal.
- `Field.wajib` utk tipe `'centang'` TIDAK ditegakkan di `skemaPerField` (selalu `.optional()`, ditemukan saat debug uji `wajibJika`) -- artinya 10 video wajib `manager_resto` ditegakkan MURNI lewat `buktiWajib` (tuntutan lampiran), bukan lewat nilai centang-nya sendiri. Ini konsisten dengan cara `buktiWajib` bekerja di form lain (isi field + bukti, bukan cuma isi field), jadi bukan bug baru -- dicatat supaya tidak ada yang mengira `wajib:true` pada `centang` benar-benar menegakkan sesuatu di luar `buktiWajib`.
- Seluruh alur UI sungguhan (pemilih outlet, wajibJika di layar/pesan error, blok Senin-saja muncul-hilang, 3 keputusan CEO dari satu form) belum diklik di browser -- logikanya sudah teruji lewat DB/SSR langsung.

---

## Task 18 (Papan Kontrol) + Task 19 (Antrean Keputusan CEO) -- 23 Agustus 2026

Awal Batch E, dikerjakan berurutan tanpa berhenti di antara task per instruksi user.

### Task 18 -- Papan Kontrol

- View `v_papan_hari_ini` (migrasi `0015`) dibuat persis `03-CALC-SPEC.md` §4.1 -- `security_invoker=on` (bukan §3.4b: pembacanya, `pusat`/`ceo`, memang sudah berhak atas baris `report` non-accounting lewat `can_see_report()`, dan `assignment`/`profile` sudah *broadly readable*). SATU tambahan terhadap versi dokumen: kolom `nudged_at` lewat LEFT JOIN KEDUA ke `report` (tanpa syarat `status <> 'draft'`) -- kalau dipaksa satu JOIN saja, baris yang baru "ditagih" (masih berstatus draft, lihat di bawah) tidak akan pernah lolos syarat join utama sehingga `nudged_at`-nya tidak pernah kelihatan.
- `tagih_laporan(assignment uuid)` -- disiapkan sebagai stub KOSONG sejak Task 04 (0002_rls.sql, "implementasi menyusul di Task 18"), diselesaikan sekarang. Masalahnya: kartu "belum lapor" secara definisi belum punya baris `report` sama sekali, padahal `nudged_at` adalah kolom di `report`. Diselesaikan dengan pola yang SAMA dengan `useSimpanDraft` (Task 10): INSERT baris draft sekali (`on conflict` pada ekspresi `report_uniq`) kalau belum ada, lalu SET `nudged_at=now()` -- kalau PIC baru mengisi laporannya BELAKANGAN, dia mengisi baris draft yang SAMA (via `useReportHariIni` yang menemukannya), bukan baris kedua yang bentrok unique index. Tetap `security definer` + guard `has_role('pusat') or has_role('ceo')`, dipanggil lewat RPC (`lib/api/papan.ts`, `useTagihLaporan`) -- BUKAN `update` langsung dari klien, sesuai peringatan eksplisit di 04-CATATAN-TEKNIS.md §3.2 soal `report_nudge` yang sengaja dihapus.
- **Bug ditemukan di luar cakupan literal Task 18, tapi wajib diperbaiki supaya warna kartu berarti sesuatu**: `components/LaporForm.tsx` sejak Task 10 (dan tidak pernah diperbaiki di Task 12-17) SELALU mengirim `warna:'hijau'` hardcode ke `useKirimReport`, mengabaikan field `type:'status_warna'` yang dipilih pengisi maupun aturan otomatis §5. Kalau dibiarkan, SETIAP kartu di Papan Kontrol akan selalu hijau, tidak peduli laporannya benar-benar bermasalah atau tidak -- fitur intinya (warna sebagai sinyal) tidak akan pernah berfungsi. Diperbaiki dengan `lib/warna.ts` (pure functions, diuji lewat pemakaian generik di `tanganiKirim`, bukan per-form): `warnaAkhir = warnaTerburuk(warnaDipilihDari(schema, isiKirim), warnaOtomatis(urgensiTerburukDariKirim(schema, isiKirim), apakahTerlambat(...)))`. Diimplementasikan §5 SEBAGIAN -- decision urgensi 1 → merah, terlambat/urgensi 2-3 → kuning -- TAPI SENGAJA TIDAK mengimplementasikan pemicu merah "ada selisih tanpa penyebab": semua field selisih di app ini (manager_resto, ita) sudah dipaksa `wajibJika` mengharuskan field penyebab terisi SEBELUM submit bisa lolos validasi zod sama sekali, jadi kondisi itu sudah tidak bisa terjadi di lapisan data -- bukan diabaikan diam-diam, dicatat di komentar `lib/warna.ts` sendiri.
- Accounting blok 6 "Cashflow Hari Ini" (`f17-accounting.ts`) punya `catatan` yang berjanji "dihitung otomatis dari blok 1, 2, dan 4" sejak ditulis di Task 17 -- TAPI tidak pernah benar-benar dibangun sampai sekarang. Diselesaikan sebagai bagian dari perbaikan warna di atas (perlu jalur yang sama): `hitungCashflowHariIni()` (`lib/api/accounting.ts`) menjumlahkan blok 2 (Uang Masuk, TIDAK termasuk field `metode_*` yang cuma rincian CARA masuknya uang yang sama, supaya tidak dobel hitung) dan blok 4 (Uang Keluar), ditampilkan lewat panel baca-saja `CashflowOtomatis`, dan DISUNTIKKAN ke `report.data` (`total_masuk`/`total_keluar`/`net_cashflow`) saat kirim -- ini yang akan dibaca `v_keuangan_rekap` di Task 20. Blok 1 (Total Dana Tersedia), 15 (Kebutuhan Dana CEO), dan 18 (Executive Summary) di `f17-accounting.ts` punya janji "otomatis" serupa di `catatan`-nya masing-masing TAPI TIDAK dikerjakan sekarang -- di luar apa yang dibutuhkan `v_keuangan_rekap`/Bagian 11 Laporan Terpusat (yang cuma perlu total_masuk/total_keluar/net/warna), dicatat sebagai utang terpisah, bukan diklaim selesai.
- `lib/teksAngka.ts` (baru) -- `angkaDariTeks`/`tanggalDariTeks` dipindah keluar dari `LaporForm.tsx` (sebelumnya fungsi lokal tak-terekspor) supaya bisa dipakai ulang oleh `hitungCashflowHariIni` juga, bukan disalin.
- DB sungguhan: `scripts/uji-papan-kontrol.mjs`, 9 titik, semua lolos (lihat baris tabel Task 18).

### Task 19 -- Antrean Keputusan CEO

- `app/keputusan/page.tsx` diganti dari `peran="ceo"` (Task 06) jadi `['ceo','pusat']`, sesuai kalimat Task 19 sendiri ("pusat boleh melihat dan menyusun urutan prioritas") -- debt ini sudah dicatat sejak baris Task 06 supaya tidak terlupa. "Menyusun urutan prioritas" diartikan sebagai kontrol urut LOKAL di UI (urgensi/deadline/nominal) tanpa tulis-menulis ke database sama sekali -- ditegaskan oleh kalimat "Selesai kalau" Task 19 sendiri: "update dari pusat ditolak database", bukan "pusat bisa reorder tersimpan".
- **Bug keamanan ditemukan SAAT membangun halaman ini, sebelum sempat ditampilkan ke siapa pun** -- policy `dec_select` (ditulis Task 04, sebelum `accounting` pernah membuat baris `decision`) mengizinkan `pusat` melihat SEMUA baris `decision` tanpa kecuali. Sejak Task 17 (blok 16 "Prioritas Pembayaran" `accounting`, mekanisme `sumberKeputusan`), laporan `accounting` MEMANG membuat baris `decision` -- tanpa perbaikan, halaman Antrean Keputusan yang baru dibangun ini akan menampilkan judul/nominal/dampak keputusan Accounting ke Pusat, melanggar CLAUDE.md aturan #3 ("Laporan accounting hanya boleh terbaca role ceo") dan komentar 0002_rls.sql sendiri ("pusat and f <> 'accounting' adalah inti kerahasiaan yang diminta klien. Jangan diubah"). Ditemukan lewat MEMBACA POLICY LAMA sebelum memakainya di halaman baru, bukan ditemukan lewat pengujian setelah dibangun.
- Perbaikan PERTAMA (migrasi `0016`): `dec_select` diubah jadi `has_role('pusat') and not exists(select ... from report where form_key='accounting')`. **Ternyata migrasi ini SENDIRI masih salah** -- ditemukan lewat `scripts/uji-keputusan-ceo.mjs` (uji DB sungguhan, BEGIN/ROLLBACK), bukan cuma dibaca kodenya: subquery `exists(...)` di dalam definisi POLICY LAIN tunduk pada RLS `report_select` milik PEMANGGIL yang sama. Untuk Pusat, baris `report` dengan `form_key='accounting'` itu SENDIRI sudah tidak terlihat lewat `report_select` -- jadi `exists(...)` selalu mengembalikan 0 baris, BUKAN karena form_key-nya beda, tapi karena baris itu memang sama sekali tak terlihat pemanggil. Akibatnya `not exists(...)` SELALU true dan pengecualian yang baru ditulis tidak pernah menyala -- kebocoran yang sama persis dengan sebelum `0016`, hanya tersamar. Diperbaiki dengan migrasi `0017`: fungsi baru `laporan_form_key(r_id uuid)` (`security definer`, pola sama dengan `has_role()`/`can_see_report()`) membaca `form_key` TANPA tunduk pada RLS pemanggil, dipakai menggantikan subquery mentah di `dec_select`.
- **Pelajaran umum dicatat di `04-CATATAN-TEKNIS.md` §3.2**: jangan pernah menaruh subquery yang RLS-sensitif langsung di definisi policy lain -- Postgres tidak mengistimewakan posisinya, subquery itu tetap berjalan di bawah RLS pemanggil. Kalau sebuah policy butuh tahu fakta dari tabel lain yang mungkin TIDAK terlihat pemanggil, jawabannya harus lewat fungsi `security definer`, seperti pola `has_role()`/`can_see_report()`/`boleh_lihat_rekap()` yang sudah ada.
- `lib/api/decision.ts` diperluas: `useAntreanKeputusan`, `useRiwayatKeputusan`, `useMemutuskan` -- ketiganya meng-embed `report:report_id(form_key, tanggal, author:author_id(nama))` lewat PostgREST; kalau RLS menolak baris `report` sumbernya, embed itu jatuh jadi `null` (bukan error), yang sekarang sudah benar berkat `0017`.
- DB sungguhan: `scripts/uji-keputusan-ceo.mjs`, 12 titik (termasuk kedua bug di atas), semua lolos.

**Keterbatasan yang dicatat, bukan diselesaikan diam-diam:**
- Seluruh alur UI sungguhan (klik Tagih, klik Setujui/Cicil/Tunda/Tolak, lihat kartu berubah warna di layar, tab Menunggu/Riwayat) belum diklik di browser -- logikanya sudah teruji lewat DB langsung.
- Panel "otomatis" blok 1/15/18 `accounting` (lihat Task 18 di atas) tetap belum dibangun -- di luar kebutuhan Task 20/21 yang konkret, bukan diklaim selesai.

### Kenapa ini dicatat sebagai pelajaran metodologi, bukan cuma dua entri bug biasa

User menandai kedua temuan di atas sebagai contoh konkret kenapa **uji DB sungguhan tidak bisa digantikan membaca kode**. Alasannya nyata di kedua kasus:

1. **Warna laporan selalu `'hijau'`** -- kalau cuma dibaca, `useKirimReport({ warna: 'hijau', ... })` di `tanganiKirim()` (Task 10) terlihat SAH secara TypeScript, lolos `tsc`, lolos `lint`, lolos `build`. Tidak ada satu pun pemeriksaan statis yang bisa menandai "nilai ini seharusnya dinamis, bukan konstanta" -- itu murni soal MAKSUD, bukan sintaks. Baru ketahuan janggal saat menulis `scripts/uji-papan-kontrol.mjs` dan membandingkan warna kartu yang DIHARAPKAN dengan yang SUNGGUH tersimpan di baris `report` setelah kirim sungguhan.
2. **Kebocoran `dec_select`** -- ini yang lebih tajam: PERBAIKAN PERTAMANYA SENDIRI (migrasi `0016`) juga terlihat benar kalau cuma dibaca. `not exists (select ... from report where form_key='accounting')` terlihat seperti pengecualian yang sah secara SQL. Yang tidak kelihatan dari membaca teks policy-nya adalah bahwa subquery itu **berjalan di bawah RLS pemanggil yang sama** -- fakta yang cuma muncul kalau benar-benar dijalankan SEBAGAI Pusat dan hasilnya dibandingkan dengan kenyataan (baris `report` accounting yang seharusnya dikecualikan, tapi tidak). `scripts/uji-keputusan-ceo.mjs` menangkapnya di percobaan PERTAMA dijalankan setelah `0016` -- bukan dugaan, output mentahnya `SALAH: decision accounting TIDAK muncul untuk Pusat` (artinya justru MUNCUL, kebalikan dari yang diharapkan).

Pola yang sama di keduanya: kesalahan yang **valid secara sintaks dan valid secara "terlihat masuk akal saat dibaca"**, cuma kelihatan salah kalau dijalankan sebagai orang yang tepat dan hasilnya dibandingkan dengan kenyataan. Ini alasan kenapa setiap task di proyek ini (bukan cuma yang menyentuh RLS) tetap memakai `scripts/uji-*.mjs` dengan penyamaran role sungguhan lewat `set_config('request.jwt.claims', ...)`, bukan berhenti di `tsc`/`lint`/`build` bersih.

---

## Reset password 7 akun uji + persiapan deploy (24 Agustus 2026)

**Cara reset password diganti atas instruksi user.** Versi pertama (`update auth.users set encrypted_password = crypt(...)`) ditolak -- itu menulis langsung ke kolom internal GoTrue, bukan jalur yang didukung Supabase secara resmi walau `pgcrypto`/`crypt()`/`gen_salt('bf')` kebetulan menghasilkan format yang sama. **Diganti** `scripts/set-password.mjs` (baru, `npm run set-password`; dinamai ulang dari `reset-password.mjs` 24 Agustus 2026 atas permintaan user -- isinya sama, satu skrip bukan dua) -- memakai `supabase.auth.admin.updateUserById(id, { password })` (Auth Admin API resmi, `service_role`, pola yang sama dengan `app/api/admin/user/route.ts` Task 23), password 16 karakter dari `crypto.randomBytes` (bukan `Math.random`) BERBEDA per akun, dicetak SEKALI ke terminal, TIDAK ditulis ke file/log/commit mana pun. `id` per email dicari lewat `SUPABASE_DB_URL` (cuma **membaca** `auth.users`, tidak pernah menulis ke situ langsung).

Dijalankan sungguhan 24 Agustus 2026 -- ketujuh akun uji (`docs/DATA-KARYAWAN.md` §3) berhasil direset, password lama `123456` sudah tidak berlaku untuk semuanya. Password baru ada di scrollback percakapan sesi ini (bukan file) -- **pindahkan ke pengelola password sebelum riwayat sesi ini hilang**, dan jalankan ulang skrip ini kapan pun perlu rotasi lagi (aman diulang, password lama otomatis diganti).

---

## Temuan pertama dari pemakaian produksi sungguhan (24 Agustus 2026)

Deploy pertama ke Vercel berhasil, lalu user langsung mengirim satu laporan `personal_marketing` sungguhan dari browser dan menemukan tiga hal -- ketiganya kelalaian spesifikasi, bukan bug implementasi terhadap spesifikasi yang ada (task board tidak pernah menyebut halaman riwayat, dan aturan "terlambat" memang tidak pernah menghitung hari libur).

### Masalah 1 -- Tidak ada halaman riwayat

Laporan yang sudah terkirim tidak bisa dilihat lagi di mana pun setelah dikirim -- seluruh layar cuma menampilkan "hari ini". **Dibuat**: `app/riwayat/page.tsx` ("Laporan Saya", tab baru di header, terlihat semua orang login) -- daftar laporan TERKIRIM (bukan draft) milik SENDIRI, 30 hari terakhir, terbaru di atas (`lib/api/riwayat.ts`, `useRiwayatSaya()`). `app/riwayat/[id]/page.tsx` -- detail satu laporan mode BACA SAJA lewat komponen baru `components/LaporanBacaSaja.tsx` (BUKAN `FormRenderer`, yang selalu mode edit react-hook-form) -- tiap field ditampilkan sesuai tipe (uang diformat, tabel jadi kartu per baris, dst.), lampiran ditampilkan lewat tombol "Lihat foto/video" yang minta signed URL sekali tap (`useSignedUrl`, sudah ada sejak Task 11), bukan URL mentah. **Sengaja dipersempit ke `author_id = diri sendiri` secara eksplisit di query** (bukan cuma mengandalkan RLS `can_see_report()`, yang justru mengizinkan Pusat/CEO membaca laporan orang lain) -- "Laporan Saya" harus selalu personal bagi siapa pun yang membukanya, termasuk CEO. Diuji `scripts/uji-riwayat.mjs` (5 titik): laporan terkirim 30 hari muncul, draft tidak, >30 hari tidak, dan -- titik paling penting -- Pusat TIDAK bisa "intip" laporan Toyib lewat halaman ini walau RLS aslinya mengizinkan itu di jalur lain.

Pemilih tanggal juga ditambahkan ke Papan Kontrol dan Laporan Terpusat (`components/PemilihTanggal.tsx`, default hari ini, bisa mundur lewat tombol ←/→ atau `<input type="date">` native). Ini butuh perubahan lebih besar dari sekadar UI: TUJUH view yang sebelumnya mengunci tanggalnya sendiri ke `(now() at time zone 'Asia/Jakarta')::date` di dalam definisi SQL (`v_papan_hari_ini`, `v_security_hari_ini`, `v_stk_hari_ini`, `v_marketing_hari_ini`, `v_pembangunan_hari_ini`, `v_pembangunan_per_lokasi`, `v_selisih_resto`) diubah jadi FUNGSI yang menerima parameter tanggal (migrasi `0020`, default tetap hari ini kalau parameter dikosongkan -- pemanggil lama seperti rollup form `pembangunan` sendiri dan dashboard CEO di Beranda tidak perlu ikut berubah). Fungsi biasa (`language sql`, BUKAN `security definer`) berjalan dengan hak pemanggil untuk RLS, perilakunya identik `security_invoker = on` di view lama. `v_keuangan_rekap` TIDAK diubah -- itu satu-satunya yang sejak awal sudah mengekspos kolom `tanggal` sebagai filter biasa. Diuji `scripts/uji-tanggal-mundur.mjs` (10 titik): tiap fungsi dites tanpa parameter (harus sama seperti dulu) DAN dengan tanggal kemarin eksplisit (harus menemukan data yang sengaja ditanam kemarin, dan TIDAK tertukar dengan "hari ini" yang kosong).

### Masalah 2 -- "TERLAMBAT" di hari yang bukan hari kerja

`apakahTerlambat()` (`lib/api/report.ts`) cuma membandingkan jam kirim dengan `policy.deadline_by_form`, tidak pernah memeriksa `policy.workdays` sama sekali -- laporan yang dikirim di hari libur tetap ditandai TERLAMBAT, padahal tidak ada kewajiban di hari itu. **Diperbaiki**: fungsi ini sekarang mengembalikan `false` (tidak pernah terlambat) kalau hari ini bukan hari kerja, SEBELUM membandingkan jam sama sekali. Efek beruntun otomatis benar tanpa perbaikan tambahan: `warnaOtomatis()` (Task 18) sudah membaca `terlambat` dari fungsi ini, jadi warna laporan hari libur juga otomatis tidak lagi ikut memerah/menguning karena "terlambat" yang keliru. Pesan konfirmasi setelah kirim (`LaporForm.tsx`) juga dapat varian ketiga: "Terkirim -- hari ini di luar hari wajib lapor, jadi tidak dihitung terlambat." (sebelumnya cuma ada "Terkirim tepat waktu" / "tercatat TERLAMBAT"). Diuji `scripts/uji-terlambat-workday.mjs` (3 titik, fungsi murni direplikasi 1:1 dari kode asli karena skrip `.mjs` tidak bisa mengimpor TypeScript langsung) -- hari bukan kerja + deadline lewat = tidak terlambat (bug ini); hari kerja + deadline lewat = tetap terlambat (regresi tidak terjadi); hari kerja + belum lewat = tidak terlambat.

### Masalah 3 -- Bahasa layar masih bahasa programmer

Tiga perbaikan terpisah:

1. **"Blok N ·" dibuang dari SEMUA judul blok** -- bukan cuma `f01-personal-marketing.ts` (yang dikeluhkan user secara langsung), tapi seluruh 15 file `forms/*.ts` (sapu bersih via `sed` pada pola `judul: '(Blok )?[0-9]+ · '`) plus `forms/blok-bersama.ts` (`blokKeputusanCeo()` kehilangan parameter `nomor` sepenuhnya -- bukan cuma diabaikan, dihapus dari signature dan 12 titik pemanggilnya, supaya tidak ada parameter mati). Judul yang jadi TIDAK UNIK setelah penomoran dibuang (mis. dua form sama-sama punya blok "Target Besok") tidak masalah -- blok itu ada di form yang berbeda, tidak pernah tertampil berdampingan. Referensi silang antar-blok di teks bantuan (`components/LaporForm.tsx`, mis. "lihat Blok 8") diganti kutip nama blok aslinya (mis. "Kontrol Stok Restoran") -- kalau tidak, referensi itu jadi menunjuk ke angka yang sudah tidak ada di mana pun.
2. **`app/papan/page.tsx`**: "Tidak ada laporan yang ditunggu hari ini menurut kebijakan hari kerja (`policy.workdays`)." → "[Hari + tanggal] bukan hari wajib lapor, jadi tidak ada laporan yang ditunggu." (nama hari & tanggal sungguhan, bukan istilah kode) -- dan "Belum ada penugasan (`assignment`) yang tercatat." → tanpa nama tabel.
3. **`app/terpusat/page.tsx`**: label sumber tiap bagian yang tadinya menyebut nama view mentah (`v_pembangunan_hari_ini`, `v_stk_hari_ini`, `v_keuangan_rekap`, `v_marketing_hari_ini`, `v_papan_hari_ini`) diganti kalimat biasa ("dari Laporan Accounting (rekap 4 angka)", dst.), dan "(Task 19)" dibuang dari label Bagian 17.

**Keputusan yang SENGAJA tidak diikuti secara harfiah, perlu dikonfirmasi user**: penomoran "Bagian 1..17" di `app/terpusat/page.tsx` (`<Seksi nomor="..">`) TETAP dipertahankan -- itu bukan nomor "Blok" bikinan implementasi, melainkan nomor SECTION dari dokumen kertas asli klien sendiri (`FORMAT-ASLI-02-PUSAT.md`) yang sudah dipakai Sabrina/CEO sebelum sistem ini ada ("Bagian 11" sudah jadi istilah mereka sendiri). Halaman ini juga bukan yang dikeluhkan user secara langsung (keluhannya soal layar form yang diisi Dadang/Kasam). Kalau user tetap mau nomor ini juga dibuang, tinggal beri tahu -- perubahannya kecil (hapus prop `nomor` dari komponen `Seksi`).

**Beranda BELUM disentuh sama sekali** (poin 4 Masalah 3) -- user eksplisit minta rencana isinya ditinjau dulu sebelum kode ditulis, disampaikan terpisah dari laporan ini.

Verifikasi seluruh batch ini: `tsc`/`build`/`lint` bersih (0 error, 1 warning informasional lama -- termasuk memperbaiki beberapa `react/no-unescaped-entities` dari kutip ganda yang ditambahkan sendiri di teks JSX saat mengganti referensi "Blok N"), `grep` angka bisnis nol hasil di seluruh file baru/ubah, 18 titik uji DB sungguhan tambahan (`uji-tanggal-mundur.mjs` 10 + `uji-riwayat.mjs` 5) plus 3 titik uji fungsi murni (`uji-terlambat-workday.mjs`).

### Masalah 3 (lanjutan) -- Beranda ditulis ulang total setelah rencana isi disetujui

Rencana isi (teks layar literal, bukan penjelasan teknis) ditunjukkan dan disetujui user dulu, BARU kodenya ditulis -- sesuai instruksi eksplisit user di pesan yang meminta rencana ini. Empat perbaikan tambahan diminta di saat yang sama:

1. **Jam batas ditampilkan, bukan cuma nama form.** `lib/tugasHariIni.ts` (baru, fungsi murni) -- `labelSisaWaktu()` menghitung "batas 18.00" (belum lewat) atau "terlambat 2 jam 5 menit" (sudah lewat), dari perbandingan menit "HH:mm" murni (bukan `Date`/instant, jadi tidak ada risiko tebakan zona waktu).
2. **Draft yang belum terkirim dibedakan dari "belum dikerjakan".** `hitungTugasHariIni()` membandingkan penugasan (`assignment` + kewajiban `personal_marketing` utk role `karyawan`) dengan baris `report` MILIK SENDIRI hari ini (`lib/api/beranda.ts`, `useLaporanHariIniSaya()`, query baru -- author_id=diri sendiri, tanggal=hari ini, draft ATAU terkirim, beda dari riwayat yang cuma ambil status ≠ draft). Status `draft` → label "tersimpan, belum dikirim" + tombol "Lanjutkan"; status `belum` → label jam batas + tombol "Isi sekarang"; status `selesai` → tidak ditampilkan di daftar sama sekali.
3. **Progres undangan/closing disembunyikan selama `pte_mulai_berlaku` masih kosong.** Dibaca dari `progres.pte_berlaku` (kolom `v_marketing_bulanan`, SATU sumber kebenaran yang sudah ada sejak Task 15 -- BUKAN membaca ulang `policy.pte_mulai_berlaku` secara terpisah, itu jebakan §7 poin 5 yang sudah pernah kejadian di modul ini). Baris "Undangan .../closing ..." di Beranda cuma dirender kalau `pte_berlaku === true`.
4. **Putri (CEO) dikeluarkan dari kewajiban `personal_marketing`.** Ditemukan lewat pemakaian Beranda sungguhan (bukan asumsi) -- CEO seharusnya tidak diminta mengisi laporan PTE marketing pribadinya sendiri, tapi role `karyawan` (satu-satunya penentu kewajiban itu) sempat dipasang ke akun uji Putri karena salah baca `BLUEPRINT.md` §4 ("semua user role `karyawan` tanpa kecuali" dimaksudkan untuk STAF, bukan pemilik perusahaan). Diperbaiki di TIGA tempat, bukan cuma dokumentasi: `docs/DATA-KARYAWAN.md` §1 dan §3 (+catatan penjelasan eksplisit supaya tidak terulang), `scripts/akun.json` (sumber seed `buat-akun.mjs` -- kalau tidak ikut diubah, re-seed di masa depan akan mengembalikan bug yang sama), dan baris `role` Putri di database PRODUKSI sungguhan (`delete from role where ... role='karyawan'`, dijalankan langsung -- `buat-akun.mjs`/`on conflict do nothing` TIDAK akan pernah menghapus role lama yang sudah kadung ada, cuma menambah yang baru). **35 orang lain TIDAK disentuh** -- aturan umum role `karyawan` tetap berlaku, ini pengecualian eksplisit satu orang.

`app/page.tsx` ditulis ulang: sapaan waktu (`sapaanWaktu()`, Selamat pagi/siang/sore/malam berdasar jam WIB) + nama dari `profile.nama`, lalu `DaftarTugas` (tiga keadaan: hari libur -- "tidak ada laporan yang wajib dikirim" + tautan Riwayat; ada tugas -- daftar kartu dengan jam batas/tombol; semua selesai -- "✅ Laporan hari ini sudah lengkap" + tautan Riwayat), lalu `DashboardCeo` (KOMPONEN LAMA, TIDAK diubah isinya) di bawahnya untuk role `ceo` -- persis "dashboard muncul DI BAWAH, bukan menggantikan" sesuai instruksi.

Diuji `scripts/uji-tugas-beranda.mjs` (20 titik, fungsi murni direplikasi 1:1 dari `lib/tugasHariIni.ts` -- alasan sama dengan `uji-terlambat-workday.mjs`, skrip `.mjs` tidak bisa mengimpor TypeScript langsung tanpa loader tambahan): batas jam `labelSisaWaktu`/`sapaanWaktu` per menit; assignment dobel ke scope yang sama dedup jadi 1 tugas (bukan 2); CEO tanpa role karyawan → 0 tugas personal_marketing; status draft/terkirim/belum masing-masing menghasilkan label+tombol yang benar; `form_key` yang belum terdaftar di registry dilewati (tidak jadi tugas mati); deadline `per_shift` (security) dipakai benar per shift. Diverifikasi juga: `grep 'karyawan'` di seluruh `app/`/`components/`/`lib/` untuk memastikan tidak ada tempat lain yang diam-diam mengasumsikan setiap user pasti punya role `karyawan` (cuma `KopHalaman.tsx` tab "Lapor" dan `lib/tugasHariIni.ts` sendiri -- keduanya memang seharusnya begitu, Putri otomatis juga kehilangan tab "Lapor" sekarang, konsisten).

`tsc`/`build`/`lint` bersih (0 error, 1 warning lama).

---

## §2 tampilan mobile + PWA (`docs/06-RENCANA-PRESENSI-MOBILE.md`, 29 Agustus 2026)

Dokumen baru dari klien menambah tiga permintaan (tampilan mobile, presensi ber-radius, ekspor Excel/PDF); dikerjakan HANYA §2 (tampilan mobile) sesuai instruksi user -- presensi (§3) dan ekspor (§4) sengaja belum disentuh, keduanya butuh keputusan kebijakan CEO yang belum ada (pertanyaan di §5 dokumen).

**Nav bawah, maksimal 5 tombol, isi menyesuaikan peran.** `lib/navUtama.ts` (baru, fungsi murni) -- satu daftar kandidat berurutan-prioritas (`['papan','keputusan','terpusat','lapor','riwayat','marketing','admin']`), Beranda+Akun jadi 2 slot tetap, 3 slot tengah diisi dari daftar itu sesuai peran yang dipunyai user. Tombol yang tidak muat (luapan) TIDAK hilang -- muncul di halaman baru `/akun` (`app/akun/page.tsx`, juga berisi kartu profil + tombol Keluar). `components/KopHalaman.tsx` sekarang merender KEDUANYA sebagai saudara: `<header class="nav-atas">` (desain lama, tak berubah) dan `<nav class="nav-bawah">` (baru) -- CSS media query yang menentukan mana yang tampil (lihat poin berikutnya), bukan JavaScript. Diuji `scripts/uji-nav-mobile.mjs` (fungsi murni, 7 kelompok skenario) -- termasuk memverifikasi ulang kedua contoh literal di dokumen (kombinasi peran Dadang, Sabrina, dst.).

**Bug nyata ditemukan saat menulis `navUtama.ts`, di luar cakupan §2 tapi keliru sejak Task 06:** tab "Keputusan" di `KopHalaman.tsx` lama cuma digerbangi `peran:'ceo'`, padahal `Terlindungi` di `app/keputusan/page.tsx` dan RLS `dec_select` sejak Task 19 sudah mengizinkan `pusat` juga MELIHAT (tanpa tombol putuskan) -- Sabrina (pusat) berwenang tapi tidak punya jalan menu ke halaman itu. Diperbaiki jadi `['ceo','pusat']` bersamaan dengan pembangunan nav baru.

**`padding-bottom: env(safe-area-inset-bottom)` + nav atas jadi nav bawah di layar sempit.** `app/tokens.css` (`--tinggi-nav-bawah: 56px`) + `app/globals.css`: `.nav-bawah` `position:fixed;bottom:0` dengan `padding-bottom:env(safe-area-inset-bottom)` (supaya tombol tidak tertutup home bar iPhone), `body` diberi `padding-bottom` setinggi nav supaya konten tidak tertutup; di atas 768px `.nav-atas` tampil dan `.nav-bawah`/padding-body dimatikan lewat `@media(min-width:768px)`. `env(safe-area-inset-bottom)` cuma bernilai non-nol kalau viewport punya `viewport-fit=cover` -- ditambahkan lewat `export const viewport: Viewport` baru di `app/layout.tsx` (Next 16 `Viewport` type, bukan meta tag manual).

**Tombol Kirim menempel di atas nav pada form panjang.** `components/FormRenderer.tsx` -- blok pesan error + tombol submit dibungkus `.tombol-kirim-menempel` (`position:sticky; bottom: calc(var(--tinggi-nav-bawah) + env(safe-area-inset-bottom))`, kembali ke posisi statis di atas 768px), tinggi tombol 44px→48px (lihat poin sasaran sentuh).

**PWA installable + `display:standalone`.** User menyebut `@serwist/next` secara eksplisit sebagai pengecualian kunci stack yang disetujui -- **sebelum dipasang**, dicek dulu (WebSearch + log build sungguhan) bahwa `@serwist/next` butuh webpack, sedangkan proyek ini build dengan Turbopack (default Next 16, dikonfirmasi dari log build, tidak ada flag `--webpack` di `package.json`). Alih-alih diam-diam mengganti paket atau memaksa paket yang salah, ditanyakan ke user lewat pilihan eksplisit -- **user memilih `@serwist/turbopack`** (Route Handler, bukan plugin bundler, `next build` tetap Turbopack sepenuhnya). Dipasang: `serwist` (dependency), `@serwist/turbopack`+`esbuild` (devDependency, sesuai kebutuhan paket itu sendiri). `next.config.ts` dibungkus `withSerwist()`; `app/serwist/[path]/route.ts` (Route Handler yang men-generate `/serwist/sw.js` dari `app/sw.ts` lewat esbuild native); `app/sw.ts` (strategi cache bawaan Serwist `defaultCache` -- halaman `NetworkFirst`, aset statis `CacheFirst`/`StaleWhileRevalidate`; SENGAJA bukan aplikasi offline penuh, tidak diminta dokumen); `app/layout.tsx` dibungkus `<SerwistProvider swUrl="/serwist/sw.js" disable={NODE_ENV==='development'}>` (dimatikan saat `npm run dev` supaya cache service worker tidak mengganggu hot-reload).

Ikon PWA (`app/icon.tsx`/`apple-icon.tsx`/`icon-192/route.tsx`/`icon-512/route.tsx`) dan `app/manifest.ts` dibuat dari kode langsung (`next/og`'s `ImageResponse`, warna token blueprint `#123A56`/`#F1F3EE`, monogram "KG") -- tidak ada aset logo di repo. `export const runtime='edge'` (deprecated di Next 16, build sempat memperingatkan) dibuang dari keempatnya; dua Route Handler polos (`icon-192`/`icon-512`) diberi `export const dynamic='force-static'` supaya tetap ter-prerender statis seperti dua konvensi `icon.tsx`/`apple-icon.tsx`.

**Bug nyata kedua, ditemukan lewat curl sungguhan (bukan cuma dibaca kodenya):** matcher `proxy.ts` (ditambah 24 Agustus utk manifest/ikon) awalnya cuma mengecualikan `sw\.js` literal di root -- rute service worker sesungguhnya `/serwist/sw.js` (di bawah folder `serwist`, bukan `/sw.js`), jadi tetap ke-redirect 307 ke `/masuk` saat dicurl tanpa sesi. Browser/OS mengambil manifest+ikon+service worker SELALU tanpa sesi (pengecekan "bisa dipasang" terjadi sebelum ada login sama sekali) -- kalau dibiarkan, seluruh fitur PWA rusak total di produksi. Diperbaiki: pengecualian `sw\.js` diganti `serwist` (prefiks folder, mengecualikan seluruh rute di bawahnya). Diverifikasi ulang lewat `npm run dev` + curl: `/serwist/sw.js` → 200 `application/javascript`; `/manifest.webmanifest`, `/icon`, `/icon-192`, `/icon-512`, `/apple-icon` → 200; halaman terlindungi (`/`, `/papan`) tanpa sesi → tetap 307 ke `/masuk` (proteksi lain tidak ikut rusak).

**Kenapa bug ini berbahaya kalau tidak ketahuan sekarang**: gejalanya SENYAP -- tidak ada error di layar, tidak ada apa pun yang "rusak" terlihat. Satu-satunya gejala adalah tombol/petunjuk "Tambah ke Layar Utama" yang tidak pernah muncul di browser HP, sesuatu yang mudah dianggap "belum sempat dicoba" alih-alih "memang rusak". Kalau tidak ditemukan lewat curl sengaja hari ini, bisa lolos berhari-hari sampai seseorang benar-benar mencoba memasang PWA-nya dari HP.

**Sasaran sentuh minimal 48px.** CLAUDE.md aturan #9 menetapkan minimal 44px untuk SELURUH aplikasi -- dokumen baru ini minta 48px, tapi cuma untuk elemen nav mobile. **Diselesaikan dengan mempersempit cakupan**, bukan menaikkan angka di semua tempat: 48px diterapkan HANYA ke tombol nav bawah, tautan luapan di `/akun`, dan tombol Kirim yang menempel (tiga elemen yang eksplisit disebut dokumen). Sasaran sentuh 44px yang sudah ada di tempat lain (field form, dst.) TIDAK diubah -- kalau user mau 48px berlaku app-wide, itu keputusan terpisah yang perlu dikonfirmasi karena mengubah aturan CLAUDE.md.

Verifikasi: `tsc --noEmit`/`npm run lint`/`npm run build` bersih (0 error, 1 warning informasional lama yang tidak terkait) -- dijalankan ULANG setelah perbaikan bug `proxy.ts` di atas, tetap bersih. `scripts/uji-nav-mobile.mjs` (fungsi murni, 7 kelompok skenario, semua lolos). Build menunjukkan `/serwist/sw.js` ter-precache 32 entri (1550KB).

---

## Uji lapangan HP -- Checkpoint 4 LEWAT (29 Agustus 2026)

User menjalankan checklist 10 langkah (disiapkan sebagai artifact interaktif satu-langkah-per-layar) langsung dari HP sungguhan terhadap produksi -- **10 dari 10 SESUAI**, tanpa satu pun meleset. Urutan sengaja dari yang paling mungkin gagal:

1. Pasang ke Layar Utama (PWA) -- **ini yang paling penting**: bug redirect `/serwist/` yang diperbaiki di batch §2 (lihat di atas) terbukti benar-benar hilang, "Tambah ke Layar Utama" muncul dan `display:standalone` bekerja (tanpa address bar) di perangkat asli, bukan cuma lewat `curl` lokal.
2. Login sebagai Dadang (`pic_lokasi`, mewakili pola pakai 30 dari 36 orang).
3. Beranda: sapaan + daftar tugas + jam batas terbaca benar.
4. Isi & kirim Laporan Personal Marketing, termasuk lampiran foto LANGSUNG dari kamera HP (bukan galeri) -- jalur `kompresGambar()` (canvas/`createImageBitmap`, satu-satunya bagian Task 11 yang sama sekali tidak bisa dites tanpa browser sungguhan) terbukti jalan di perangkat nyata.
5. Uji penolakan bukti wajib -- centang "Live"=Ya tanpa lampiran, submit DITOLAK, halaman scroll otomatis ke field, field ter-highlight. Ini menutup TIGA baris debt Task 09 sekaligus (highlight, `scrollIntoView`, submit tertahan) yang dari awal proyek cuma "disimpulkan dari cara kerja `handleSubmit`", sekarang benar-benar diamati.
6. Isi Laporan Lokasi, centang "Butuh keputusan CEO".
7. Riwayat -- kedua laporan muncul, detail baca-saja terbuka, foto dari kamera bisa dilihat lagi lewat signed URL.
8. Login sebagai Putri (CEO) -- item dari Langkah 6 muncul di antrean Keputusan dengan nama pelapor & asal laporan benar.
9. Login sebagai Sabrina (`pusat`) -- Laporan Terpusat menampilkan angka nyata dari laporan Dadang, bukan kosong.
10. Sabrina coba buka `/lapor/accounting` langsung lewat address bar -- **ditolak**, halaman "Tidak punya akses" tampil. Ini kasus "login tapi peran salah" yang sejak Task 06 cuma pernah diuji jalur anonim lewat proxy, sekarang teruji jalur sungguhan yang justru lebih penting (Task 06 debt).

Baris CHECKPOINT 4 yang ikut tertutup langsung oleh kesepuluh langkah ini ditandai ✅ di bagian "Utang uji -- CHECKPOINT 4" di bawah, satu per satu -- TIDAK ditebak lolos untuk hal yang tidak diamati langsung (mis. tombol Setujui/Cicil/Tunda/Tolak di antrean Keputusan tetap tercatat terbuka, karena Langkah 8 cuma memverifikasi barisnya MUNCUL, bukan mengklik tombolnya).

## Task 24 -- Matriks uji RLS lengkap, 14 baris (29 Agustus 2026)

Seluruh matriks §4 `04-CATATAN-TEKNIS.md` dijalankan sekali jalan dalam SATU transaksi lewat `scripts/uji-task24-matriks.mjs` (penerus `scripts/uji-checkpoint2.mjs`, pola sama: `pg.Client` satu sesi, `set_config('request.jwt.claims', ...)` untuk menyamar sebagai tiap akun, `auth.uid()` dicek ulang tiap ganti persona, BEGIN/ROLLBACK di akhir apa pun hasilnya). **14 dari 14 LOLOS di percobaan pertama** -- tidak ada yang gagal, jadi tidak perlu perbaikan+ulang sesuai instruksi user.

Dua baris yang dari Checkpoint 2 (23 Agustus) memang sengaja ditunda karena infrastrukturnya belum ada, sekarang dijalankan untuk pertama kali:

- **#3** (`select * from v_keuangan_rekap` sebagai `pusat`): laporan `accounting` dummy diberi angka SUNGGUHAN (`total_masuk=5.000.000`, `total_keluar=2.000.000`) DAN umpan rahasia `saldo_bank:"RAHASIA_UMPAN_JANGAN_BOCOR"` ditanam di kolom `data` JSON-nya (metodologi "umpan RAHASIA" sesi ini) -- hasil view persis 5 kolom (`tanggal`/`total_masuk`/`total_keluar`/`net`/`warna`), `net=3.000.000` benar, dan umpan itu **tidak** ikut muncul di mana pun pada hasil query. LOLOS.
- **#10** (buka path storage orang lain tanpa signed URL): satu baris `storage.objects` ditanam untuk laporan Toyib, lalu Kasam (karyawan biasa, tanpa peran istimewa apa pun) mencoba `select` LANGSUNG ke `storage.objects` (bukan lewat `createSignedUrl`, persis skenario "buka path mentah") -- 0 baris, RLS `bukti_read` menahan. LOLOS.

`npm run build`, `npx tsc --noEmit`, `npm run lint` seluruhnya bersih di kode produksi saat ini (0 error).

---

## Atur ulang kata sandi lewat Admin -- ditutup sebelum peluncuran (29 Agustus 2026)

Saat menulis `docs/07-CATATAN-PELUNCURAN.md`, ditemukan (bukan diminta lebih dulu): sistem ini **tidak punya halaman "Lupa Password"**, dan alur reset bawaan Supabase (email) tidak bisa dipakai -- email `@koperumnas.local` tidak nyata, satu-satunya jalan reset yang ada (`scripts/set-password.mjs`) butuh terminal + kunci service role, dalam praktik cuma bisa lewat developer. Untuk 36 orang ini pasti jadi masalah nyata minggu pertama, bukan risiko teoretis. User setuju temuannya dan minta dikerjakan sebelum peluncuran, sebelum lanjut membaca catatan itu.

**Dibangun:**
- `supabase/migrations/0021_reset_password_log.sql` -- tabel `reset_password_log` (`actor_id`, `target_id`, `created_at`), RLS aktif, HANYA policy `select` (`has_role('ceo')`) -- sengaja TANPA policy insert/update/delete sama sekali, supaya cuma jalur `service_role` (Route Handler) yang bisa menulis, klien biasa (anon/authenticated) otomatis ditolak RLS tanpa perlu policy eksplisit yang menolak. Dijalankan langsung (`npm run db --`), diverifikasi `relrowsecurity=true`.
- `app/api/admin/user/[id]/reset-password/route.ts` (baru) -- pola PERSIS `app/api/admin/user/route.ts` (Task 23): dua lapis penjaga (sesi login, lalu `has_role('ceo')` lewat klien sesi itu sendiri), password baru `crypto.randomBytes(12).toString('base64url').slice(0,16)` (pola sama `set-password.mjs`), `admin.auth.admin.updateUserById()`, lalu insert `reset_password_log`. Kegagalan mencatat log SENGAJA tidak menggagalkan respons (password sudah terlanjur berhasil diganti saat itu) -- cuma di-`console.error`, supaya pemanggil tidak salah kira reset gagal lalu mencoba lagi tanpa perlu.
- `lib/api/admin.ts` -- `useAturUlangKataSandi()`, `fetch` POST ke rute di atas.
- `app/admin/page.tsx` (tab Pengguna) -- tombol "Atur ulang kata sandi" per baris (dengan `confirm()` sebelum jalan -- fitur ini mengganti password ORANG LAIN langsung, satu-satunya aksi di halaman Admin yang diberi konfirmasi eksplisit, sebanding besarnya dampak). Password baru tampil SEKALI di kotak hijau di bawah barisnya (`select-all`, gampang disalin), ditutup manual atau otomatis tertutup begitu reset lain dijalankan -- tidak pernah disimpan di state React lain.

**Sengaja TIDAK dibangun**: alur "Lupa Password" mandiri lewat email -- ditolak eksplisit oleh user, minta-ke-admin dianggap jalur yang lebih benar untuk perusahaan ini. Role baru `admin_akun` untuk Sabrina/HRD -- diusulkan (bukan diputuskan sepihak), user **menyetujui isi usulannya tapi menunda pelaksanaannya** -- lihat "Keputusan ditunda" tepat di bawah bagian ini, jangan cuma baca paragraf ini karena detail alasannya ada di situ.

**Diverifikasi:** `tsc`/`build`/`lint` bersih (0 error, 1 warning lama tidak terkait); `scripts/uji-reset-password-log.mjs` (RLS, 3 titik, penyamaran `set role authenticated` + BEGIN/ROLLBACK) -- karyawan biasa 0 baris terlihat, ceo lihat baris yang ada, karyawan biasa insert langsung (bukan lewat service_role) ditolak; `curl` sungguhan tanpa sesi ke rute baru -- 307 ke `/masuk`, proxy menolak sebelum sampai handler (matcher `proxy.ts` sudah benar untuk pola `/api/admin/**` sejak Task 23, tidak perlu perubahan). **Belum diverifikasi**: sesi cookie CEO sungguhan mengklik tombolnya di browser (sama seperti kasus serupa Task 23, masuk CHECKPOINT 4 -- butuh replikasi cookie `@supabase/ssr` yang rumit lewat `curl` mentah); mekanisme inti (`admin.auth.admin.updateUserById`) sendiri sudah terbukti jalan lewat `scripts/set-password.mjs` yang dipakai sungguhan 2x sesi ini, jadi risikonya rendah.

### Keputusan ditunda: role `admin_akun` (29 Agustus 2026)

**Usulan disetujui ISINYA, tapi PELAKSANAANNYA ditunda** -- dicatat di sini persis supaya tidak hilang begitu percakapan yang mengusulkannya tidak lagi diingat.

**Usulan:** role baru `admin_akun`, diberikan ke SATU orang tepercaya tertentu (kandidat yang disebut: Sabrina) untuk mengklik "Atur ulang kata sandi" di halaman Admin, supaya tidak semua permintaan reset harus menunggu CEO sendiri pegang HP/laptop.

**Pengecualian WAJIB kalau role ini pernah dibuat** (user menegaskan ulang, ini alasan utama fitur ini ditunda bukan langsung dibangun): pemegang `admin_akun` **TIDAK BOLEH** bisa mengatur ulang kata sandi akun `ceo` maupun `accounting`. Alasannya konkret, bukan kehati-hatian umum: siapa pun yang bisa mereset password Accounting **bisa masuk ke laporan keuangan** dengan password barunya sendiri -- membuat seluruh matriks RLS 14 baris yang sudah LOLOS (Task 24, khususnya baris #1/#3/#13 yang menjaga kerahasiaan `accounting`) jadi tidak berarti apa-apa, karena kebocorannya lewat jalur AUTH, bukan lewat query yang RLS awasi. Kalau implementasi nanti dijalankan, pengecualian ini WAJIB diberlakukan di server (Route Handler), bukan cuma disembunyikan di UI -- cek eksplisit "target punya role `ceo` atau `accounting` → tolak, kecuali pelaku sendiri `ceo`".

**Kenapa ditunda, bukan langsung dibangun (alasan user, dicatat verbatim maksudnya):** belum ada data seberapa sering tombol reset ini benar-benar dipakai. Kalau ternyata cuma ~2x/bulan, CEO sendiri cukup -- menambah role adalah privilege baru yang, sekali orang terbiasa memakainya, jauh lebih sulit dicabut lagi dibanding tidak pernah dibuat sama sekali. Keputusan yang gampang dibatalkan (tunda) lebih aman daripada keputusan yang sulit dibatalkan (buat lalu cabut).

**Syarat untuk ditinjau ulang:** setelah sistem berjalan **~1 bulan produksi dengan 36 pengguna asli** (bukan 7 akun uji), lihat `reset_password_log` -- hitung berapa kali dipakai per minggu. Kalau frekuensinya tinggi (mis. rutin tiap minggu ada yang minta), baru pertimbangkan membangun `admin_akun` sesuai usulan di atas, LENGKAP dengan pengecualian `ceo`/`accounting`-nya. Kalau jarang, biarkan seperti sekarang -- `ceo` saja.

---

## Token desain diganti total -- "ringan, bukan resmi" (30 Agustus 2026)

User sendiri yang menulis token lama (`04-CATATAN-TEKNIS.md` §6, arah "cetak biru gambar teknik") dan sendiri yang membatalkannya: dioptimalkan untuk terlihat khas, bukan untuk 35 orang lapangan mengisi form panjang tiap hari dari HP. Rencana token ditunjukkan dulu ke user sebelum kode ditulis (instruksi eksplisit), disetujui dengan 5 koreksi (dijelaskan di tiap sub-bagian di bawah), BARU diterapkan.

**Token baru** (nilai lengkap ada di `04-CATATAN-TEKNIS.md` §6 dan `app/tokens.css`):
- Latar `#FBFBF9` (dulu `#E6E9E3`), garis `#E4E0D3` lembut hangat (dulu `#9FAEA9` dipakai di 1,5-2px `--tinta` pekat).
- `--tinta` (teks isi) dihangatkan ke `#1F2A33` (dulu `#10202E`) -- **koreksi user**, bukan usulan awal saya (usul pertama tidak menyentuh `--tinta` sama sekali; user sendiri yang minta setelah melihat token pertama, alasannya kontras terlalu keras untuk dibaca lama di atas kertas hangat). `--biru`/`--hijau`/`--kuning`/`--merah` **TIDAK disentuh** -- ditegaskan ulang user sendiri, itu bahasa status baku perusahaan.
- SATU keluarga huruf, Plus Jakarta Sans, untuk SELURUH antarmuka -- dulu dua (Barlow Condensed + IBM Plex Sans). Mono (IBM Plex Mono) dipersempit HANYA untuk rupiah/jam/koordinat.
- `text-transform: uppercase` DIBUANG TOTAL -- keluhan #1 user ("penyebab terbesar rasa kakunya").
- `border-radius`: 8px input/tombol, 12px kartu, 999px (pil) khusus tombol lampirkan -- dulu 0 di mana pun (CLAUDE.md aturan #10 lama). **Kecuali sel tabel** (`<td>`/`<th>`) -- ditemukan sendiri saat menerapkan (bukan diminta): radius 12px per sel bikin celah aneh di grid tabel, jadi dikecualikan tetap kotak.
- Hierarki eksplisit: judul bagian 17px/weight 500/warna `--biru`, label field 13px/warna `--label` (token baru), isian 16px.
- **Koreksi user (3 hal di luar daftar awal, tapi ikut menentukan rasa):** jarak antar field 16px/antar bagian 28px (dulu gap-3/gap-6 Tailwind = 12px/24px), tinggi input minimal 44px (jaring pengaman lewat CSS global, bukan cuma andalan per-komponen), `line-height: 1.6` khusus teks penjelasan (kelas baru `.teks-penjelasan`).
- Baris bukti terlampir: latar `--hijau-lembut` (token baru) + ikon ✓ + teks "Bukti terlampir" -- bukan cuma daftar nama file polos (`components/fields/LampiranInput.tsx`).

**Mekanisme penerapan -- kenapa cuma segelintir file untuk sebagian besar perubahan:** radius, uppercase-removal, garis, dan keluarga huruf SEMUANYA diterapkan lewat SATU perubahan di `app/globals.css` (selector global: `.border`, `button`/`input`/`select`/`textarea`, `h1-h6`, dst.) -- BUKAN diulang di tiap komponen. Uppercase khususnya: aturan lama cuma ada di SATU baris (`h1,h2,h3,h4,h5,h6,button,label{text-transform:uppercase}`), jadi menghapusnya otomatis membersihkan SELURUH aplikasi sekaligus, persis instruksi "jangan setengah lama setengah baru". Keluarga huruf: `--display`/`--body` (nama token lama) dipertahankan sebagai ALIAS ke `--font` yang baru di `app/tokens.css` -- kode yang sudah menulis `var(--display)` di puluhan tempat TIDAK perlu diubah untuk urusan font-nya, cuma tokennya yang berubah.

**Yang TETAP butuh diedit satu-satu** (tidak bisa lewat CSS global karena beda konteks/ukuran per elemen): hierarki ukuran judul-bagian vs label vs teks biasa -- 59 titik `fontFamily: 'var(--display)'` di 15 file, ditinjau SATU-SATU (bukan disamakan buta) supaya judul section (17px/weight 500/biru) tidak disamakan dengan label baris list yang lebih kecil (cukup weight 500 tanpa ukuran dipaksa naik).

**Bug nyata ditemukan sambil menata (bukan dicari-cari):** `app/akun/page.tsx` memanggil `tabTerlihat()` TANPA parameter `divisi` -- kadiv+HRD yang tab "Tinjau Absensi"-nya kalah prioritas nav bawah dan seharusnya muncul di halaman Akun (luapan) akan kehilangannya DI SITU JUGA (`bolehTinjauAbsen` salah mengira `false` tanpa `divisi`). Diperbaiki sekalian, sama pola dengan bug `KopHalaman.tsx` yang sudah pernah ditemukan.

**Sisir ulang "Blok" (instruksi #1, dilanjutkan dari perbaikan Masalah 3 tanggal 24 Agustus):** ditemukan 3 sisa jargon di TEKS YANG BENAR-BENAR TAMPIL ke pengguna (`field.bantuan`/`block.catatan`, dirender `FormRenderer.tsx`) -- `forms/f01-personal-marketing.ts` ("...di Blok 4 (PTE)..." dan "...progres Blok 3 & 6" -- yang kedua disederhanakan jadi "progres undangan yang ditampilkan di bagian atas", bukan dipaksa mengarang ulang nomor blok lama yang sudah dibuang), `forms/f16-ita.ts` ("Blok ini cuma tampil hari Senin" -> "Bagian ini..."). Sisa "Blok" lain di seluruh `forms/`/`components/` SEMUANYA komentar developer (JSDoc, `//`), bukan teks layar -- diverifikasi lewat grep bertarget, tidak dihapus (komentar developer boleh tetap teknis). **"Bagian 1-17" di Laporan Terpusat TETAP dipertahankan** sesuai instruksi -- itu nomor dari kertas asli perusahaan, bukan istilah "Blok" bikinan implementasi.

**Diverifikasi:** `tsc --noEmit`/`npm run lint`/`npm run build` bersih (0 error, 1 warning lama tidak terkait). **BELUM diverifikasi visual sungguhan** -- user eksplisit akan memeriksa langsung dari HP, bukan dari deskripsi; tidak ada tool browser/screenshot di sesi ini untuk memverifikasi tampilan akhir sendiri.

---

## Umpan balik lapangan pertama dari HP sungguhan (30 Agustus 2026)

Setelah token desain di atas dipakai nyata di HP: nav bawah dirasa "masih tulisan" -- terlalu kaku dibanding maksud "ringan" yang baru disepakati. Accounting terjawab. Dua dari enam titik absen dikirim.

**Ikon nav bawah.** `components/NavIcon.tsx` (baru) -- 11 ikon SVG tulis tangan (garis tipis, `currentColor`, TANPA library ikon -- CLAUDE.md §2 melarang "library ikon berat"), satu per tab (`beranda`/`absen`/`riwayat`/`lapor`/`papan`/`keputusan`/`marketing`/`terpusat`/`admin`/`akun`/`absen-tinjau`). `ikonUntukTab()` memetakan tab dinamis `lapor-dinamis-*` ke ikon "lapor" yang sama. **HANYA nav bawah (mobile) yang berubah** -- ikon + label kecil (10px) di bawahnya, bukan ikon saja (dipertimbangkan sengaja: 35 orang lapangan belum tentu langsung kenal ikonnya, label kecil tetap menjaga bisa dipelajari). Nav atas (desktop) TIDAK disentuh sama sekali, sesuai instruksi eksplisit user ("kecuali kita dalam desktop device"). **Belum diverifikasi visual sungguhan** -- sama seperti batch token desain sebelumnya, user yang akan mengecek langsung dari HP.

**Accounting = Shabita (menjawab pertanyaan blocking §0 `docs/07-CATATAN-PELUNCURAN.md`).** Diperbarui di TIGA tempat -- `profile.nama` akun uji `accounting@koperumnas.local` di database produksi (`UPDATE` langsung, bukan lewat script), `scripts/akun.json` (sumber seed 36 akun asli), dan `docs/DATA-KARYAWAN.md` (§2/§3/§4, placeholder `"GANTI"`/`❓` diganti). `docs/07-CATATAN-PELUNCURAN.md` §0 baris 1 ditandai ✅ terjawab.

**2 dari 6 titik absen masuk (kemudian jadi 5 dari 6, lihat pembaruan di bawah).** User mengirim dua koordinat tanpa label -- **ditanya dulu lewat `AskUserQuestion` yang itu titik mana** (bukan ditebak dari angkanya) sebelum disimpan, karena label yang salah pada koordinat presensi sungguhan = orang absen di tempat yang keliru tanpa sadar (persis risiko yang sudah dicatat di §3.1 dokumen). Jawaban: **Kantor Pusat** (`-6.159509598802398, 106.84286041754672`) dan **Indokopi Jatinegara** (`-6.218618166784612, 106.86853622283408`) -- dimasukkan langsung ke `lokasi_absen` (SQL langsung, bukan migrasi -- ini data operasional, bukan perubahan skema), baris `CONTOH` dinonaktifkan (`aktif=false`, bukan dihapus).

**+3 titik lagi, kali ini sudah berlabel (30 Agustus 2026, pesan terpisah)** -- **Indosteak** (`-6.176809266505165, 106.87548763558218`), **Tajur** (`-6.524353196506258, 106.92400902890137`), **Bekasi** (`-6.094979202877694, 107.09285972305547`). Tajur & Bekasi ditautkan ke `lokasi_absen.lokasi_id` yang cocok di tabel `lokasi` (sudah ada sejak Task 05) -- Indosteak TIDAK ditautkan, karena kolom itu cuma mengarah ke `lokasi`, bukan `outlet` (keterbatasan skema asli dokumen, bukan sesuatu yang saya perbaiki diam-diam). **Total sekarang 5 dari 6** -- tinggal **DTI** yang masih kosong. Belum ada `penugasan_absen` (siapa absen di titik mana) sama sekali -- langkah berikutnya lewat Admin setelah titik terakhir lengkap.

**Diverifikasi:** `tsc --noEmit` bersih untuk `NavIcon.tsx`/`KopHalaman.tsx`. Data Shabita & 2 koordinat dicek ulang lewat `select` langsung setelah `update`/`insert`.

---

## Tombol bundar Absen di tengah nav bawah (30 Agustus 2026)

Susulan langsung dari umpan balik ikon di atas -- masih di sesi yang sama, user minta Absen naik level dari tab biasa jadi tombol bundar terangkat, terpisah dari lima tab lainnya, dengan status hari ini terlihat tanpa buka halaman.

**Struktur baru nav bawah.** `absen` DIHAPUS TOTAL dari `PRIORITAS_TENGAH` (`lib/navUtama.ts`) -- bukan lagi tab yang bersaing prioritas, jadi slot tengah turun dari 3 jadi 2 (satu kiri, satu kanan tombol bundar) SELAMA tombolnya tampil. `terpusat` dinaikkan di atas `keputusan` supaya susunan CEO/Pusat jadi persis "Papan · [ABSEN] · Terpusat" (instruksi eksplisit) -- **Keputusan pindah ke halaman Akun**, bukan hilang. Urutan sisanya (`absen-tinjau`/`lapor`/`riwayat`/`marketing`/`admin`) SENGAJA tidak diubah dari batch presensi sebelumnya -- minim disrupsi ke kombinasi peran yang sudah teruji (Fauzy: Lapor+Riwayat tetap menang lawan Marketing, bukan berubah karena penataan ulang ini).

`tabBawah()`/`tabLuapan()` (`lib/navUtama.ts`) sekarang menerima parameter `punyaTitikAbsen: boolean` -- `true` (2 slot tengah, tombol bundar tampil) kalau user punya >=1 baris `penugasan_absen`; `false` (kembali 3 slot tab biasa, TANPA tombol bundar sama sekali) kalau tidak. **✅ Interpretasi dikonfirmasi user (30 Agustus 2026), dipertahankan tanpa perubahan**: "penugasan per orang memang lebih tepat daripada peran -- Putri dan Sabrina belum tentu absen di titik yang sama dengan satpam." Efek sampingnya sudah dilihat langsung: 2 `penugasan_absen` UJI COBA dibuat sengaja (Dadang & Putri -> Kantor Pusat, lihat "Utang WAJIB sebelum presensi dipakai sungguhan" di bawah) supaya tombolnya bisa dites dari HP sebelum penugasan sungguhan lengkap.

`components/AbsenFab.tsx` (baru) -- bundar 56px, `var(--biru)` + ikon putih (`NavIcon` ikon "absen", `currentColor` mewarisi putih), naik `translateY(-14px)` dari posisi wajarnya di nav, label kecil di bawahnya. Status dari `useAbsenHariIni()` (hook yang sama dipakai `app/absen/page.tsx`, tidak ada logika baru): belum absen masuk -> "Absen masuk" (biru); sudah masuk belum pulang -> "Absen pulang" (biru); dua-duanya -> "Sudah absen" (`var(--kosong)`, abu) -- SELALU menuju `/absen` apa adanya, halaman itu sendiri yang menentukan aksi/menampilkan ringkasan, tombol ini murni cermin status + jalan pintas.

**Padding ekstra supaya tombol tidak menutupi isi halaman.** Token baru `--tambahan-fab-absen: 28px` (`app/tokens.css`), ditambahkan ke `body`'s `padding-bottom` DAN posisi `bottom` tombol Kirim yang menempel (`app/globals.css`) -- **sengaja UNCONDITIONAL** (berlaku di semua halaman mobile, bukan cuma yang tombolnya tampil): `punyaTitikAbsen` adalah hasil query async yang tidak terjangkau dari CSS `body` (dikendalikan `app/layout.tsx`, bukan komponen nav) -- sedikit ruang kosong ekstra untuk yang tombolnya tidak tampil jauh lebih aman daripada risiko baris terakhir ketutupan untuk yang tombolnya tampil.

**Diverifikasi:** `tsc --noEmit`/`npm run lint`/`npm run build` bersih (0 error, 1 warning lama tidak terkait). `scripts/uji-nav-mobile.mjs` ditulis ulang total (bukan cuma disunting) mengikuti struktur baru -- 15 titik, semua LOLOS, termasuk memastikan Dadang (pic_lokasi+karyawan) tetap dapat kedua tab Lapor-nya (bukan Riwayat) walau slot tengah turun dari 3 jadi 2. **BELUM diverifikasi visual sungguhan** -- posisi persis tombol relatif ke label (offset `translateY`/`marginTop` yang dipakai) ditulis berdasar perhitungan manual, bukan pengukuran di browser sungguhan; user yang akan mengecek langsung dari HP dan mungkin perlu koreksi kecil setelah dilihat nyata.

---

## §4 Ekspor Excel & PDF (`docs/06-RENCANA-PRESENSI-MOBILE.md`, 30 Agustus 2026)

`exceljs` dipasang (pengecualian kunci stack yang disetujui). Urutan dikerjakan sesuai prioritas eksplisit user: absensi, marketing, laporan per divisi, keuangan, PDF Terpusat.

**Route Handler `app/api/ekspor/{absensi,marketing,laporan,keuangan}/route.ts`** -- SEMUANYA lewat `lib/ekspor/otorisasi.ts`'s `periksaPeranEkspor()`, pola PERSIS `app/api/admin/user/route.ts` (Task 23): sesi login dulu (proxy.ts sudah menolak anonim sebelum sampai sini, diverifikasi ulang lewat curl -- 307 di keempatnya), lalu `role` dicek lewat klien SESI ITU SENDIRI (bukan admin/service_role -- endpoint ekspor sama sekali tidak butuh itu, cukup RLS yang sama dengan layar). Gerbang per endpoint:
- Absensi -> `ceo`/`pusat`/kadiv+divisi HRD (sama seperti halaman Tinjau Absensi)
- Marketing -> `kontrol_marketing`/`ceo`/`pusat` (sama seperti halaman Marketing)
- Laporan per divisi -> `pusat`/`ceo` (sama seperti Laporan Terpusat)
- **Keuangan -> HANYA `ceo`/`accounting`**, ditegaskan dua kali oleh user

**Ditemukan sambil menulis ekspor marketing, BUKAN dicari-cari -- spesifikasi (bukan kode) yang keliru:** `v_marketing_bulanan` (Task 15, migrasi 0007) HANYA PERNAH menghitung bulan BERJALAN (`hi.d := (now() WIB)::date` dipakai tanpa parameter). Ekspor bulanan untuk penggajian justru paling dibutuhkan untuk bulan yang SUDAH SELESAI -- kalau dipaksa query view ini untuk Juli sambil sekarang Agustus, hasilnya tetap angka Agustus, salah tanpa error apa pun. **Migrasi `0023_marketing_bulanan_untuk_bulan.sql`** menambah fungsi `marketing_bulanan_untuk(p_bulan)` yang diparameterkan -- pola SAMA dengan konversi view->fungsi 24 Agustus (Masalah 1). **Keputusan awal: fungsi baru DITAMBAHKAN, TIDAK MENGGANTIKAN view lama** -- dashboard Marketing (Task 22) dan `lib/kalenderPte.ts` (4 file pemanggil) TETAP pakai view lama apa adanya, di luar cakupan "kerjakan ekspor" untuk disentuh saat itu. Konsekuensinya: rumus hari_wajib/hari_bolong sempat ADA DI DUA TEMPAT. **⚠️ SUDAH DISATUKAN -- lihat "Utang ekspor -- KEDUANYA SELESAI" di bawah, migrasi `0024`.** Jangan baca paragraf ini sebagai kondisi saat ini, ini catatan sejarah kenapa `0023` dibuat seperti itu. Diverifikasi (saat `0023` ditulis): hasil fungsi (tanpa parameter) SAMA PERSIS dengan `v_marketing_bulanan` untuk bulan berjalan (5 baris dibandingkan manual), dan untuk bulan lalu (Juli) menghasilkan rentang data yang benar-benar berbeda (tidak bocor dari Agustus).

**Rekap Laporan per Divisi -- interpretasi "satu sheet per divisi" dicatat eksplisit:** satu sheet per FORM (`form_key`), BUKAN literal per `profile.divisi` -- form_key sudah = satu jenis laporan berkolom konsisten (mis. `hrd`→divisi HRD), sedangkan `personal_marketing` dikirim SEMUA karyawan lintas divisi; mengelompokkan per `profile.divisi` asli akan mencampur form berkolom beda-beda dalam satu sheet yang sama. **`accounting` (`rahasia: true`) DIKECUALIKAN MUTLAK dari daftar form yang diekspor di sini** -- gerbang halaman ini (pusat+ceo) lebih lebar dari kerahasiaan accounting (ceo+accounting saja); kalau ikut lewat sini, Sabrina bisa membaca laporan keuangan lewat jalur belakang, pelanggaran fatal CLAUDE.md #3. Field bertipe `tabel` (larik objek) diringkas jadi teks `"N baris"`, TIDAK diekspansi jadi banyak baris Excel -- di luar cakupan yang diminta, dicatat sebagai penyederhanaan. Nama sheet disanitasi (`lib/ekspor/workbook.ts`, `namaSheetAman()`) -- ditemukan saat menulis: nama form DTI ("Laporan Harian DTI / Precast / Perikas") mengandung `/`, karakter yang DILARANG Excel di nama sheet; tanpa sanitasi, ekspor form itu akan gagal total dengan error dari `exceljs`, bukan cuma tampilan jelek.

**Rekap Keuangan -- diuji lewat HTTP SUNGGUHAN, bukan penyamaran RLS.** Instruksi eksplisit user: "Sabrina memanggil endpoint ekspor keuangan langsung lewat curl, harus ditolak" -- pola pengujian BERBEDA dari hampir semua skrip lain sesi ini (yang memakai penyamaran `set role authenticated` di Postgres, cukup untuk RLS tapi TIDAK menguji kode Route Handler-nya sendiri). **`scripts/uji-ekspor-keuangan-curl.mjs`** (baru): daripada menebak sendiri format cookie `@supabase/ssr` (risiko salah diam-diam -- sudah berkali-kali ditunda ke "CHECKPOINT 4" di sesi ini untuk kasus serupa, mis. Task 23 admin route), skrip ini memakai `createServerClient` dari LIBRARY-NYA SENDIRI dengan cookie jar in-memory untuk login sungguhan sebagai Sabrina, lalu MENANGKAP PERSIS Set-Cookie yang ditulis library-nya -- bukan hasil reka-reka. Password Sabrina direset SEKALI, SCOPED ke satu akun (bukan ketujuh-tujuhnya seperti `set-password.mjs`), khusus untuk uji ini. **Hasil: LOLOS pada percobaan pertama** -- ekspor keuangan sungguhan lewat HTTP nyata terhadap Sabrina (pusat) -> 403; kontrol positif di skenario yang sama (ekspor absensi, yang MEMANG boleh untuk pusat) -> 200 dengan `content-type` xlsx yang benar, membuktikan sesi/cookie-nya sungguh valid, bukan kebetulan selalu ditolak karena format cookie salah. **Ini menutup kelas masalah yang berkali-kali ditunda sesi ini** -- metode yang sama (login sungguhan lewat `createServerClient` + cookie jar in-memory) bisa dipakai lagi kapan pun butuh menguji Route Handler lain lewat sesi sungguhan, bukan cuma RLS.

**PDF Laporan Terpusat -- TIDAK ada library PDF baru dipasang.** Sesuai instruksi ("pakai CSS cetak yang sudah ada"): tombol `window.print()` yang sudah dibangun Task 21 dipertahankan apa adanya, cuma ditambah `@page { size: A4; margin: 12mm; }` di CSS cetak `app/terpusat/page.tsx` -- sebelumnya cuma mengatur ukuran huruf & sembunyikan nav/tombol saat cetak, belum pernah mengatur ukuran kertas eksplisit. **Belum diverifikasi visual sungguhan** -- hasil cetak/simpan-PDF asli dari browser HP/desktop belum pernah dilihat, masuk CHECKPOINT 4 baru di bawah.

**UI:** `components/TombolEkspor.tsx` (baru, generik) -- pemilih bulan (`<input type="month">`) + tautan unduh, dipasang di `/absen/tinjau` (absensi), `/marketing` (kepatuhan marketing), `/terpusat` (laporan per divisi + keuangan, yang terakhir cuma dirender kalau `roles.includes('ceo')` karena Accounting/Shabita tidak bisa membuka halaman Terpusat sama sekali -- **utang dicatat**: Shabita saat ini TIDAK PUNYA jalan lewat UI untuk mengunduh ekspor keuangannya sendiri, cuma CEO yang punya tombolnya; endpoint-nya sendiri SUDAH mengizinkan `accounting`, tinggal tempat kliknya yang belum ada).

**Diverifikasi:** `tsc --noEmit`/`npm run lint`/`npm run build` bersih (0 error, 1 warning lama tidak terkait). Keempat endpoint dicurl tanpa sesi -> 307 (proxy menolak). `scripts/uji-ekspor-keuangan-curl.mjs` -- 2 titik, LOLOS (403 keuangan, 200 kontrol positif absensi), HTTP sungguhan bukan penyamaran.

## Utang ekspor -- KEDUANYA SELESAI (30 Agustus 2026, sesi lanjutan)

- ✅ **SELESAI -- Shabita sekarang punya jalan navigasi.** `app/keuangan/page.tsx` (baru) -- gabung `<LaporForm formKey="accounting">` (form harian) + `TombolEkspor` (rekap bulanan) dalam SATU halaman, digerbangi `['accounting','ceo']` (lebih ketat dari rute generik `/lapor/accounting` yang sama sekali tidak ada `Terlindungi`). Ditambahkan ke `lib/navUtama.ts` (`TAB_TETAP` + `PRIORITAS_TENGAH`, prioritas PALING RENDAH sengaja -- untuk Shabita tidak masalah, kandidatnya cuma 3 [lapor/riwayat/keuangan] yang semuanya muat; untuk CEO memastikan TIDAK PERNAH menggeser Papan/Terpusat yang sudah teruji sungguhan di HP).
- ✅ **SELESAI -- rumus marketing bulanan SATU sumber, bukan dua.** Ditegaskan user sebagai "utang berbahaya" (rumus penentu bonus/potongan gaji) -- **migrasi `0024_satukan_marketing_bulanan.sql`**: `v_marketing_bulanan` diganti (`CREATE OR REPLACE VIEW`, bukan drop+create -- kolomnya persis sama, tidak ada yang disisipkan) jadi SEKADAR `select * from marketing_bulanan_untuk()` (parameter default = bulan berjalan). View lama TIDAK dihapus -- dashboard Marketing & `lib/kalenderPte.ts` tetap memanggilnya apa adanya, tidak ada 4 file pemanggil yang perlu diubah. `security_invoker=on` dicek ulang, tetap terpasang setelah `CREATE OR REPLACE`.

  **Dibuktikan, bukan cuma diklaim** (instruksi eksplisit user): `scripts/uji-satukan-marketing-bulanan.mjs` -- menyamar sebagai Fauzy (`kontrol_marketing`, persona sungguhan lewat penyamaran RLS, konsisten dengan instruksi "pakai penyamaran JWT kalau memang perlu menguji lapisan database") membandingkan `v_marketing_bulanan` vs `marketing_bulanan_untuk()` baris per baris, kolom per kolom, untuk bulan berjalan. **Hasil: 7 baris, IDENTIK PERSIS.** Bukan kebetulan identik -- SEKARANG SECARA STRUKTURAL tidak mungkin berbeda, karena view itu literal memanggil fungsi yang sama, bukan dua rumus terpisah yang kebetulan menghasilkan angka sama hari ini.

---

## §3 Presensi ber-radius (`docs/06-RENCANA-PRESENSI-MOBILE.md`, 29 Agustus 2026)

Dikerjakan atas instruksi eksplisit **"jangan menunggu"** koordinat asli CEO (§5 dokumen belum terjawab) -- mekanismenya yang harus jadi dulu, satu baris `lokasi_absen` CONTOH (koordinat dari contoh dokumen sendiri, `-6.914744, 107.609810`, berlabel "CONTOH -- ganti dari halaman Admin") dipakai supaya halaman Absen tidak kosong/rusak saat pertama dicoba.

**Rencana layar disetujui dulu sebelum kode** (instruksi eksplisit user) -- termasuk 4 tambahan yang diminta setelah rencana pertama disetujui:

1. **Titik terdekat DITAMPILKAN, bukan dipilih diam-diam.** Setelah GPS didapat, layar konfirmasi "{nama titik} · {jarak} meter" + tombol `[ Ganti ]` (kalau punya >1 penugasan) muncul SEBELUM kamera -- baru lanjut ke pengecekan radius setelah user menekan Lanjutkan. Mencegah kasus "absen di Tajur tercatat di Kantor Pusat karena kebetulan lebih dekat" tanpa disadari.
2. **Halaman Tinjau Absensi (`/absen/tinjau`) ikut dibangun**, di luar 6 poin awal -- tanpa ini `izinkan_dengan_tanda` tidak ada gunanya (tanda 🟡 masuk database, tidak ada yang pernah melihatnya). Daftar presensi 🟡 yang `keputusan_hrd is null`, tombol Lihat Foto (signed URL, pola sama `LaporanBacaSaja.tsx`), jarak+akurasi GPS, catatan, tombol Terima/Tolak lewat RPC `putuskan_absensi()`.
3. **Draft lokal kalau kirim gagal SETELAH foto diambil.** `lib/absenDraftLokal.ts` -- foto (base64) + koordinat + status disimpan ke `localStorage` (di-scope per `userId`, sama kehati-hatian dengan catatan cache service worker di `app/sw.ts`) begitu unggah/insert gagal. Layar "Belum terkirim -- coba lagi" muncul otomatis saat halaman dibuka lagi (bukan cuma saat gagal langsung), retry TIDAK mengulang GPS/kamera dari awal.
4. **Layar persetujuan privasi SENGAJA BELUM dibangun** -- lihat "Utang WAJIB sebelum go-live" di bawah, dicatat sebagai penghalang, bukan diam-diam dilewati.

**Jam kerja PER ORANG** (poin ke-5, di luar 4 di atas) -- `penugasan_absen.jam_masuk`/`jam_pulang` (nullable, fallback ke `policy.jam_masuk`/`jam_pulang` global kalau kosong), diatur lewat RPC `atur_jam_kerja()` di tab Admin "Titik Absen" baru -- BUKAN update tabel langsung (menghindari jebakan `with check(true)` yang sudah didokumentasikan di `04-CATATAN-TEKNIS.md` §3.2 soal `report_nudge`: RLS tidak bisa membatasi KOLOM, cuma BARIS).

**"role hrd/kadiv HRD" (istilah user)** -- role `kadiv` generik dipegang banyak kepala divisi (CS, Perizinan, IT, Teknik, DTI), TIDAK CUKUP dicek dari role saja. `is_hrd_kadiv()` (SQL, security definer) menggabungkan `role='kadiv' AND profile.divisi='HRD'` -- satu fungsi, dipakai di RLS `absensi_select`, kedua RPC (`putuskan_absensi`/`atur_jam_kerja`), dan nav (`lib/navUtama.ts`, tab "Tinjau Absensi") supaya logikanya SATU tempat.

**Skema (migrasi `0022_presensi.sql`)** -- persis §3.2 dokumen (`lokasi_absen`, `penugasan_absen`, `absensi`) PLUS dua penyesuaian yang perlu, bukan menyimpang diam-diam:
- `penugasan_absen.jam_masuk`/`jam_pulang` (kolom baru, tidak ada di skema asli dokumen) -- untuk poin jam-per-orang di atas.
- `absen_keputusan` enum (`diterima`/`ditolak`) + kolom `absensi.keputusan_hrd` (baru) -- skema asli dokumen sudah punya `disetujui_oleh`/`catatan` tapi TIDAK PUNYA tempat menyimpan HASIL keputusan Terima/Tolak sendiri (itu ditambahkan setelah user minta layar Tinjau, poin 2 di atas -- di titik penulisan skema §3.2 awal, layar review belum diminta).
- Bucket storage baru `absensi` (privat, limit 10MB, mime dikunci `image/jpeg`) -- path `{user_id}/...`, BUKAN `{report_id}/...` seperti pola `bukti` (Task 11): baris `absensi` belum ada saat foto diunggah (`foto_path` wajib diisi SAAT insert), jadi tidak ada id baris untuk folder.
- RLS: baris sendiri SELALU; `ceo`/`pusat` SELALU (pola sama akses umum mereka ke `report`, presensi bukan data serahasia `accounting`); `is_hrd_kadiv()` juga. **TIDAK ADA policy update/delete sama sekali** untuk `absensi` dari klien biasa -- bukti kehadiran, sekali tercatat tidak boleh diubah pengirimnya sendiri; peninjauan HRD lewat `putuskan_absensi()` (security definer, guard peran manual, pola sama `tagih_laporan()` Task 18), bukan UPDATE biasa.

**Trust model presensi -- keputusan sadar, bukan kelalaian:** `jarak_meter`/`status`/`akurasi_meter` DIHITUNG DI KLIEN (browser), disimpan apa adanya lewat insert biasa (RLS cuma cek `user_id = auth.uid()`), BUKAN dihitung ulang server-side. Ini KONSISTEN dengan pola `apakahTerlambat()` yang sudah ada (status TERLAMBAT laporan harian juga dihitung klien) -- dan sejalan PERSIS dengan §3.1 dokumen sendiri: "presensi ber-radius adalah penghalang, bukan bukti" -- GPS browser bisa dipalsukan siapa pun yang niat, foto wajah (yang TIDAK bisa dipalsukan semudah itu) adalah pengaman sesungguhnya. Membangun verifikasi jarak sisi server untuk radius yang dokumennya sendiri bilang "bukan anti-curang" adalah over-engineering yang tidak diminta.

**Halaman Absen (`app/absen/page.tsx`)** -- kamera pakai `getUserMedia({facingMode:'user'})` sungguhan (`components/CameraCapture.tsx`), BUKAN `<input type=file capture>` seperti `LampiranInput.tsx` (Task 11) -- presensi butuh JAMINAN kamera depan (selfie) untuk verifikasi wajah, `capture` pada input file tidak konsisten memaksakan itu lintas browser. `lib/gambar.ts`'s `kompresGambar()` diperlebar tipenya dari `File` ke `Blob` (perubahan aman, `File` adalah `Blob`) supaya bisa dipakai langsung untuk hasil `canvas.toBlob()` dari kamera, bukan cuma dari `<input>`.

**Nav** -- tab "Absen" (peran:null, semua yang login) ditambahkan ke `lib/navUtama.ts` dengan prioritas TINGGI tapi SENGAJA DI BAWAH papan/keputusan/terpusat (bukan paling atas) -- kalau ditaruh paling atas, itu akan menggeser Terpusat keluar dari 3 slot CEO/Pusat yang BARU SAJA teruji sungguhan di HP (Checkpoint 4, 29 Agustus 2026). Efek sampingnya, diverifikasi lewat `scripts/uji-nav-mobile.mjs` (diperbarui, bukan cuma ditambah): untuk Dadang (pic_lokasi+karyawan, 1 tab Lapor dinamis), `absen` sekarang MENANG lawan `riwayat` -- "Laporan Saya" pindah ke halaman Akun (tetap ada, bukan hilang), digantikan Absen di slot nav bawah. Ini trade-off sadar (presensi dipakai 2x/hari, riwayat lebih jarang), bukan regresi tak disadari.

**Diverifikasi:**
- `scripts/uji-absen.mjs` (fungsi murni -- haversine, urutan titik terdekat, keterlambatan menit -- 13 titik, semua LOLOS).
- `scripts/uji-presensi-rls.mjs` (RLS penuh: `lokasi_absen`/`absensi`/`is_hrd_kadiv()`/`putuskan_absensi()`/`atur_jam_kerja()`, 12 titik, penyamaran RLS + BEGIN/ROLLBACK -- termasuk MEMINJAM SEMENTARA akun uji Fauzy/Kasam untuk menguji kadiv+HRD vs kadiv+divisi-lain, karena `profile.id` punya foreign key ke `auth.users` jadi tidak bisa insert profil palsu, semua LOLOS).
- `scripts/uji-storage-absensi.mjs` (RLS `storage.objects` bucket `absensi`, 3 titik, semua LOLOS).
- `scripts/uji-nav-mobile.mjs` (diperbarui lengkap untuk tab absen/absen-tinjau, 15 titik, semua LOLOS -- termasuk memastikan CEO/Pusat/Sabrina TIDAK kehilangan Papan/Keputusan/Terpusat).
- `tsc --noEmit`/`npm run lint`/`npm run build` bersih (0 error, 1 warning lama tidak terkait).

**Sengaja TIDAK dibangun di tahap ini** (dicatat, bukan lupa): entri `manual_hrd` (`absen_status` enum, ada di skema asli dokumen) -- HRD membuat entri presensi manual untuk yang GPS-nya gagal total bukan bagian dari yang diminta, belum ada jalur UI yang menulisnya. `absen_wajib_foto` policy secara TEKNIS bisa `false`, tapi `foto_path` di skema `absensi` (dari dokumen sendiri) `not null` -- kontradiksi kecil, diselesaikan dengan mempercayai NOT NULL yang lebih kuat (foto SELALU wajib di kode, apa pun nilai policy-nya) daripada membangun jalur lewati-kamera yang akan gagal insert di database. Laporan HRD bagian 1 terisi otomatis dari presensi, dan pengecualian cuti/sakit/izin dari `hari_bolong` -- direncanakan supaya skema TIDAK perlu bongkar ulang (kolom `tanggal`/`user_id`/`status` di `absensi` sudah cukup untuk agregasi itu nanti), tapi belum dibangun, sesuai instruksi.

## Utang WAJIB sebelum presensi dipakai sungguhan

- 🔴 **Layar persetujuan privasi (§3.1 dokumen) belum dibangun.** UU Perlindungan Data Pribadi mengharuskan karyawan tahu data lokasi+foto apa yang direkam SEBELUM perekamannya mulai -- pengumuman WhatsApp TIDAK CUKUP sebagai bukti persetujuan (alasan eksplisit user, bukan formalitas). Perlu satu layar yang disetujui SEKALI seumur akun, tercatat tanggalnya. **Sampai ini ada, jangan nyalakan presensi untuk 36 orang sungguhan** -- mekanismenya sudah jalan (7 akun uji boleh dipakai untuk uji teknis), tapi pemakaian produksi menunggu ini.
- **Koordinat titik absen -- 5 dari 6 sudah masuk (30 Agustus 2026):** Kantor Pusat, Indokopi (Jatinegara), Indosteak, Tajur, Bekasi -- semuanya dimasukkan lewat SQL langsung (bukan halaman Admin -- Admin-nya sendiri sudah bisa dipakai untuk ini ke depannya), baris CONTOH dinonaktifkan (`aktif=false`, BUKAN dihapus -- gampang dipulihkan kalau ternyata perlu). Tajur & Bekasi ditautkan ke `lokasi.id` yang cocok. **Tinggal DTI yang masih kosong.**
- 🟡 **2 `penugasan_absen` UJI COBA dibuat sengaja (30 Agustus 2026) -- BUKAN penugasan sungguhan, jangan dianggap data produksi final:** Dadang -> Kantor Pusat, Putri -> Kantor Pusat (titik sama, radius 200m, `izinkan_dengan_tanda`) -- semata supaya user bisa menekan tombol bundar Absen sungguhan dari HP sebelum DTI & penugasan asli lengkap. **Penugasan sungguhan (siapa absen di titik mana yang benar) menyusul setelah DTI masuk** -- dua baris uji coba ini kemungkinan perlu dihapus/diganti nanti lewat tab Admin "Titik Absen", bukan otomatis jadi penugasan permanen.

---

## Migrasi stack: Vite → Next.js 15/16 (21 Agustus 2026)

Keputusan diambil ulang oleh user, didokumentasikan di `BLUEPRINT.md` §2. Proyek di-scaffold ulang total dengan `create-next-app@latest --ts --app --tailwind --eslint --src-dir=false` (perintah persis dari `docs/MULAI-DI-SINI.md`).

**Versi Next.js: 16.3.2, bukan 15.** `create-next-app@latest` memasang versi terbaru (16), bukan 15 seperti tertulis di BLUEPRINT §2. Saya berhenti dan menanyakan ini secara eksplisit (scaffold Next.js sendiri men-generate `AGENTS.md` berisi peringatan "This is NOT the Next.js you know... breaking changes... may differ from your training data") — user memilih **lanjut dengan Next.js 16**. Konsekuensinya:
- `LayoutProps<'/'>` di `app/layout.tsx` adalah tipe baru yang butuh `npx next typegen` (atau `next dev`/`next build`) dijalankan dulu sebelum `tsc` bersih — bukan tersedia begitu saja.
- ESLint bawaan (`eslint-plugin-react-hooks` versi baru) punya aturan `react-hooks/refs` yang salah-tangkap (false positive) pola `useController` react-hook-form — lihat catatan di baris Task 07/08 di bawah.
- `next lint` sudah dihapus di v16; script `lint` di `package.json` memakai `eslint` langsung (sudah begitu dari scaffold, bukan saya ubah).
- Turbopack aktif default untuk `dev` dan `build` (tidak perlu flag `--turbopack` lagi).
- **BLUEPRINT.md dan CLAUDE.md belum diperbarui menyebut versi 16** — masih tertulis "Next.js 15". Ini utang dokumentasi, bukan kesalahan kode. *(Belum saya perbaiki sendiri karena bukan bagian dari 5 langkah yang diminta — tandai untuk diperbaiki.)*

**Duplikat `CLAUDE.md` ditemukan dan diberesi.** Proses update dokumen user menulis versi baru ke `docs/CLAUDE.md`, sementara versi lama (Vite) masih ada di root — dua sumber kebenaran yang saling bertentangan (root eksplisit bilang "Jangan pasang: Next.js"). Root `CLAUDE.md` ditimpa dengan isi baru (Next.js), `docs/CLAUDE.md` dihapus. Juga memperbaiki satu typo warisan di dalamnya: aturan #6 masih menyebut `src/forms/` padahal struktur baru §5 taruh `forms/` di root (tanpa `src/`, sesuai `--src-dir=false`).

**Task 01, 07, 08, 09 dipindah 1:1** dari `src/{components,forms,lib,styles}` (Vite) ke `{components,forms,lib}/` + `app/tokens.css` (Next.js root, `--src-dir=false`). Karena struktur folder relatif sama (components/, forms/, lib/ tetap sibling), **tidak ada satu pun import path yang berubah** — hanya penambahan `'use client'` di `FormRenderer.tsx` dan seluruh `components/fields/*.tsx` (BLUEPRINT §3.4). `forms/*.ts` dan `lib/rupiah.ts` tidak diberi `'use client'` (bukan komponen, tidak pakai hook).

Yang **tidak bisa** dipindah 1:1 (perbedaan mendasar Vite vs Next.js App Router, bukan pilihan bebas):
- Font Google (dulu `<link>` di `index.html`) → sekarang `next/font/google` di `app/layout.tsx`. Token `--display`/`--body`/`--mono` di `tokens.css` diubah dari nama font literal menjadi `var(--font-display)` dkk. supaya nyambung ke variabel yang di-generate `next/font`. Komponen yang memakai token ini (`FormRenderer`, semua field) **tidak disentuh** — mereka hanya mengonsumsi `var(--display)`, jadi tidak tahu dan tidak perlu tahu perubahan di baliknya.
- `index.html` (lang, title) → `app/layout.tsx` pakai `<html lang="id">` + `export const metadata`.
- `src/App.tsx` (placeholder Task 01) → `app/page.tsx`, isi sama (judul + subjudul pakai token).

**Task 02 (klien Supabase, AuthProvider, halaman Masuk) SENGAJA TIDAK dipindah** — instruksi migrasi eksplisit hanya menyebut 01/07/08/09. File lama (`src/lib/supabase.ts`, `src/auth/AuthProvider.tsx`, `src/pages/Masuk.tsx`, `src/pages/Beranda.tsx`, `src/App.tsx` routing) ikut terhapus bersama `src/` lama dan **tidak ada lagi**. Task 02 perlu dikerjakan ulang dari nol nanti: BLUEPRINT §2/§5 sekarang mensyaratkan `@supabase/ssr` dengan dua klien terpisah (`lib/supabase/client.ts` browser, `lib/supabase/server.ts` cookie-based) menggantikan satu klien `@supabase/supabase-js` + localStorage yang lama, dan routing lewat App Router menggantikan React Router. Status Task 02 dikembalikan ke ⬜ di tabel bawah.

**Verifikasi ulang setelah pindah** (bukan cuma "berhasil di-copy", logika dijalankan ulang):
- `buildZodSchema()` dites lagi dengan 3 kasus yang sama persis seperti sebelum migrasi (centang tanpa bukti → ditolak dengan pesan sama; dengan bukti → lolos; tidak dicentang → lolos). Hasil identik.
- SSR (`renderToStaticMarkup`) FormRenderer dengan ke-10 tipe field + `nilaiAwal`, dari lokasi baru. Uang terformat, lampiran nilaiAwal muncul, tabel 1 baris sesuai nilaiAwal, ke-10 field render — semua OK, sama seperti hasil sebelum migrasi.
- `npx tsc --noEmit`, `npm run build`, `npm run lint` ketiganya bersih pada kondisi akhir.
- `npm run dev` dijalankan sungguhan, `curl` ke `/` → HTTP 200, `<title>Pusat Kontrol Koperumnas Group</title>` dan `lang="id"` terkonfirmasi ada di HTML yang sungguh-sungguh dikirim server.

**`.env.local`/`.env.local.example` diganti nama variabel** `VITE_SUPABASE_*` → `NEXT_PUBLIC_SUPABASE_*` sesuai instruksi.

**Efek samping ditemukan & diperbaiki selama migrasi** (tidak diminta eksplisit, tapi blocking/salah kalau dibiarkan):
- `.gitignore` sempat tertimpa total oleh punya scaffold Next.js (termasuk pola `.env*` yang akan meng-ignore `.env.local.example` yang justru harus ter-track) — digabung ulang manual.
- Warning `turbopack.root` ambigu karena ada `package-lock.json` tak terkait di folder induk (di luar repo ini) — diberesi dengan `turbopack.root` eksplisit di `next.config.ts`, bukan menyentuh file di luar repo.

| # | Task | Status | Diverifikasi | Catatan |
|---|---|---|---|---|
| 01 | Fondasi proyek & token desain | ✅ | Dipindah ke Next.js: `npx tsc --noEmit`, `npm run build`, `npm run lint` bersih; `npm run dev` + `curl` ke `/` → HTTP 200, `<title>` dan `lang="id"` terkonfirmasi di HTML sungguhan. | **Di-scaffold ulang total dari Vite ke Next.js 16** (21 Agustus 2026) — lihat bagian "Migrasi stack" di atas. `tokens.css` pindah ke `app/tokens.css`, font Google lewat `next/font/google` (bukan `<link>`), `index.html` digantikan `app/layout.tsx`. |
| 02 | Klien Supabase & sesi login | ✅ (sebagian terverifikasi nyata, sisanya menunggu M9 + migrasi) | `tsc`/`build`/`lint` bersih. **Diuji sungguhan, bukan diasumsikan**: (1) `npm run dev` + `curl` ke `/` tanpa sesi → **HTTP 307 redirect ke `/masuk`**, dan `curl` ke `/masuk` langsung → **HTTP 200, tidak redirect** — proxy benar-benar jalan (log server menunjukkan `proxy.ts: 14ms`), bukan disimpulkan dari kode. (2) `pesanErrorMasuk()` diuji ulang 2 kasus — selalu Bahasa Indonesia. (3) Build menunjukkan baris `ƒ Proxy (Middleware)`, konfirmasi Next.js mengenali `proxy.ts` dengan benar. | **Kode ditulis mengikuti persis** panduan resmi Supabase (`supabase.com/docs/guides/getting-started/ai-prompts/nextjs-supabase-auth`, dibuka sebelum menulis kode) DAN tipe `@supabase/ssr@0.12.4` yang benar-benar terpasang di `node_modules` (bukan disalin buta dari panduan) — `setAll` di `proxy.ts` memakai parameter `headers` kedua sesuai `SetAllCookies` di `node_modules/@supabase/ssr/dist/main/types.d.ts` (mencegah cookie sesi ke-cache CDN/reverse proxy), `createBrowserClient` tidak diberi opsi `cookies` sama sekali (opsional, ditangani otomatis oleh library — dikonfirmasi dari `createBrowserClient.d.ts`). **`middleware.ts` di task board sudah usang** — dicek ke docs Next.js 16 resmi (`node_modules/next/dist/docs/.../16-proxy.md` + upgrade guide `version-16.md`), dan panduan Supabase yang dibuka juga sudah memakai `proxy`/`proxy.ts`. File dibuat sebagai `proxy.ts` dengan fungsi bernama `proxy`, bukan `middleware.ts`/`middleware`. **Belum bisa diverifikasi** (butuh migrasi Task 03/04 + M9 dulu): login dengan akun CEO sungguhan, `roles` terisi `['ceo','karyawan']`, refresh tidak logout, `signOut` kembali ke `/masuk` — masuk daftar CHECKPOINT 4. `react-hooks/refs` dibatasi ke `components/fields/**/*.tsx` saja lewat `files` di `eslint.config.mjs` (bukan mati global lagi), atas instruksi user. |
| 03 | Migrasi database — tabel inti | ✅ | **Dijalankan dan diverifikasi langsung lewat koneksi Postgres** (`scripts/db.mjs`, `SUPABASE_DB_URL`), bukan cuma dibaca dari layar user: 11 tabel ada persis sesuai nama (`information_schema.tables`), `report_uniq` + `assignment_uniq` ada (`pg_indexes`), `pte_daily.lengkap` adalah generated column `ALWAYS` (`information_schema.columns`), trigger `report_updated` + `pte_daily_updated` ada (`information_schema.triggers`). Ternyata migrasi ini sudah pernah berhasil dijalankan user di sesi sebelumnya (klaim "database kosong" user salah — dibuktikan lewat screenshot Table Editor sebelum sesi verifikasi langsung ini), tidak perlu di-run ulang. | — |
| 04 | Row Level Security | ✅ **CHECKPOINT 2 LOLOS** | **Dijalankan lewat `npm run db -- supabase/migrations/0002_rls.sql`** (exit code 0), diverifikasi: `rls_aktif = true` di 11 tabel, 24 policy persis sesuai spesifikasi (`report_nudge` memang tidak ada), 4 fungsi/RPC ada, 2 policy storage ada. **Lalu matriks uji CHECKPOINT 2 dijalankan sungguhan** lewat `scripts/uji-checkpoint2.mjs` (penyamaran `set role authenticated` + `request.jwt.claims`, sanity check `auth.uid()` di tiap blok, GUC `uji.*`, EXCEPTION tunggal, reset role sebelum anon) — **13 dari 14 baris matriks LOLOS** dengan output mentah dicatat penuh di chat log sesi ini: kontrol positif (accounting lihat punya sendiri) = 1 → GRANT sehat; `pusat`/`kontrol_marketing` 0 baris di laporan accounting; `ceo` bisa lihat; insert atas nama orang lain ditolak; update laporan orang lain/tanggal lampau ditolak; keputusan cuma bisa diputuskan `ceo`; laporan dobel ditolak unique index; shift pagi+siang tidak bentrok; anon 0 baris dengan `auth.uid()` NULL. Seluruh 13 `auth.uid()` berisi UUID benar & konsisten per persona. Data dummy di-`ROLLBACK`, tidak ada yang tersimpan. | #3 (`v_keuangan_rekap`, Task 20) dan #10 (signed URL storage, Task 11) tetap dilewati dengan alasan eksplisit — sudah tercatat di "Utang uji" di bawah. |
| 05 | Seed `policy`, lokasi, outlet | ✅ | `supabase/migrations/0003_seed.sql` **dijalankan sungguhan** (`npm run db`) lalu diverifikasi query langsung: `policy` 13 baris (≥9 syarat), `lokasi` = Tajur/Bekasi/DTI (**bukan** Ciwidey/Pangalengan/Soreang), `outlet` = Indosteak/Indokopi, `assignment` 5 baris cocok persis DATA-KARYAWAN.md §5 (Dadang→pic_lokasi/Tajur, Kasam→security/DTI/pagi, Sabrina→pusat+hrd, Accounting→accounting). `lib/api/policy.ts` (`usePolicy()`, TanStack Query) dibuat; `components/Providers.tsx` (`QueryClientProvider` + `AuthProvider`) baru dipasang di `app/layout.tsx` — belum ada sebelumnya sejak pivot Next.js. `tsc`/`build`/`lint` bersih. `grep` angka bisnis (500000, 300000, 20) di seluruh `*.tsx` — nol hasil. | Path task board masih menyebut `src/api/policy.ts` (stale, sama seperti `middleware.ts` sebelumnya) — dibuat di `lib/api/policy.ts` sesuai struktur §5 yang benar. **Belum diverifikasi**: `usePolicy()` benar-benar mengembalikan data di browser sungguhan — butuh sesi login (RLS `policy_select` mensyaratkan `auth.uid() is not null`), masuk CHECKPOINT 4. Rukost sengaja belum jadi lokasi/outlet (masih pertanyaan terbuka ke CEO). |
| 06 | Kerangka layout, routing, penjaga peran | ✅ | `tsc`/`build`/`lint` bersih. **Diverifikasi nyata**: (1) `curl` ke ketujuh rute baru (`/`, `/papan`, `/keputusan`, `/marketing`, `/terpusat`, `/admin`, `/lapor/personal_marketing`) tanpa sesi → semua **307 ke `/masuk`** (proxy melindungi rute baru juga, bukan cuma `/`); `/masuk` sendiri tetap 200. (2) Logika filter tab diuji langsung (bukan dibaca kodenya) dengan 4 kombinasi peran — `karyawan` saja → persis `['Beranda','Lapor']` (cocok "Selesai kalau"); `ceo+karyawan` → 5 tab; `pusat+kadiv+karyawan` → 4 tab; `kontrol_marketing` → 3 tab. `npm run build` menunjukkan semua rute ter-generate benar. | **Batas Server/Client dijaga eksplisit** sesuai peringatanmu: `app/layout.tsx` tetap Server Component murni (tidak ada `'use client'`), cuma me-render `<KopHalaman/>` (client) dan `<Providers>` (client) sebagai children — bukan ikut jadi client sendiri. **Keputusan desain (dokumen belum sedetail ini per-rute):** `/keputusan` sekarang cuma `peran="ceo"` sesuai kalimat eksplisit Task 06 ("non-CEO → tidak punya akses"), padahal Task 19 nanti bilang `pusat` boleh MELIHAT (tanpa tombol putuskan) — perlu diperluas jadi `['ceo','pusat']` saat Task 19 dikerjakan, dicatat di sini supaya tidak lupa. `/marketing` cuma `kontrol_marketing` untuk sekarang, Task 22 nanti minta `ceo`+`pusat` juga ikut bisa buka. `/terpusat` cuma `pusat`. `lib/tanggal.ts` **belum pernah dibuat sejak awal proyek** — dibuat sekarang persis sesuai `03-CALC-SPEC.md` §6, plus satu fungsi tambahan `tanggalIndonesiaWIB()` (format "Jumat, 21 Agustus 2026") yang tidak ada di spec tapi dibutuhkan header — bukan pengganti fungsi yang sudah ada, cuma tambahan. Tanggal di header pakai `suppressHydrationWarning` + hitung langsung saat render (bukan `useEffect`+`setState`) — ESLint (`react-hooks/set-state-in-effect`, aturan baru React Compiler-era) menangkap pola lama sebagai potensi cascading render; ini bukan false positive seperti `react-hooks/refs` kemarin, jadi diperbaiki polanya, bukan aturannya yang dimatikan. | **Belum diverifikasi** (butuh browser + sesi login sungguhan): halaman "Tidak punya akses" benar-benar tampil saat user LOGIN dengan peran salah membuka `/keputusan` (baru diuji jalur ANONIM via proxy, belum jalur "login tapi peran salah" via `Terlindungi`); tidak ada elemen terpotong di lebar 360px (baru ditinjau dari kode — `flex-wrap` di baris atas header, `overflow-x-auto` di strip tab sengaja dikontain supaya scroll horizontal kalau ada cuma di situ, bukan di halaman). Masuk CHECKPOINT 4. **Celah ditemukan & diperbaiki 23 Agustus 2026 (sebelum Batch D):** tab "Lapor" cuma hardcode ke `personal_marketing` untuk peran `karyawan` -- user `pic_lokasi` (Dadang, TANPA peran `karyawan`) login dan sama sekali tidak punya jalan ke `/lapor/pic_lokasi` lewat menu, padahal rutenya sendiri sudah bisa diakses sejak Task 13. Diperbaiki dengan `lib/navLapor.ts` (`tabLaporDinamis()`, fungsi murni, diuji tanpa React/DOM) -- tab dinamis dibangun dari `assignment` (SATU SUMBER yang akan dipakai lagi Papan Kontrol, Task 18), bukan daftar tetap. Menambah baris assignment (nanti lewat admin, Task 23) langsung memunculkan tab begitu sesi dimuat ulang, tanpa deploy kode. `personal_marketing` sengaja TIDAK lewat mekanisme ini (bukan assignment per-orang, kewajiban semua `karyawan` — tetap tab tetap berbasis peran). Diuji 6 skenario murni: PIC 2 lokasi → 1 tab "Lapor Lokasi" (bukan 2); satpam 2 shift → 1 tab "Lapor Keamanan"; karyawan tanpa assignment → 0 tab dinamis; assignment ke form_key yang BELUM ada schema-nya (`pusat`, punya Sabrina) → dilewati, tidak ada tab mati; `personal_marketing` di data assignment (seharusnya tidak pernah terjadi, tapi dijaga) tetap tersaring. |
| 07 | Tipe `FormSchema` + `FormRenderer` | ✅ | Dipindah ke `forms/types.ts`, `forms/index.ts`, `components/FormRenderer.tsx` (+ `'use client'`). `tsc`/`build`/`lint` bersih. SSR ulang setelah pindah — 10 field render, sama seperti sebelumnya (lihat "Migrasi stack"). | **Masih berlaku dari sebelum migrasi**: perilaku submit → `console.log` interaktif di browser sungguhan belum diuji live (tidak ada tool browser/DOM headless di sesi ini) — lihat CHECKPOINT 4. |
| 08 | Komponen field | ✅ | Dipindah ke `components/fields/*.tsx`, semua + `'use client'`. `tsc`/`build`/`lint` bersih (setelah menonaktifkan false-positive `react-hooks/refs`, lihat "Migrasi stack"). SSR ulang: Uang terformat, StatusWarna/YaTidak/Lampiran/Tabel baca `nilaiAwal` dengan benar — sama seperti sebelum migrasi. | **Masih berlaku dari sebelum migrasi**: readback field tak-terkontrol di browser sungguhan dan uji viewport 360px belum diamati langsung — lihat CHECKPOINT 4. |
| 09 | Aturan bukti wajib | ✅ | Dipindah ke `forms/validasi.ts`. `buildZodSchema().safeParse()` dites ulang 3 kasus di lokasi baru — hasil identik dengan sebelum migrasi, termasuk teks pesan `"Live dicentang tapi belum ada bukti"`. | **Masih berlaku dari sebelum migrasi**: highlight merah muda & `scrollIntoView` belum diamati di browser sungguhan — lihat CHECKPOINT 4. Keputusan desain `buktiWajib` (tombol menempel di baris field, path `_bukti.<fieldKey>`) tidak berubah, sudah dikonfirmasi user sebelumnya. |
| 10 | Simpan draft & kirim laporan | ✅ | `tsc`/`build`/`lint` bersih. **Diuji nyata terhadap DB, bukan cuma dibaca**: (1) `batasJamKirim()`/`apakahTerlambat()` diuji langsung dengan bentuk `policy` sungguhan — default 18:00, per-form (`manager_resto`→23:00), per-shift (`security`+pagi→14:30, +malam→07:30), fallback kalau shift kosong. (2) Skenario "kirim dua kali" disimulasikan **sebagai `toyib` sungguhan** (penyamaran `set role authenticated` sama seperti Checkpoint 2) langsung ke Postgres: insert draft 1x, update (kirim) 2x berturut-turut → **jumlah baris tetap 1**, `data`/`warna` ikut ter-update ke nilai kirim terakhir. Data dummy di-ROLLBACK. | **Keputusan desain penting:** `useSimpanDraft`/`useKirimReport` TIDAK memakai Supabase `.upsert()` dengan `onConflict` — index unik `report_uniq` dibangun dari ekspresi `coalesce(lokasi_id, outlet_id, ...)`, bukan kolom polos, dan PostgREST (dipakai supabase-js) cuma bisa menarget `ON CONFLICT` lewat daftar nama kolom biasa. Sebagai gantinya: SELECT dulu (via `useReportHariIni`) untuk tahu `reportId` ada atau tidak, INSERT cuma sekali kalau belum ada, sesudahnya SELALU UPDATE by id. Ini juga yang membuat "kirim dua kali = satu baris" otomatis benar secara struktur, bukan penanganan kasus khusus. `submitted_at` pakai `new Date().toISOString()` — ini **bukan** pelanggaran aturan "jangan toISOString untuk tanggal": aturan itu soal `.slice(0,10)` mengambil tanggal kalender (bisa meleset hari di WIB), sedangkan `submitted_at` adalah `timestamptz` presisi penuh, representasi ISO-UTC untuknya benar dan standar. `lib/debounce.ts` dibuat sebagai utilitas generik untuk "draft tersimpan tiap 5 detik" — pemasangannya ke form sungguhan (watch react-hook-form → debounce → panggil `useSimpanDraft`) menyusul di Task 12 saat halaman form pertama benar-benar ada untuk diuji end-to-end. | **Belum diverifikasi**: perilaku debounce 5 detik di browser sungguhan (Task 12), dan "isi form → refresh browser → isian masih ada" (butuh halaman form sungguhan + browser) — masuk CHECKPOINT 4. |
| 11 | Unggah lampiran ke Storage | ✅ (mekanisme; wiring UI di Task 12) | `tsc`/`build`/`lint` bersih. **Diverifikasi end-to-end lewat HTTP sungguhan** (bukan cuma dibaca kodenya): login sungguhan sebagai `toyib` (password `123456`) lewat Auth REST API → dapat `access_token` nyata; upload file lewat endpoint & header PERSIS yang dipakai `unggahDenganProgres()` → **200 berhasil**; akses path mentah tanpa signed URL (baik benar-benar publik maupun cuma pakai anon key tanpa token) → **400 ditolak keduanya**; `createSignedUrl` → dapat URL, diakses → **200, isi file cocok persis**. Logika penolakan video diuji langsung dengan 4 ukuran (49/50/51/120 MB) — persis di batas 50MB diterima, di atasnya ditolak dengan pesan menyebut ukuran filenya, sesuai kalimat "Selesai kalau". Semua data uji (report dummy + file storage) dihapus bersih setelahnya, dicek ulang `count(*)=0`. | **Bucket `bukti` TIDAK ADA sama sekali** saat task ini dimulai — M3 di `00-SETUP-MANUAL.md` belum dikerjakan siapa pun. Saya buat sendiri lewat SQL langsung (`insert into storage.buckets`) persis parameter M3: privat, limit 52428800 bytes (50MB) — bukan menebak, angkanya sama persis dengan dokumen. Diberitahukan saat itu juga, bukan diam-diam. **Keputusan desain:** upload pakai `XMLHttpRequest` langsung ke endpoint REST Storage (`POST {url}/storage/v1/object/bukti/{path}`), BUKAN `supabase.storage.upload()` — dicek ke `node_modules/@supabase/storage-js/dist/index.d.mts`, SDK resmi 2.x tidak punya callback progres byte-per-byte (pakai `fetch` di baliknya, bukan XHR), jadi tidak mungkin memenuhi "tampilkan bilah progres per file" tanpa ini. `captured_at` diisi dari `file.lastModified` (satu-satunya metadata waktu yang tersedia tanpa parser EXIF — tidak ada library EXIF yang diizinkan BLUEPRINT §2) — proksi wajar, bukan tanggal pengambilan foto sungguhan kalau beda dari waktu modifikasi file. Kualitas JPEG `0.8` ditulis sebagai konstanta teknis (`KUALITAS_JPEG`), BUKAN dari `policy` — beda dari `gambar_max_px` yang memang ada sebagai kunci `policy` dan diteruskan sebagai parameter; kualitas kompresi bukan "aturan bisnis" dalam pengertian CLAUDE.md aturan #4. **Wiring ke `LampiranInput.tsx`/`FormRenderer` sengaja ditunda ke Task 12** — perlu `reportId` sungguhan (baru ada setelah draft dibuat, Task 10) dan nilai `policy` (Task 05), keduanya baru punya konteks nyata saat halaman `personal_marketing` sungguhan dibangun. | **Belum diverifikasi**: `kompresGambar()` (canvas/`createImageBitmap`) — API browser murni, tidak ada DOM di Node, tidak bisa dites di sesi ini sama sekali. Foto 4MB beneran jadi <500KB, dan bilah progres per-file di UI — keduanya juga CHECKPOINT 4. |
| 12 | Form `personal_marketing` + sinkron `pte_daily` | ✅ **LENGKAP 9 blok** | `tsc`/`build` bersih, `lint` 0 error (1 warning informasional). `grep` angka bisnis nol hasil (satu ditemukan & diperbaiki: label "Undangan 20 orang" jadi dinamis `{invitTarget}`). **Riwayat dua tahap**: tahap 1 (sebelum `REFERENSI-FORMAT-LAPORAN.md` yang benar tersedia) cuma 6 PTE + closing sederhana, diuji 2 skenario (lengkap vs 5/6). **Tahap 2 — form lengkap 9 blok** setelah dokumen benar diberikan: SSR ke-9 blok dikonfirmasi render tanpa crash (butuh `QueryClientProvider` karena `LampiranInput` sekarang pakai `usePolicy()`); `ringkasanPteHariIni()` diuji 4 skenario (lengkap; konten di bawah `pte_konten_minimal`; `live='tidak'` walau ada sisa bukti; semua angka terisi tapi nol bukti sama sekali) — semua benar. End-to-end penuh diuji sebagai `toyib` sungguhan: `live='ya'` (string, bukan boolean) tersinkron benar ke `pte_daily.live=true`; `closing_list` dengan kolom `lokasi` diresolusi ke `lokasi_id` sungguhan ("Tajur" cocok), dan nama lokasi tak dikenal jatuh ke `null` alih-alih menggagalkan seluruh baris. | **Dua bug ditemukan & diperbaiki SEBELUM ditulis ke schema** (bukan setelah): (1) `live` di spesifikasi baru bertipe `ya_tidak` (string 'ya'/'tidak'), bukan `centang` (boolean) — kode lama pakai `Boolean(data.live)`, yang salah karena `Boolean('tidak') === true`. Diperbaiki jadi `data.live === 'ya'` eksplisit. (2) `forms/validasi.ts`'s `terisi()` untuk tipe `ya_tidak` tadinya menganggap "sudah dijawab apa saja" = terisi -- ini SALAH untuk field macam `live` ber-`buktiWajib`: menjawab "Tidak" tidak seharusnya memicu tuntutan bukti. Diperbaiki jadi cuma `nilai === 'ya'` yang dianggap terisi (perbaikan umum, bukan hack khusus `live`). **Fitur baru ditambahkan**: `Field.wajibYa` (forms/types.ts) untuk field `pernyataan` yang harus PERSIS "ya" (bukan cuma "sudah dipilih") sebelum submit boleh jalan -- diuji langsung, pesan error persis `"Pernyataan wajib dicentang"`. `pernyataan_at` + `pernyataan_nama` dibubuhkan ke `data` saat submit. **Keterbatasan yang dicatat, bukan diselesaikan diam-diam**: `live_platform` ("muncul kalau live=ya") ditampilkan SELALU dengan teks bantuan, bukan disembunyikan kondisional -- `FormRenderer` belum punya visibilitas field kondisional, di luar cakupan task ini. Kolom `lokasi` di `closing_list` seharusnya dropdown dari tabel `lokasi` (spesifikasi), tapi `Tabel` (Task 08) belum punya dropdown sungguhan per sel -- diketik bebas lalu dicocokkan by name ke `lokasi.nama` (upaya terbaik, `lokasi_id` jadi `null` kalau tidak cocok, bukan menolak baris). Blok 1/5/8 (identitas, status PTE hari ini, status 3-baris) dirender di `LaporForm.tsx` langsung, BUKAN sebagai field schema -- semuanya "hanya baca/dihitung sistem", schema untuk field INPUT. Blok 3 & 6 sama-sama menampilkan progres undangan/closing "dihitung" -- ditampilkan SEKALI di bagian atas `LaporForm`, dirujuk lewat catatan di kedua blok, bukan diduplikasi (dokumen sendiri bilang jangan bikin dua field untuk `undang_jumlah`; prinsip yang sama saya terapkan ke tampilan progresnya juga). File `docs/REFERENSI-FORMAT-LAPORAN (1).md` ganda ter-upload dua kali (isi salah lalu isi benar) -- keduanya diberesi jadi satu file kanonis. | **Belum diverifikasi**: seluruh alur lewat UI/browser sungguhan (isi form, debounce 5 detik, klik kirim, progres unggah, tampilan Blok 1/5/8 di layar). Semua logika sudah diuji lewat DB/SSR langsung, interaksi manusia-ke-layar-nya belum — CHECKPOINT 4. Lima format asli (`pusat`, `accounting`, `it`, `manager_resto`, `ita`) belum di repo, dibutuhkan Task 14/16/17/21 — tidak menghambat Task 12/13 (dikonfirmasi user). |
| 13 | Form `pic_lokasi` | ✅ | `tsc`/`build` bersih, `lint` 0 error (1 warning informasional lama). `grep` angka bisnis nol hasil di semua file baru/ubah. **SSR**: ke-10 blok render tanpa crash (2 judul mengandung `&` awalnya salah dilaporkan "hilang" oleh skrip uji sendiri gara-gara React meng-escape `&`→`&amp;` di HTML — bukan bug komponen, diperbaiki di skrip ujinya). Kunci kontrak `unit_dibangun`/`unit_finishing`/`unit_selesai`/`unit_belum_mulai` (untuk `v_pembangunan_hari_ini`, Task 20) dikonfirmasi ada persis, tidak ada key field dobel di 60+ field. `buildZodSchema` diuji 3 skenario: kirim tanpa bukti progress → **ditolak** (foto/video wajib SETIAP hari, tidak bergantung field lain); `video_kondisi_lokasi='tidak'` → **tidak** menuntut bukti (bukti di situ kondisional, beda dari progress); `='ya'` tanpa bukti → **ditolak**. **DB sungguhan** (`scripts/uji-task13.mjs`, penyamaran `set role authenticated` sama seperti Checkpoint 2, dibungkus BEGIN/ROLLBACK): (1) Dadang ditugaskan 2 lokasi (Tajur asli + Bekasi sementara) → berhasil kirim **2 baris report terpisah** utk `pic_lokasi` hari yang sama, tidak bentrok `report_uniq`. (2) Centang "Butuh keputusan CEO" + judul terisi → baris `decision` dibuat, `status` default **`menunggu`**. (3) Dadang (bukan `ceo`) coba ubah `status` decision → **ditolak** RLS `dec_decide`. (4) Insert `decision` merujuk laporan **milik orang lain** (dibuat sebagai Toyib) → **ditolak** RLS `dec_insert`. Semua di-ROLLBACK, dikonfirmasi ulang lewat `count(*)` — 0 sisa di `report`/`decision`, penugasan Dadang balik ke 1 (cuma Tajur). | **Field.buktiWajib dipakai dengan CARA BARU**: sebelumnya (Task 12) selalu ditempel ke field yang jawabannya sendiri menentukan apakah bukti dituntut (mis. `live` ya_tidak). Di sini, bukti foto/video "wajib SETIAP HARI" (bukan kondisional) dicapai dengan menempelkannya ke `progress_catatan` yang `wajib:true` — field wajib SELALU "terisi" begitu form valid, jadi `buktiWajib` di situ otomatis berarti "tuntut bukti di setiap pengiriman", tanpa perlu mengubah `forms/validasi.ts` sama sekali. `video_kondisi_lokasi` (bagian 7, "video kondisi lokasi") tetap pakai pola lama (kondisional pada `ya`). **Pemilih lokasi untuk scope `lokasi`**: `LaporForm.tsx` sekarang generik — baca `assignments` dari `useAuth()` (sudah ada sejak awal, belum pernah dipakai), filter berdasar `form_key`; 0 penugasan → pesan "tidak punya penugasan", 1 → otomatis, >1 → tombol pilih dulu sebelum form/query lain jalan. Ditambahkan `ScopeOpsi.aktif` (baru, opsional, default `true`) ke `lib/api/report.ts` supaya `useReportHariIni` bisa **dimatikan** (`enabled: false` TanStack Query) selagi lokasi belum dipilih — mencegah query/insert nyasar ke `lokasi_id=null`. `lib/api/lokasi.ts` (`useDaftarLokasi`) dan `lib/api/decision.ts` (`buatKeputusanDariLaporan`) baru, keduanya generik (bukan cuma dipakai `pic_lokasi` — form ber-scope `lokasi`/`outlet` lain di Task 14-16 bisa pakai pola sama). Bagian "9 · Laporan Personal Marketing" dan "11 · Rekap Lokasi untuk Sabrina" di dokumen asli **sengaja TIDAK jadi field schema** — keduanya cuma me-restate/menghitung ulang data dari blok lain (progres undangan/closing lewat `useProgresBulananSaya()` yang sudah ada dari Task 12, status laporan personal hari ini lewat `useReportHariIni('personal_marketing')`) — cuma 3 hal di bagian 11 yang benar-benar input baru (`masalah_utama`, `keputusan_ceo`+`keputusan_ceo_judul`, `status_lokasi`) yang jadi field. **Tidak disentuh, di luar cakupan task ini**: `components/KopHalaman.tsx` — tab nav "Lapor" masih hardcode ke `/lapor/personal_marketing` untuk role `karyawan` saja; user ber-role `pic_lokasi` (tanpa `karyawan`) tidak akan melihat tab apa pun ke `/lapor/pic_lokasi` di menu (rute-nya sendiri tetap bisa diakses langsung via URL, tidak ada penjaga peran di `/lapor/[formKey]`, sama seperti Task 09-12). Ini kemungkinan wilayah Task 22 (navigasi/menu per-peran) — dicatat di sini supaya tidak lupa, bukan diperbaiki diam-diam sekarang. | Seluruh alur UI sungguhan (pilih lokasi, isi 60+ field, unggah bukti progress, kirim, lihat baris di antrean keputusan) belum diklik di browser — logikanya sudah diuji lewat DB/SSR langsung. Masuk CHECKPOINT 4 (baris ditambahkan di bawah). |
| 14 | Form `it`, `hrd`, `security`, `perizinan` | ✅ **LENGKAP** (`it` diselesaikan 23 Agustus 2026, tiga lainnya sebelumnya) | `hrd`/`security`/`perizinan`: lihat catatan lama di bawah. `it`: `tsc`/`build`/`lint` bersih, `grep` angka bisnis nol hasil. SSR: 14 blok render tanpa crash, tidak ada key dobel, `data_pembayaran_terupdate` cuma satu field (dokumen asli menanyakannya identik dua kali di blok 1 & 3 -- disatukan, bukan dipertahankan dobel), blok `keputusan_ceo` punya 4 kunci standar + 2 tambahan (`tindakan_sudah_dilakukan`, `belum_selesai`, sesuai isi asli blok 15). DB sungguhan (`scripts/uji-it-belum-upload.mjs`, penyamaran RLS, BEGIN/ROLLBACK): Dadang PIC 2 lokasi (Tajur+Bekasi), Tajur kirim laporan+attachment `field_key='progress'`, Bekasi kirim laporan TANPA attachment. Sebagai profil persis Diki (role `kadiv`+`karyawan`, assignment `form_key='it'`) → view mengembalikan **cuma Bekasi**. Sebagai Toyib (tanpa assignment `it`) → 0 baris. | **§7 "PIC lokasi yang belum kirim foto/video" (D2) dihitung dari `attachment`, TIDAK diketik** -- via view security-definer baru `v_pic_lokasi_belum_upload_progress` (migrasi 0012, pola §3.4b, penjaga `boleh_lihat_rekap('it')`) karena IT (role `kadiv`) tidak punya baris `can_see_report()` ke `pic_lokasi`, sama seperti Ronald. **Beda penting dari rekap Pembangunan**: view ini CUMA berisi `lokasi_id`+`lokasi` -- TIDAK ADA nama PIC, karena §3.4b eksplisit melarang "nama orang" lewat view security-definer. Nama PIC diambil TERPISAH lewat `assignment`+`profile` (`lib/api/it.ts`, `usePicLokasiBelumUpload()`) -- kedua tabel itu sudah *broadly readable* untuk siapa pun yang login (`0002_rls.sql`: "profil: semua yang login boleh melihat nama rekan"), jadi tidak butuh security definer sama sekali untuk bagian nama. Ini contoh konkret instruksi user "jangan pakai security definer di mana pun yang tidak perlu" -- cuma bagian yang benar-benar terkunci RLS yang lewat view khusus. |
| 15 | Form `pembangunan`, `dti`, `kendaraan`, `cs`, `ga` | ✅ | Kelima: `tsc`/`build` bersih, `lint` 0 error, `grep` angka bisnis nol hasil di seluruh 8 file form baru + `blok-bersama.ts` + `LaporForm.tsx` + `KopHalaman.tsx`. SSR: ke-9 schema Batch D (8 form baru + `pic_lokasi` yang ikut diuji ulang setelah direfaktor) render tanpa crash, semua judul blok ada, tidak ada key field dobel DALAM satu schema, blok `keputusan_ceo` (kalau ada) selalu punya 4 kunci standar (`masalah_utama`/`keputusan_ceo`/`keputusan_ceo_judul`/`status_hari_ini`), `buildZodSchema` tidak error -- 1 skrip uji generik (`.tmp-test`, dihapus setelah lolos) menjalankan pengecekan yang sama utk kesembilan sekaligus, bukan diketik ulang per form. DB sungguhan (`scripts/uji-batch-d.mjs`, penyamaran RLS, BEGIN/ROLLBACK): kirim laporan `hrd` (BUKAN `pic_lokasi`) dengan `keputusan_ceo=true`+judul terisi → baris `decision` tetap terbuat, status `menunggu` -- membuktikan pemicu keputusan CEO sungguh generik di `LaporForm.tsx`, bukan diperiksa satu-satu per `formKey`. | **Ekstraksi komponen bersama** (instruksi eksplisit user, "jangan disalin ulang delapan kali"): `forms/blok-bersama.ts` (`blokKeputusanCeo()`) dipakai ke-9 form (termasuk `pic_lokasi` yang diretrofit -- field `status_lokasi` di-rename `status_hari_ini` supaya konsisten lintas form, tidak ada konsumen lain yang bergantung pada nama lama). `LaporForm.tsx`: pemicu "buat baris decision" dan blok "Laporan Personal Marketing" digeneralisasi dari kondisi `formKey === 'pic_lokasi'` (Task 13) jadi berlaku utk formKey APAPUN yang memakai kunci standar tsb -- satu pemeriksaan, bukan bertambah satu `if` per form baru. **`pembangunan` BUKAN kontrak dengan `v_pembangunan_hari_ini`:** view `03-CALC-SPEC.md` §4.2 menjumlahkan `report where form_key='pic_lokasi'` SAJA; form `pembangunan` (Kepala Pembangunan, rekap SELURUH lokasi -- dokumennya sendiri bilang "Detail per lokasi ada di Laporan PIC Lokasi") hidup di `form_key` & namespace JSON yang sama sekali terpisah, karena itu field-nya sengaja diberi sufiks `_total` (`unit_sedang_dibangun_total`, dst.) supaya tidak tertukar dengan kunci kontrak `pic_lokasi`. Tidak ada view yang membaca `form_key='pembangunan'` saat ini. `cs` blok rekap pakai "MASALAH URGENT" (bukan "MASALAH UTAMA") dengan PIC/target tambahan -- dilebur ke `masalah_utama` bersama, tidak diduplikasi (blok 2 "KELUHAN URGENT" sudah lebih detail). `ga` satu-satunya form Batch D yang butuh field tambahan di blok bersama ("📦 KEBUTUHAN") -- `blokKeputusanCeo()` diberi parameter opsional `fieldTambahan` utk kasus ini. `perizinan` blok 1 "Tahapan" (6 checkbox) dimodelkan sebagai kolom teks bebas di tabel, bukan 6 checkbox per baris -- `Tabel` (Task 08) belum punya checkbox per sel, keterbatasan yang sama dengan yang sudah dicatat di Task 12/13. |
| 16 | Form `manager_resto`, `ita` | ✅ | `tsc`/`build`/`lint` bersih. Schema `f16-manager-resto.ts` (13 blok) dan `f16-ita.ts` (10 blok + 1 blok Senin-saja) sudah lengkap dan terdaftar. 5 keputusan user terimplementasi: (1) Dapur dobel dipertahankan cuma di blok 6, dihapus dari blok 8. (2) Blok 12 jadi ringkasan baca-saja (`ringkasanKebutuhanBesok`). (3) Blok 8 Ita silang-cek independen + angka Manager ditampilkan otomatis. (4) Blok 10 Accounting: precast/DTI/material/infrastruktur otomatis, kontraktor/listrik diketik. (5) Blok 16 Accounting: 3 baris decision terpisah via `sumberKeputusan`. Migrasi `0013`/`0014` dibuat dan dijalankan. `wajibJika` dan `hanyaHari` diuji langsung. DB sungguhan 9 uji (`scripts/uji-batch16-17.mjs`). | Lihat catatan naratif di atas ("Task 16/17 -- 23 Agustus 2026"). |
| 17 | Form `accounting` (rahasia) | ✅ | Schema `f17-accounting.ts` (17 blok, `rahasia: true`). Keputusan 4 (blok 10 campuran otomatis+manual) dan keputusan 5 (3 decision terpisah) terimplementasi. `sumberKeputusan` diuji: 4 baris → cuma 3 decision terbuat (constraint urgensi 1-3), deadline invalid jadi `null`. Blok 13 rekonsiliasi resto 3 lapis (`OmzetRestoOtomatis`) lewat query biasa (bukan security definer). `can_see_report()` sudah memberi accounting akses ke `manager_resto`/`ita`. | Lihat catatan naratif di atas. |
| 18 | Papan Kontrol | ✅ | `tsc`/`build`/`lint` bersih (1 warning informasional lama). View `v_papan_hari_ini` (migrasi `0015`, `security_invoker=on`, persis 03-CALC-SPEC.md §4.1 + kolom `nudged_at` lewat LEFT JOIN kedua). `tagih_laporan()` (stub kosong sejak 0002_rls.sql) diselesaikan: INSERT draft sekali lalu UPDATE `nudged_at` (pola sisipkan-sekali-lalu-update yang sama dengan `useSimpanDraft`), BUKAN update langsung (policy `report_nudge` sengaja dihapus). DB sungguhan (`scripts/uji-papan-kontrol.mjs`, penyamaran RLS, BEGIN/ROLLBACK, 9 titik): assignment baru langsung jadi kartu "belum lapor" tanpa ubah kode; karyawan biasa ditolak memanggil `tagih_laporan`; Pusat menagih → `nudged_at` terisi TAPI `report_id` tetap null (nudge bukan submit palsu); PIC lalu mengisi baris draft yang SAMA sampai terkirim → tetap SATU baris, kartu berubah warna sesuai `warna` yang baru benar-benar dihitung (lihat kolom Catatan). | **Bug ditemukan & diperbaiki sebelum Task 18 selesai, di luar scope literalnya tapi wajib supaya kartu berwarna berarti sesuatu**: `LaporForm.tsx` SELALU mengirim `warna:'hijau'` hardcode sejak Task 10/12, tidak peduli field `status_warna` yang dipilih pengisi maupun kondisi otomatis (§5) -- semua kartu Papan Kontrol akan selalu hijau selamanya kalau dibiarkan. Diperbaiki dengan `lib/warna.ts` (`warnaTerburuk`, `warnaDipilihDari`, `urgensiTerburukDariKirim`, `warnaOtomatis`) -- generik, dipakai `tanganiKirim` untuk SEMUA form, bukan per-formKey: warna akhir = paling buruk antara warna yang dipilih pengisi sendiri (field `type:'status_warna'` pertama di schema, kalau ada) dan warna otomatis (decision urgensi 1 → merah; terlambat atau decision urgensi 2/3 → kuning). **Sengaja TIDAK diimplementasikan**: pemicu merah "ada selisih tanpa penyebab" (§5) -- setiap field selisih di app ini sudah dipaksa `wajibJika` mengharuskan penyebab terisi sebelum submit bisa lolos sama sekali, jadi kondisinya sudah dicegah di lapisan validasi, bukan dibiarkan. `bolehTagih` (tombol Tagih) dibatasi ke role `pusat` saja sesuai kalimat task board ("Untuk role pusat: tombol Tagih") -- CEO melihat kartu yang sama tanpa tombol itu. "Hari Minggu papan kosong" dicek CLIENT-SIDE (`policy.workdays` vs `hariISOWIB()` di `app/papan/page.tsx`), bukan di view -- view tetap berguna dipakai hari apa pun (mis. kalau nanti ada kebutuhan histori). |
| 19 | Antrean Keputusan CEO | ✅ | `tsc`/`build`/`lint` bersih. `lib/api/decision.ts` diperluas: `useAntreanKeputusan` (status `menunggu`, urut `urgensi` lalu `deadline` -- persis `decision_antrean_idx`), `useRiwayatKeputusan`, `useMemutuskan` (Setujui/Cicil/Tunda/Tolak, isi `decided_by`/`decided_at`/`keputusan_catatan`). `app/keputusan/page.tsx` diganti `['ceo','pusat']` (dari cuma `'ceo'`, sesuai debt yang dicatat di baris Task 06) dengan dua tab (Menunggu/Riwayat); Pusat dapat kontrol urut lokal (urgensi/deadline/nominal) sebagai ganti "menyusun urutan prioritas" -- tanpa tombol keputusan sama sekali, dan tidak ada mutasi apa pun yang bisa dipanggil Pusat dari UI. DB sungguhan (`scripts/uji-keputusan-ceo.mjs`, penyamaran RLS, BEGIN/ROLLBACK, 12 titik): Pusat TIDAK melihat decision dari laporan `accounting` tapi TETAP melihat decision form lain; CEO melihat keduanya; urutan urgensi benar; update dari Pusat ditolak database (`rowCount=0`); CEO memutuskan → `decided_by`/`decided_at`/catatan tersimpan, baris pindah ke riwayat. | **Bug keamanan ditemukan & diperbaiki SEBELUM halaman ini sempat menampilkannya ke siapa pun** -- lihat bagian narasi baru "Task 18/19 -- 23 Agustus 2026" di bawah untuk kronologi lengkap (dua migrasi, `0016` lalu perbaikan `0017` setelah `0016` sendiri ternyata masih bocor lewat jebakan RLS-bersarang). Ringkas: policy `dec_select` (sejak Task 04) mengizinkan Pusat melihat SEMUA baris `decision` tanpa kecuali; sejak Task 17 (blok "Prioritas Pembayaran" `accounting`), itu berarti Pusat bisa membaca judul/nominal/dampak keputusan Accounting -- melanggar CLAUDE.md aturan #3. |
| 20 | View agregasi + dashboard angka CEO | ✅ | `tsc`/`build`/`lint` bersih. Migrasi `0018` -- tiga view SISA dari 03-CALC-SPEC.md §4 yang belum pernah ada (`v_marketing_bulanan` §3 dan `v_pembangunan_per_lokasi` §3.4b sudah ada sejak Task 15/16, TIDAK dibuat ulang, sesuai instruksi user): `v_pembangunan_hari_ini` (§4.2, `security_invoker=on`), `v_keuangan_rekap` (§4.3, `security_invoker=off`+guard has_role, PERSIS 4 kolom), `v_selisih_resto` (§4.4, `security_invoker=on`, INNER JOIN manager_resto+ita hari ini). Dashboard dipasang di `app/page.tsx` (beranda), tampil kalau `roles.includes('ceo')` -- tidak ada rute baru, task board tidak menyebut rute khusus. DB sungguhan (`scripts/uji-dashboard-ceo.mjs`, penyamaran RLS, BEGIN/ROLLBACK, 13 titik): `v_pembangunan_hari_ini` menjumlahkan 2 laporan `pic_lokasi` dengan benar dan SELALU tepat 1 baris; `v_keuangan_rekap` memberi Pusat 4 angka walau Pusat TANPA akses baris `report` accounting sama sekali, kolom PERSIS 5 (`tanggal`/`total_masuk`/`total_keluar`/`net`/`warna`), field lain (umpan `"RAHASIA"`) tidak bocor, karyawan biasa 0 baris; `v_selisih_resto` cuma menampilkan outlet yang KEDUA laporannya (manager_resto DAN ita) sudah masuk hari ini, selisih dihitung benar. | Panel privilese CEO (dana tersedia, piutang, kewajiban 7/30 hari, surplus/kekurangan) BUKAN dari view baru -- CEO sudah punya akses baris PENUH ke `accounting` lewat `can_see_report()`, jadi `useLaporanAccountingHariIni()` (`lib/api/accounting.ts`) query biasa lalu `hitungRingkasanKeuanganCeo()` menjumlahkan field DALAM SATU baris (pola sama dengan `hitungCashflowHariIni`, bukan "menghitung ulang agregasi lintas laporan" yang dilarang aturan #7 -- itu soal agregasi LINTAS baris). **⚠️ "Surplus/kekurangan" TIDAK punya rumus di 03-CALC-SPEC.md §4** -- istilah itu cuma muncul di daftar kerja Task 20 di task board, tanpa formula bernomor. Dipilih (bukan ditebak diam-diam, didokumentasikan di kode & di sini): `danaTersedia - kewajiban30Hari` (satu-satunya field kewajiban yang benar-benar diketik sebagai TOTAL, `total_kewajiban_30_hari`). Label di UI eksplisit "Surplus/kekurangan (vs kewajiban 30 hari)", BUKAN diklaim KPI resmi yang sudah disetujui CEO -- **perlu dikonfirmasi ke CEO**, masuk daftar asumsi belum dikonfirmasi bersama §7 BLUEPRINT. `v_selisih_resto` §4.4 juga meminta efek samping "kalau selisih ≠ 0, kedua laporan ditandai 🔴 + otomatis dibuat `decision` urgensi 2" -- **TIDAK diimplementasikan**, dicatat sebagai utang (butuh trigger lintas-laporan yang dirancang hati-hati supaya tidak infinite-loop, di luar cakupan "view + dashboard" yang literal). `components/AngkaGrid.tsx` baru (generik, sesuai penamaan di `04-CATATAN-TEKNIS.md` §5). |
| 21 | Laporan Terpusat Sabrina (auto-isi) | ✅ | `tsc`/`build`/`lint` bersih. `app/terpusat/page.tsx` diganti `['pusat','ceo']` (dari cuma `'pusat'`) -- Bagian 11 beda per peran (Task 21 sendiri minta itu). 15 bagian baca-saja memakai peta sumber dari "Analisis 5 format asli" (`docs/PROGRESS.md`): §1/§14 IT, §2 CS, §3 GA, §4 `v_security_hari_ini` (baru, SUM lintas lokasi+shift), §5 HRD, §6 Perizinan, §7 `v_pembangunan_hari_ini`(Task20)+target dari `v_pembangunan_per_lokasi`(Task16)+Laporan Pembangunan, §8 listing langsung `pic_lokasi` per lokasi, §9 `v_stk_hari_ini` (baru), §10 DTI, §11 `v_keuangan_rekap` (Keputusan B -- format Accounting menang, 4 angka utk Pusat) + versi lengkap CEO (reuse Task 20), §12 Kendaraan, §13 `v_marketing_hari_ini` (baru) + `v_marketing_bulanan` (Task15, DIFILTER bukan diagregasi ulang) utk daftar tertinggal, §15 `v_papan_hari_ini` (Task18, reuse) + `decision` (Task19, reuse `useAntreanKeputusan`). Migrasi `0019` -- tiga view baru, SATU-satunya yang genuinely butuh SUM lintas banyak baris report hari ini (security & pic_lokasi scope `'lokasi'`, marketing lintas seluruh karyawan); sembilan sumber LAIN (form `scope:'global'`) query biasa satu baris, tidak butuh view. `forms/f21-pusat.ts` (baru, terdaftar di `forms/index.ts`) -- HANYA §16 Target Besok (7 field) + §17 keputusan TAMBAHAN dari Pusat (`sumberKeputusan`, mekanisme generik yang sama dgn accounting Task17) + Kesimpulan (3 field) -- direnderkan lewat `<LaporForm formKey="pusat">` yang SUDAH ADA, bukan mekanisme simpan baru. Tombol cetak (`window.print()`) + CSS `@media print` (`print-hide`/`print-seksi`) di `app/terpusat/page.tsx`. DB sungguhan (`scripts/uji-terpusat.mjs`, penyamaran RLS, BEGIN/ROLLBACK, 12 titik): `v_security_hari_ini`/`v_stk_hari_ini` menjumlahkan benar lintas 2 laporan; `v_marketing_hari_ini` menghitung `total_karyawan` benar dan `sudah_lapor_hari_ini` TIDAK menghitung laporan berstatus draft. | Belum dikonfirmasi eksplisit ke CEO: Keputusan B (§11 ikut format Accounting, bukan 7-angka format Sabrina asli) -- sudah dicatat sebagai pertanyaan terbuka sejak "Analisis 5 format asli", TETAP belum ditanyakan (di luar kapasitas sesi ini menghubungi CEO). **Cakupan field per bagian bersifat REPRESENTATIF, bukan 100% seluruh field skema divisi** -- tiap bagian menampilkan angka bernomor + 1-3 catatan narasi kunci (masalah/kendala/PIC/target), bukan mengeksplor SEMUA 40-90 field per form; ini keputusan sadar mengingat volume (8+ schema, ratusan field) vs nilai tambah menampilkan seluruhnya di satu halaman ringkasan -- FORMAT-ASLI-02-PUSAT.md sendiri juga cuma minta ringkasan per bagian, bukan rincian penuh. §4 Security tidak menampilkan narasi per-shift (keperluan penting/kejadian) di halaman ringkasan, cuma jumlahnya -- diarahkan "lihat laporan Security masing-masing". §14 IT/medsos cuma menampilkan platform Koperumnas utama (bukan rincian per-outlet DTI/Indokopi/Indosteak lengkap). Efek otomatis §4.4 (selisih resto -> tandai merah + decision) yang dicatat sbg utang Task 20 TETAP belum diimplementasikan. |
| 22 | Dashboard Kontrol Marketing | ✅ | `tsc`/`build`/`lint` bersih. `app/marketing/page.tsx` diganti `['kontrol_marketing','ceo','pusat']` (dari cuma `kontrol_marketing`, sesuai "Selesai kalau" Task 22 sendiri). Tabel SEMUA karyawan dari `useMarketingBulananSemua()` (baru, `v_marketing_bulanan` tanpa filter `user_id` -- view yang sama Task 15, tidak ditulis ulang), kolom undangan/closing/hari bolong pakai `statusUndangan`/`statusClosing` (sudah ada) dan status bonus/potongan pakai `hitungKelayakanBonus`/`hitungPotongan` (sudah ada, Task 12) -- SEMUA rumus dari fungsi terpusat yang sudah ada, tidak ada rumus baru ditulis di halaman ini. Filter per divisi (dropdown dari nilai unik `divisi` yang muncul) dan urut "paling tertinggal" (`hari_bolong` desc) vs nama. Klik baris -> detail kalender bulan berjalan per karyawan: `lib/kalenderPte.ts` (`kalenderPteBulanIni`, fungsi murni) memutuskan warna PER TANGGAL dari baris `pte_daily` (`usePteBulanIniUntuk`, baru) + `profile.mulai_kerja` (`useMulaiKerja`, baru) -- TIDAK menghitung ulang `hari_bolong` itu sendiri (tetap dari view), cuma menggambar per hari mana yang bolong. DB sungguhan (`scripts/uji-marketing-dashboard.mjs`, penyamaran RLS, BEGIN/ROLLBACK, 6 titik): `kontrol_marketing` bisa baca `v_marketing_bulanan` DAN `pte_daily` milik ORANG LAIN (dua query baru halaman ini, RLS `pte_select`/`profile_select` sudah mengizinkan sejak 0002_rls.sql, tidak ada policy baru dibuat); karyawan biasa (bukan kontrol_marketing/ceo/pusat/manager_resto) TETAP 0 baris `pte_daily` orang lain. | "Awal wajib" kalender (tgl 1 bulan / `pte_mulai_berlaku` / `mulai_kerja`, yang paling akhir) dihitung ULANG di `lib/kalenderPte.ts` dengan rumus yang SAMA persis `v_marketing_bulanan` (03-CALC-SPEC.md §3) supaya visualnya konsisten dengan angka `hari_bolong` yang ditampilkan -- kalau rumus `greatest()` di view berubah, `kalenderPteBulanIni` harus ikut diubah manual, dicatat sebagai kaitan yang perlu diingat (sama pola dengan catatan `pte_daily.lengkap` generated column di §1). |
| 23 | Halaman admin | ✅ | `tsc`/`build` bersih, `lint` 0 error (1 warning informasional lama). CRUD `profile`/`role`/`assignment`/`lokasi`/`outlet`/`policy` (`lib/api/admin.ts`, `app/admin/page.tsx`, 5 tab) SEMUANYA lewat klien browser biasa -- CEO sudah punya akses tulis penuh lewat RLS `*_admin` (0002_rls.sql, Task 04), TIDAK ada policy baru dibuat. Route Handler `app/api/admin/user/route.ts` (server-only, BUKAN `'use client'`) SATU-SATUNYA yang membaca `SUPABASE_SERVICE_ROLE_KEY` -- dua lapis penjaga sebelum kunci dipakai: (1) ada sesi login (proxy.ts sudah menolak sebelum sampai sini, dicek ulang eksplisit di dalam handler sebagai jaring pengaman kedua), (2) sesi itu `has_role('ceo')` (query `role` pakai klien SESI ITU SENDIRI, bukan admin client). `lib/api/admin.ts` (`useBuatPengguna`) cuma `fetch('/api/admin/user')`, tidak pernah mengimpor/menyentuh nama kunci itu. Logikanya dipindah dari `scripts/buat-akun.mjs` (Task 23 sendiri minta ini) TAPI diseerhanakan: skrip CLI aslinya insert profile/role lewat koneksi Postgres langsung (`SUPABASE_DB_URL`, secret terpisah); Route Handler memakai `admin.from(...)` (klien admin Supabase-js yang sama, PostgREST) supaya cuma SATU secret (`SUPABASE_SERVICE_ROLE_KEY`) yang dibutuhkan saat deploy produksi, bukan dua. Diverifikasi: `curl` sungguhan (`npm run dev`) ke `/api/admin/user` TANPA sesi -> **307 dialihkan proxy ke `/masuk`** (tidak pernah sampai ke handler), konfirmasi lapis pertama benar-benar jalan. DB sungguhan (`scripts/uji-admin.mjs`, penyamaran RLS, BEGIN/ROLLBACK, 8 titik): CEO berhasil insert lokasi/assignment + update policy lewat klien biasa; karyawan biasa DITOLAK RLS pada ketiganya; pemeriksaan isi file (bukan cuma dibaca) mengonfirmasi tidak ada `'use client'` di `route.ts` dan `SUPABASE_SERVICE_ROLE_KEY` tidak disebut sama sekali di `lib/api/admin.ts`/`app/admin/page.tsx`. | **Belum diverifikasi lewat sesi cookie CEO sungguhan** (butuh replikasi format cookie `@supabase/ssr` yang rumit lewat `curl` mentah) -- kasus "CEO login -> buat pengguna -> berhasil" dan "non-CEO login -> ditolak 403" TETAP masuk CHECKPOINT 4, konsisten dengan pola pengujian seluruh proyek ini (penyamaran RLS di Postgres menggantikan sesi cookie browser sungguhan di mana-mana, bukan cuma di sini). Halaman admin TIDAK punya UI hapus pengguna/nonaktifkan akun (hanya tambah role/lokasi/outlet/assignment + nonaktifkan lokasi/outlet) -- di luar permintaan eksplisit Task 23 ("CRUD sederhana"), tidak diklaim lengkap. |
| 24 | Uji RLS & deploy | ✅ | Deploy Vercel sudah terjadi lebih awal (24 Agustus 2026, lihat "Reset password 7 akun uji + persiapan deploy"). Matriks RLS §4 `04-CATATAN-TEKNIS.md` (14 baris, `scripts/uji-task24-matriks.mjs`) dijalankan lengkap 29 Agustus 2026 -- **LOLOS SEMUA di percobaan pertama**, termasuk dua baris yang dulu ditunda (#3 `v_keuangan_rekap`, #10 storage tanpa signed URL). `tsc`/`build`/`lint` bersih. Uji lapangan HP sungguhan (Checkpoint 4, 10 langkah) juga 10/10 SESUAI -- lihat "Uji lapangan HP" di atas. | Utang cuti/sakit/izin PTE TETAP terbuka (lihat "Utang WAJIB sebelum go-live") -- Task 24 selesai tidak berarti sistem siap menghitung gaji, cuma berarti RLS & jalur produksi sudah teruji. |

---

## Perkakas `scripts/` (pengecualian dependensi, disetujui user)

`pg` dan `dotenv` dipasang sebagai **devDependency**, di luar stack aplikasi yang dikunci `BLUEPRINT.md` §2 — user menyetujui eksplisit ini karena keduanya cuma dipakai skrip CLI lokal, tidak pernah masuk bundle aplikasi.

- **`scripts/db.mjs`** (`npm run db -- <file.sql | "query">`) — konek langsung ke Postgres lewat `SUPABASE_DB_URL` (pooler Supabase), cetak hasil mentah sebagai tabel. Dipakai untuk verifikasi skema/migrasi. **Koneksi ini adalah pemilik tabel — melewati SELURUH RLS.** Tidak boleh dipakai untuk uji RLS (itu tugas skrip Checkpoint 2 yang menyamar lewat `set role authenticated` + `auth.uid()` sanity check). Ada jaring pengaman `sensor()` yang menyaring string mirip DSN dari pesan error sebelum dicetak, supaya `SUPABASE_DB_URL` tidak pernah nyasar ke layar/log.
- **`scripts/buat-akun.mjs`** (`npm run akun`) — **cikal bakal Task 23**. Pakai `supabase.auth.admin.createUser` (butuh `SUPABASE_SERVICE_ROLE_KEY`, dibaca dari `.env.local`, tidak pernah ditulis ke file lain), baca daftar akun dari `scripts/akun.json` (reusable untuk 36 karyawan nanti, bukan cuma 7 akun uji), idempoten (email yang sudah ada dilewati, dicek lewat `SUPABASE_DB_URL` sebelum createUser — bukan menebak dari pesan error GoTrue). Setelah akun ada, mengisi `profile`+`role` lewat query terparameter yang dibangun dari `akun.json` (bukan SQL statis tersalin ulang), supaya logikanya sama persis untuk 7 atau 36 akun. **Route Handler `app/api/admin/user` di Task 23 nanti memakai logika create-user + isi-profile-role yang sama persis** — skrip ini adalah versi CLI-nya duluan.
- **⚠️ Password sementara `123456` untuk SEMUA akun baru — keputusan sengaja user, bukan kelalaian.** Dipilih supaya tidak perlu klik manual 7× (lalu 36× bulan depan) dan supaya proses verifikasi fitur tetap simpel di tahap ini. **Wajib direset ke password asli per-orang sebelum dipakai karyawan sungguhan** — termasuk akun `ceo` dan `accounting`, dua peran paling sensitif di sistem ini. Belum ada mekanisme reset dibuat; catat ini supaya tidak lupa sebelum go-live.
- Kedua skrip diverifikasi jalan sungguhan: `db.mjs` diuji dengan query sepele dulu sebelum dipakai untuk hal penting; `buat-akun.mjs` dijalankan 2× berturut-turut untuk membuktikan idempotensinya (run kedua: semua "sudah ada, dilewati", tidak ada duplikat role).

---

## Utang uji — CHECKPOINT 4 (verifikasi browser sungguhan)

Belum bisa diuji di sesi ini karena tidak ada tool browser/DOM headless. Bukan diklaim selesai — **wajib dicek ulang saat sampai CHECKPOINT 4**, sebelum lanjut ke batch berikutnya. **Diperbarui 29 Agustus 2026** setelah uji lapangan HP sungguhan (10/10 SESUAI, lihat bagian "Uji lapangan HP" di atas) — baris yang terbukti langsung oleh uji itu ditandai ✅ SELESAI, sisanya tetap terbuka apa adanya, tidak ditebak lolos:

- ✅ **SELESAI (24 Agustus 2026, sempat lupa dicoret):** ~~ketujuh akun uji masih berpassword 123456~~ — sudah direset lewat `scripts/set-password.mjs` (lihat bagian "Reset password 7 akun uji" di atas), password lama sudah tidak berlaku.
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** submit form `onSubmit` terpanggil dengan data benar (Task 07); readback nilai field terisi benar dari yang diketik (Task 08) — dibuktikan lewat 2 laporan penuh (`personal_marketing` 9 blok, `pic_lokasi`) berhasil diisi & terkirim dengan data yang lalu tampil benar di Riwayat & Laporan Terpusat.
- Field `tabel` tidak menyebabkan scroll horizontal di lebar 360px — form yang dipakai (`personal_marketing` punya field tabel `closing_list`) berhasil diisi tanpa keluhan di HP asli, tapi lebar layar & scroll horizontal spesifik tidak diperiksa langsung — **masih terbuka, bukan diklaim lolos**.
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** highlight latar merah muda pada field gagal validasi, `scrollIntoView` ke field itu, dan submit yang benar-benar tertahan saat bukti wajib kosong (Task 09) — ketiganya persis yang terjadi di Langkah 5 uji lapangan HP ("Live"=Ya tanpa bukti → ditolak, scroll otomatis, field ter-highlight).
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** login sebagai CEO (Putri) berhasil di browser sungguhan (Task 02) — catatan: klaim lama "`roles` terisi `['ceo','karyawan']`" sudah tidak berlaku, Putri sekarang cuma `['ceo']` sejak perbaikan Beranda 24 Agustus (lihat "Masalah 3 lanjutan" di atas), bukan regresi.
- Refresh halaman tidak melempar user keluar (sesi lewat cookie) — **masih terbuka**, tidak ada langkah refresh eksplisit di uji lapangan HP (Task 02).
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** `signOut()` mengembalikan ke `/masuk` — dilakukan 2x nyata (ganti dari Dadang→Putri→Sabrina) di uji lapangan HP (Task 02).
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** `usePolicy()` mengembalikan data nyata di browser dengan sesi aktif — dibuktikan tidak langsung: label jam batas di Beranda (Langkah 3) berasal dari `policy.deadline_by_form` lewat `usePolicy()`, dan tampil benar (Task 05).
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** halaman "Tidak punya akses" (`Terlindungi`) tampil saat user LOGIN dengan peran salah — persis Langkah 10 (Sabrina/`pusat`, login sungguhan, buka `/lapor/accounting` langsung → ditolak) (Task 06).
- Tidak ada elemen terpotong / scroll horizontal di lebar 360px — uji lapangan HP melewati ±10 layar di HP asli tanpa keluhan dilaporkan, tapi tidak ada pemeriksaan lebar-piksel eksplisit per layar — **dianggap berkurang risikonya, belum "selesai" secara formal** (Task 06).
- Debounce 5 detik draft otomatis, dan "isi form → refresh → isian masih ada" — **masih terbuka**, uji lapangan HP mengisi & langsung mengirim, tidak ada langkah refresh-di-tengah-isi (Task 10/12).
- `kompresGambar()` dari kamera HP sungguhan — **sebagian terbukti**: dua foto diambil langsung dari kamera (Langkah 4, evidence "Live") berhasil terkompresi, terunggah, dan tampil lagi di Riwayat (Langkah 7) tanpa error/macet — risiko utama (API canvas gagal total di device asli) sudah gugur. Angka pasti "<500KB" tidak diukur, jadi baris ini belum dicoret penuh (Task 11).
- Bilah progres unggah per file benar-benar terlihat & bergerak di UI sungguhan — **masih terbuka**, tidak dilaporkan spesifik di uji lapangan HP (Task 11).
- ✅ **SELESAI SEBAGIAN (29 Agustus 2026, HP sungguhan):** alur form `personal_marketing` 9 blok — isi, kirim, unggah bukti dari kamera, dan pesan konfirmasi (terlambat/tepat waktu) semua terjadi nyata di Langkah 4-5. **Masih terbuka**: autosave 5 detik tidak teramati langsung (form diisi lalu langsung dikirim, tidak ada jeda-lalu-refresh untuk membuktikan draft tersimpan sendiri), tampilan angka "undangan ___/20" tidak dilaporkan spesifik (Task 12).
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** form `pic_lokasi` — isi & kirim berhasil, bukti wajib menahan submit tanpa lampiran (sama seperti Langkah 5, field bukti progress `pic_lokasi` juga `buktiWajib`), dan baris `decision` dari "Butuh keputusan CEO" benar-benar muncul di `/keputusan` sebagai Putri (Langkah 6+8, rantai penuh end-to-end) (Task 13). **Masih terbuka**: pemilih lokasi untuk PIC dengan >1 lokasi — tidak relevan untuk Dadang (cuma 1 penugasan, Tajur), belum ada PIC 2-lokasi sungguhan yang login di produksi untuk mengujinya.
- Tab nav dinamis (`lib/navLapor.ts`): benar-benar muncul di header saat user dengan assignment sungguhan login di browser (mis. Dadang → tab "Lapor Lokasi", Kasam → "Lapor Keamanan") — logikanya sudah diuji murni (6 skenario tanpa React), belum diklik di browser (Task 06, diperbaiki 23 Agustus 2026)
- Kedelapan form Task 14/15 (`hrd`, `security`, `perizinan`, `pembangunan`, `dti`, `kendaraan`, `cs`, `ga`): seluruh alur UI sungguhan (pemilih kombinasi lokasi+shift utk `security`, isi puluhan field per form, tabel dinamis tambah/hapus baris, kirim, baris `decision` yang tercipta dari kedelapannya) belum diklik/diketik di browser — logikanya sudah diuji lewat DB/SSR langsung (Task 14/15)
- Tiga blok rollup baca-saja di `pembangunan` (`RekapUnitOtomatis`/`RekapMaterialOtomatis`/`RekapInfrastrukturOtomatis`, revisi D3/D4, sekarang lewat view agregat `v_pembangunan_per_lokasi` + `boleh_lihat_rekap()`): tampilannya di browser sungguhan belum dilihat — logika query sudah diuji lewat DB langsung sebagai profil persis Ronald (`scripts/uji-boleh-lihat-rekap.mjs`, RLS-nya sudah SELESAI, bukan lagi diblokir), interaksi manusia-ke-layarnya belum
- Form `it` (Task 14, 23 Agustus 2026): ke-14 blok belum diklik/diketik di browser, blok baca-saja "PIC lokasi belum upload" (`v_pic_lokasi_belum_upload_progress` + `boleh_lihat_rekap('it')`) belum dilihat tampil sungguhan sebagai Diki — logika sudah diuji lewat DB langsung (`scripts/uji-it-belum-upload.mjs`), interaksinya belum
- Form `manager_resto`/`ita`/`accounting` (Task 16/17, 23 Agustus 2026): pemilih outlet (scope `outlet`, generalisasi dari pemilih lokasi) belum diklik sungguhan; pesan error `wajibJika` (mis. `selisih_stok` wajib kalau `ada_selisih_stok='ya'`) belum dilihat muncul di layar; blok `hanyaHari` (Stock Opname Senin di `ita`) belum diamati benar-benar hilang/muncul sesuai hari sungguhan di browser (baru diuji lewat injeksi tanggal manual di skrip); keempat panel baca-saja (`KebutuhanBesokRestoOtomatis`, `StokManagerUntukItaOtomatis`, `KebutuhanPembangunanAccountingOtomatis`, `OmzetRestoOtomatis`) belum dilihat tampil sungguhan; pembuatan 3 baris `decision` terpisah dari satu field `sumberKeputusan` (blok 16 `accounting`) belum diklik lewat form sungguhan — seluruh logika di atas sudah diuji lewat DB nyata (`scripts/uji-batch16-17.mjs`, 9 titik, BEGIN/ROLLBACK), interaksi manusia-ke-layarnya belum
- Papan Kontrol (Task 18, 23 Agustus 2026): kartu belum-lapor/sudah-lapor, bar "x/y PIC sudah melapor", tombol Tagih (Pusat) yang benar-benar mengubah kartu di layar, dan pesan "hari ini bukan hari kerja" pada hari Minggu belum dilihat di browser sungguhan — logikanya sudah diuji lewat DB langsung (`scripts/uji-papan-kontrol.mjs`, 9 titik). Warna kartu (baru benar-benar dihitung mulai commit ini, lihat catatan naratif) belum diamati berubah warna sungguhan saat submit form apa pun dari HP.
- Panel "Cashflow Hari Ini (otomatis)" `accounting` (Task 18/20) belum dilihat tampil sungguhan menghitung ulang saat blok 2/4 diketik.
- ✅ **SELESAI SEBAGIAN (29 Agustus 2026, HP sungguhan):** antrean Keputusan CEO -- baris muncul di tab "Menunggu" dengan judul, nama pelapor (Dadang), dan asal laporan (Lokasi) yang benar (Langkah 8 uji lapangan HP) (Task 19). **Masih terbuka**: tombol Setujui/Cicil/Tunda/Tolak beserta kolom catatan, tab Riwayat, dan kontrol urut lokal Pusat (urgensi/deadline/nominal) belum diklik sungguhan -- logikanya sudah diuji lewat DB langsung (`scripts/uji-keputusan-ceo.mjs`, 12 titik, termasuk perbaikan kebocoran RLS `dec_select`).
- Dashboard CEO di beranda (Task 20, 23 Agustus 2026): keempat angka keuangan, panel privilese (dana tersedia/piutang/kewajiban), rekap unit pembangunan, dan silang-cek omzet resto belum dilihat tampil sungguhan saat login sebagai CEO — logikanya sudah diuji lewat DB langsung (`scripts/uji-dashboard-ceo.mjs`, 13 titik).
- ✅ **SELESAI SEBAGIAN (29 Agustus 2026, HP sungguhan):** Laporan Terpusat login sebagai Sabrina -- bagian PIC Lokasi terisi angka nyata dari laporan Dadang yang baru dikirim (Langkah 9), dan bagian keuangan menampilkan cuma 4 angka rekap tanpa rincian transaksi (Langkah 10, ikut menutup uji kerahasiaan Accounting) (Task 21). **Masih terbuka**: beda tampilan Bagian 11 antara Pusat dan CEO (cuma dilihat sebagai Pusat), tombol cetak/ekspor PDF, dan pengisian Bagian 16/17/Kesimpulan lewat `<LaporForm formKey="pusat">` -- logikanya sudah diuji lewat DB langsung (`scripts/uji-terpusat.mjs`, 12 titik).
- Dashboard Kontrol Marketing (Task 22, 23 Agustus 2026): tabel filter-per-divisi, tombol urut "paling tertinggal", klik baris membuka kalender per karyawan (warna hijau/merah per tanggal) belum diklik/dilihat sungguhan di browser — logikanya sudah diuji lewat DB langsung (`scripts/uji-marketing-dashboard.mjs`, 6 titik) dan `tsc` untuk `lib/kalenderPte.ts`, belum ada uji unit murni terpisah untuk fungsi kalendernya sendiri (di luar `tsc`). Warning `react-hooks/exhaustive-deps` sempat muncul di `app/marketing/page.tsx` (array fallback `?? [1,2,3,4,5,6]` bikin `useMemo` selalu recompute) -- diperbaiki di commit Task 23 (dipindah ke dalam callback `useMemo`), sebelum sempat dilaporkan sebagai "bersih" yang keliru.
- Halaman Admin (Task 23, 23 Agustus 2026): kelima tab (Lokasi/Outlet/Penugasan/Policy/Pengguna) belum diklik sungguhan di browser sebagai CEO -- termasuk kasus konkret "Selesai kalau" Task 23 sendiri: tambah lokasi baru + assign PIC lalu lihat kartunya muncul di Papan Kontrol, dan ubah `policy.closing_target` 2->3 lalu lihat angka `___/3` berubah di seluruh aplikasi. Logikanya sudah diuji lewat DB langsung (`scripts/uji-admin.mjs`, 8 titik) + `curl` sungguhan ke `/api/admin/user` tanpa sesi (307, proxy menolak). Sesi cookie CEO sungguhan (buat pengguna baru lewat UI, bukan penyamaran RLS) juga masuk CHECKPOINT 4.
- ✅ **SELESAI (29 Agustus 2026, HP sungguhan):** Riwayat "Laporan Saya" -- daftar berisi kedua laporan yang baru dikirim, klik baris membuka detail baca-saja lengkap, tombol "Lihat foto/video" (signed URL) berhasil membuka foto yang tadi diambil dari kamera (Langkah 7 uji lapangan HP).
- Pemilih tanggal di Papan Kontrol & Laporan Terpusat (24 Agustus 2026): tombol ←/→ dan `<input type="date">` native belum disentuh sungguhan di HP (perilaku `<input type="date">` di browser HP asli beda-beda per OS), termasuk memastikan tombol Tagih otomatis nonaktif saat melihat tanggal bukan hari ini. Logikanya sudah diuji lewat DB langsung (`scripts/uji-tanggal-mundur.mjs`, 10 titik).
- Perbaikan "TERLAMBAT" di hari libur (24 Agustus 2026): pesan konfirmasi baru ("di luar hari wajib lapor") dan warna kartu Papan Kontrol yang seharusnya tidak lagi memerah/menguning gara-gara ini belum dilihat sungguhan saat mengirim laporan di hari libur asli. Logika fungsi murni sudah diuji (`scripts/uji-terlambat-workday.mjs`, 3 titik).
- ✅ **SELESAI SEBAGIAN (29 Agustus 2026, HP sungguhan):** Beranda baru -- sapaan waktu + nama, dan daftar "yang perlu dikerjakan" dengan jam batas terbaca benar (Langkah 3 uji lapangan HP). **Masih terbuka**: cuma keadaan "ada tugas" yang teramati -- keadaan "semua selesai" dan "hari libur" belum pernah dilihat sungguhan (perlu diuji lagi di hari kerja setelah semua tugas terkirim, atau di hari Minggu); update otomatis kartu hilang dari daftar setelah kirim TANPA refresh manual tidak diamati eksplisit; progres undangan/closing yang tersembunyi selama `pte_mulai_berlaku` kosong tidak dilaporkan spesifik. Logika murni sudah diuji (`scripts/uji-tugas-beranda.mjs`, 20 titik).
- ✅ **SELESAI SEBAGIAN (29 Agustus 2026, HP sungguhan):** §2 tampilan mobile + PWA -- **"Tambah ke Layar Utama" dan `display:standalone` BERHASIL** (Langkah 1: ikon muncul, dibuka tanpa address bar/tombol browser) -- risiko terbesar batch ini (dan bug `/serwist/` yang baru diperbaiki) terbukti tidak muncul lagi. Nav dipakai terus-menerus sepanjang 10 langkah (ganti akun 2x, pindah antar Beranda/form/Riwayat/Keputusan/Terpusat/Akun) tanpa keluhan navigasi. **Masih terbuka**: `env(safe-area-inset-bottom)` di iPhone bertakik (notch) spesifik -- model HP yang dipakai uji tidak diketahui, jadi belum bisa dipastikan; swap nav-atas↔nav-bawah di lebar 768px (di luar cakupan uji HP, perlu tablet/resize); tombol Kirim menempel (sticky) tidak dikonfirmasi spesifik terlihat menempel saat scroll form panjang; registrasi service worker lewat DevTools/Application panel belum diperiksa langsung (instalasi berhasil adalah bukti tidak langsung yang kuat, tapi bukan pemeriksaan DevTools).
- Tombol "Atur ulang kata sandi" di Admin (29 Agustus 2026): sesi cookie CEO sungguhan mengklik tombolnya di browser belum diamati -- logikanya sudah diuji lewat DB langsung (`scripts/uji-reset-password-log.mjs`, 3 titik) + `curl` tanpa sesi (307, proxy menolak) + mekanisme inti (`admin.auth.admin.updateUserById`) sudah terbukti jalan sungguhan lewat `scripts/set-password.mjs` 2x sesi ini. Sama seperti kasus Task 23 yang serupa (buat pengguna baru) -- risiko rendah, bukan belum pernah teruji sama sekali.
- **Presensi ber-radius (29 Agustus 2026) -- SAMA SEKALI belum dicoba di HP sungguhan**, beda dari fitur lain di daftar ini yang setidaknya sudah lewat uji lapangan HP Checkpoint 4: `getUserMedia({facingMode:'user'})` kamera depan sungguhan (beda mekanisme dari `<input capture>` yang sudah teruji di Langkah 4 uji lapangan HP -- BELUM tentu perilakunya sama persis lintas browser HP nyata); `navigator.geolocation` akurasi/timeout/permission-denied sungguhan di lapangan (satpam hujan pagi, dalam gedung, dst. -- skenario yang jadi ALASAN dibangunnya fitur ini, tapi belum pernah dialami langsung); draf lokal (`localStorage`, `lib/absenDraftLokal.ts`) menyintas benar-benar keluar-masuk app/refresh di tengah sinyal buruk; halaman Tinjau Absensi (`/absen/tinjau`) diklik sungguhan sebagai Sabrina/kadiv-HRD; tab Admin "Titik Absen" (tambah titik, atur jam per orang) diklik sungguhan. Logikanya SUDAH teruji penuh lewat DB (`scripts/uji-absen.mjs` + `scripts/uji-presensi-rls.mjs` + `scripts/uji-storage-absensi.mjs`, total 28 titik) -- interaksi manusia-ke-kamera-ke-GPS-nya belum sama sekali.

- **Ekspor Excel & PDF (30 Agustus 2026):** file .xlsx keempat ekspor belum pernah DIBUKA sungguhan di Excel/Google Sheets asli -- baru diverifikasi lewat `content-type` header + kode yang menyusunnya. Format `#,##0`/`dd/mm/yyyy`, baris beku, dan lebar kolom belum pernah dilihat langsung tampilannya. PDF Terpusat (`window.print()` + `@page` A4 baru) belum pernah dicetak/disimpan sungguhan dari browser HP maupun desktop -- "muat A4 & terbaca" baru asumsi dari CSS, bukan pengamatan. Mekanisme otorisasi SUDAH teruji lewat HTTP sungguhan (`scripts/uji-ekspor-keuangan-curl.mjs`) -- yang belum cuma tampilan hasil filenya.

## Batch — Cuti diatur dari web (30 Agustus 2026)

Instruksi eksplisit user, tiga hal sekaligus -- nomor 1 (pemetaan Inservice) dan nomor 3 (konfirmasi koordinat) dikerjakan lebih dulu (cepat), lalu rencana nomor 2 (Cuti) dipresentasikan dan disetujui SEBELUM kode ditulis, dengan dua koreksi user (gerbang tinjau = `ceo`+`is_hrd_kadiv()` SAJA tanpa `pusat`; Inservice juga dapat assignment `cs`).

**Cuti (migrasi `0025_cuti.sql` + `0026_cuti_insert_guard.sql`):**
- Tabel `cuti` (`user_id`, `tanggal_mulai`, `tanggal_selesai`, `jenis` enum cuti/sakit/izin, `keterangan`, `status` enum diajukan/disetujui/ditolak, `surat_path`, `catatan_keputusan` -- TERPISAH dari `keterangan` supaya catatan keputusan HRD tidak pernah menimpa alasan asli karyawan, `disetujui_oleh`, `disetujui_at`).
- Gerbang tinjau **SENGAJA BEDA dari Tinjau Absensi**: `ceo` + `is_hrd_kadiv()` SAJA (fungsi yang sama dari migrasi `0022`, TIDAK dibuat gerbang baru) -- **TANPA `pusat`**, instruksi eksplisit user (koreksi 1): "Peran 'pusat' TIDAK otomatis dapat akses -- kalau suatu saat ada orang lain berperan pusat tanpa HRD, dia tidak boleh menyetujui cuti orang." Sabrina tetap ikut karena dia HRD (kadiv+divisi HRD), bukan karena dia `pusat`.
- `putuskan_cuti(p_id, p_disetujui, p_catatan)` -- RPC security definer, guard manual, pola sama `putuskan_absensi()`. Tidak ada policy update untuk klien biasa sama sekali.
- **Celah RLS ditemukan sendiri sebelum sempat di-commit** (`0026`): `cuti_insert` awalnya cuma memeriksa `user_id = auth.uid()`, tidak memeriksa kolom `status` -- karyawan mana pun bisa insert baris `status:'disetujui'` langsung lewat REST, MELEWATI `putuskan_cuti()` sepenuhnya, dan baris itu akan ikut mengecualikan `hari_bolong`-nya sendiri tanpa persetujuan siapa pun. Pola jebakan sama seperti `04-CATATAN-TEKNIS.md` §3.2 (RLS membatasi BARIS, bukan otomatis ISI KOLOM). Diperbaiki: `with check (user_id = auth.uid() and status = 'diajukan' and disetujui_oleh is null and disetujui_at is null)`.
- Storage bucket `cuti` (privat, path `{user_id}/...`, pola sama `absensi`) untuk lampiran surat opsional (JPEG/PNG/PDF).
- Halaman `/cuti` (pengajuan -- semua yang login) dan `/cuti/tinjau` (persetujuan -- `Terlindungi peran={['ceo']} boleh={bolehHrdKadiv}`, TANPA `'pusat'` di daftar peran, beda sengaja dari `/absen/tinjau`).

**Satu rumus, bukan rumus ketiga:** `marketing_bulanan_untuk(p_bulan)` (migrasi 0023, sudah jadi satu-satunya sumber sejak 0024 menyatukan `v_marketing_bulanan`) di-`CREATE OR REPLACE` dengan signature PERSIS SAMA -- satu-satunya perubahan: `generate_series` `hari_wajib` sekarang mengecualikan hari yang tertutup baris `cuti` berstatus `disetujui`. Karena `v_marketing_bulanan` delegasi ke sini, dashboard Marketing (Task 22) otomatis ikut benar tanpa disentuh.

**§3.5b lagi ("satu angka, satu pengisi"):** field manual `pegawai_sakit`/`pegawai_izin`/`pegawai_cuti` DIHAPUS dari `forms/f14-hrd.ts` blok "Absensi Hari Ini" -- diganti panel baca-saja `AbsensiCutiOtomatis` (`components/LaporForm.tsx`) yang menghitung dari tabel `cuti` (status disetujui, mencakup hari ini) lewat `useCutiUntukTanggal` (`lib/api/cuti.ts`). Bagian 5 "HRD/Absensi" di Laporan Terpusat (`app/terpusat/page.tsx`) ikut disambungkan ke sumber yang sama, bukan lagi baca `hrd.data.pegawai_sakit` dst. `daftar_tidak_hadir` (tabel manual per-orang termasuk tanpa keterangan) SENGAJA TETAP -- itu detail yang lebih kaya dari `cuti`, bukan duplikat.

**Diverifikasi lewat DB sungguhan** (`scripts/uji-cuti-rls.mjs`, 1 transaksi BEGIN/ROLLBACK, 12 titik, **SEMUA LOLOS**):
1. Karyawan (Toyib) insert pengajuan sendiri (`status` default `'diajukan'`) -- berhasil.
2. Karyawan insert LANGSUNG dengan `status:'disetujui'` (celah yang diperbaiki 0026) -- **ditolak**.
3. Pemilik select baris sendiri -- 1 baris.
4. Karyawan lain (belum dipinjami role) select baris orang lain -- 0 baris.
5. **Koreksi 1 dibuktikan dengan satu persona ganda**: Kasam dipinjami role `'pusat'` DAN `'kadiv'` sekaligus (divisi tetap DTI, BUKAN HRD) -- select baris orang lain TETAP 0 baris (`'pusat'` saja tidak cukup, `kadiv` divisi lain juga tidak cukup).
6. `kadiv`+divisi HRD (Fauzy dipinjam, pola sama `uji-presensi-rls.mjs`) -- select baris orang lain -- 1 baris.
7. `ceo` (Putri) -- select baris orang lain -- 1 baris.
8. Tidak ada policy update untuk klien -- pemilik sendiri update `status` langsung (bukan RPC) -- 0 baris terupdate.
9. `putuskan_cuti()` dipanggil karyawan biasa -- ditolak.
10. `putuskan_cuti()` dipanggil persona `'pusat'`+`kadiv`(DTI) -- **tetap ditolak**.
11. `putuskan_cuti()` dipanggil `kadiv`+HRD (Fauzy) -- berhasil, `status='disetujui'`, `disetujui_oleh` = Fauzy.
12. `marketing_bulanan_untuk()`: satu hari kerja nyata yang tertutup cuti disetujui Toyib -- `hari_wajib` = (hari kerja penuh bulan berjalan) − 1, persis harapan.

**Belum diuji (dicatat, bukan diam-diam dilewati):** interaksi manusia-ke-layar sungguhan di browser (`/cuti` isi form + unggah surat, `/cuti/tinjau` klik Setujui/Tolak) -- logikanya SUDAH teruji penuh lewat DB (12 titik di atas), sama seperti presensi saat pertama dibangun. Uji sesi HTTP sungguhan (pola `scripts/uji-ekspor-keuangan-curl.mjs`) TIDAK dipakai di sini secara sengaja -- itu metode untuk Route Handler berbekal service-role key; `/cuti` dan `/cuti/tinjau` murni klien + RLS, jadi penyamaran JWT database-layer (pola `uji-presensi-rls.mjs`) adalah alat yang tepat sesuai pengecualian yang sudah disepakati ("kecuali memang perlu menguji lapisan database").

`tsc --noEmit`/`npm run lint`/`npm run build` bersih (0 error, 1 warning lama tidak terkait, `/cuti` dan `/cuti/tinjau` muncul di daftar rute build).

**Catatan Inservice/CS (menutup pertanyaan §2 DATA-KARYAWAN.md nomor 3, lanjutan):** CEO melengkapi jawaban -- Dedi/Yundi/Fauzan/Cahya (Inservice) JUGA bertugas CS. Karena form `cs` `scope:'global'` awalnya diasumsikan SATU pengisi per hari (`lib/api/terpusat.ts` -- `useLaporanHariIni` dgn `.maybeSingle()`, akan ERROR kalau >1 baris), diperiksa dulu ke user sebelum diterapkan (instruksi eksplisit: "jangan diputuskan sendiri, ini menyentuh bentuk data"). Diputuskan: TETAP satu form `cs` untuk semua (bukan berbasis lokasi, bukan satu-orang-ditunjuk) -- kode rollup Terpusat diperbaiki: `useLaporanCsHariIni` (baru) mengambil SEMUA baris `cs` hari itu, `app/terpusat/page.tsx` Bagian 2 menjumlahkan angka lintas pengisi + menampilkan tiap masalah urgent per orang. Papan Kontrol TIDAK perlu diubah -- `papan_untuk_tanggal` (migrasi 0020) sudah join per `assignment` dengan `author_id`, otomatis benar untuk banyak pengisi. `docs/DATA-KARYAWAN.md` §1/§2 diperbarui dua kali (Koreksi 1 lalu Koreksi 2).

## Utang WAJIB sebelum go-live penggajian sungguhan

- ✅ **SELESAI (30 Agustus 2026, batch Cuti di atas)** -- ~~cuti/sakit/izin yang disetujui belum mengecualikan `hari_bolong`~~. Ditemukan & dicatat 22 Agustus 2026, ditegaskan ulang 29 Agustus 2026. Ditutup lewat tabel `cuti` + halaman `/cuti`/`/cuti/tinjau` + `marketing_bulanan_untuk()` yang sekarang mengecualikan hari cuti disetujui dari `hari_wajib` (bukti: `scripts/uji-cuti-rls.mjs` titik #12). **Penghalang TEKNIS ini sekarang tertutup.**
- ✅ **Konsekuensi dari gerbang di atas juga tertutup TEKNISNYA** -- `policy.pte_mulai_berlaku` sekarang AMAN diisi kapan pun tanpa merugikan karyawan yang cuti resmi. **Tapi TANGGAL AKTIVASINYA TETAP KEPUTUSAN CEO, BUKAN AGENT** -- ditegaskan eksplisit user (30 Agustus 2026): "Betul juga soal pte_mulai_berlaku: penghalang teknisnya tertutup setelah ini, tapi tanggalnya tetap aku yang isi, bukan kamu." Nilai produksi TETAP `null` sampai CEO sendiri yang mengisinya lewat Admin -- jangan diisi tanggal apa pun oleh siapa pun selain CEO.

## Utang uji (jangan sampai hilang)

- ✅ **SELESAI (29 Agustus 2026, Task 24)** — ~~Matriks RLS #3 (`select * from v_keuangan_rekap` sebagai `pusat` → harap 4 kolom saja, tanpa saldo bank) belum bisa diuji~~. Dijalankan lewat `scripts/uji-task24-matriks.mjs`: persis 5 kolom (`tanggal`/`total_masuk`/`total_keluar`/`net`/`warna`), `net` terhitung benar, umpan `saldo_bank:"RAHASIA_UMPAN..."` yang ditanam di `data` JSON laporan sumber TIDAK ikut bocor lewat view. LOLOS.
- ✅ **SELESAI (29 Agustus 2026, Task 24)** — ~~Matriks RLS #10 (buka path storage orang lain tanpa signed URL → harap ditolak) belum bisa diuji~~. Dijalankan lewat `scripts/uji-task24-matriks.mjs`: objek storage ditanam untuk laporan Toyib, karyawan lain (Kasam, tanpa peran istimewa) mencoba `select` langsung ke `storage.objects` (bukan lewat `createSignedUrl`) → 0 baris. LOLOS.
- ✅ **SELESAI (29 Agustus 2026, Task 24)** — Seluruh 14 baris matriks §4 `04-CATATAN-TEKNIS.md` dijalankan sekali jalan dalam satu transaksi (`scripts/uji-task24-matriks.mjs`, BEGIN/ROLLBACK, tidak ada data tersisa) — **LOLOS SEMUA, tanpa perbaikan yang diperlukan**. Detail 14 baris (skenario/harapan/hasil mentah) ada di laporan Task 24 yang dikirim ke user, bukan disalin ulang di sini.
- ✅ **SELESAI (23 Agustus 2026)** — ~~`can_see_report()` tidak memberi role `kadiv` akses ke laporan `pic_lokasi` milik orang lain~~. Diselesaikan BUKAN dengan melebarkan `can_see_report()` (semua opsi begitu ditolak user), melainkan opsi (d): view agregat `security_invoker=off` + penjaga `boleh_lihat_rekap()` (`04-CATATAN-TEKNIS.md` §3.4b, migrasi `0009_boleh_lihat_rekap.sql`). Detail lengkap + output mentah 3 uji di bagian "Revisi Batch D" di atas, subbagian "Diselesaikan — opsi (d)".

## Catatan lintas-task

- **BASI (per 23 Agustus 2026, dibiarkan di sini sebagai riwayat):** `docs/REFERENSI-FORMAT-LAPORAN.md` sekarang SUDAH ADA dan sudah dipakai penuh (Task 12). `docs/02-FORMAT-LAPORAN-DIVISI-BARU.md` sudah dipakai lengkap utk `pic_lokasi` (Task 13) + `hrd`/`security`/`perizinan` (Task 14 sebagian) + `pembangunan`/`dti`/`kendaraan`/`cs`/`ga` (Task 15 penuh) — 9 dari 9 formatnya sudah terpakai semua. Yang MASIH belum ada di repo, dan MASIH memblokir: `it` (Task 14 sisa), `pusat`, `accounting`, `manager_resto`, `ita` (Task 16, 17) — user sudah eksplisit menyuruh JANGAN sentuh keempat form terakhir itu sampai formatnya tersedia (instruksi Batch-D-diurutkan-ulang, 23 Agustus 2026).
- `docs/MODE-OTONOM.md` yang dirujuk instruksi awal tidak ada. Urutan batch & aturan checkpoint diambil dari prompt pertama user langsung, dikonfirmasi user.
- `CLAUDE.md` awalnya ditemukan di `docs/CLAUDE.md`, dipindahkan ke root proyek atas konfirmasi user.
- `docs/01-SPESIFIKASI-SISTEM.md` adalah dokumen LAMA yang sudah digantikan `BLUEPRINT.md` — isinya bertentangan (mengusulkan tabel `laporan_field` bergaya EAV, sedangkan BLUEPRINT memutuskan data laporan disimpan JSONB di `report.data`). Dipindahkan ke `docs/arsip/` atas instruksi user. **Jangan pernah dipakai sebagai acuan.**
- `docs/02-PROMPT-PACK.md` adalah prompt untuk manusia menempel ke agent, bukan dokumen untuk dibaca agent. Dilewati.
- `docs/05-RENCANA-FASE-2.md` adalah rencana setelah Fase 1 selesai. Belum dibaca, jangan dikerjakan sekarang.
